# MalformedIdError class

The malformed-id error response — the single permitted id exception (spec §7.2; R-7.2-h). An error reply to a request whose id could not be read MAY carry a `null` id or omit it entirely. The C# counterpart of the TypeScript `buildParseErrorResponse` / `MalformedIdErrorResponseSchema` / `isAcceptableMalformedIdErrorResponse` helpers.

```csharp
public static class MalformedIdError
```

## Public Members

| name | description |
| --- | --- |
| const [ParseErrorCode](MalformedIdError/ParseErrorCode.md) | The standard JSON-RPC "Parse error" code (spec §22 / §7.2): `-32700`. |
| static [BuildParseErrorResponse](MalformedIdError/BuildParseErrorResponse.md)(…) | Builds a parse-error response for a request whose id could not be read (spec §7.2; R-7.2-h). Mirrors TypeScript `buildParseErrorResponse`. |
| static [BuildParseErrorResponseNode](MalformedIdError/BuildParseErrorResponseNode.md)(…) | Builds the wire JSON object for a parse-error response, honoring the explicit-`null` vs omitted-id distinction (spec §7.2; R-7.2-h). |
| static [IsAcceptableMalformedIdErrorResponse](MalformedIdError/IsAcceptableMalformedIdErrorResponse.md)(…) | Returns `true` when *value* is an acceptable malformed-id error response: a JSON-RPC 2.0 error object whose id is a string, a number, `null`, or omitted (spec §7.2; R-7.2-h). Mirrors TypeScript `isAcceptableMalformedIdErrorResponse`. |

## See Also

* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
