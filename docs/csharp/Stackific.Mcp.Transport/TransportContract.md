# TransportContract class

Transport-contract helpers: directionality (§7.4) and the per-request statelessness model (§7.4, §7.6). The C# counterpart of the free functions in the TypeScript `transport/contract.ts`.

```csharp
public static class TransportContract
```

## Public Members

| name | description |
| --- | --- |
| static [DeriveRequestContext](TransportContract/DeriveRequestContext.md)(…) | Derives the per-request context (protocol version, client identity, client capabilities) solely from the request's own `_meta`, never from the connection or any prior request (spec §7.6; R-7.6-e, R-7.6-f). Mirrors TypeScript `deriveRequestContext`. (2 methods) |
| static [ExtractEnvelopeForMirroring](TransportContract/ExtractEnvelopeForMirroring.md)(…) | Extracts the envelope fields a transport MAY mirror into transport-level metadata for routing/inspection (for example HTTP headers; see S14/S15) (spec §7.4; R-7.4-e). Mirrors TypeScript `extractEnvelopeForMirroring`. |
| static [IsDirectionPermitted](TransportContract/IsDirectionPermitted.md)(…) | Returns `true` when a message of *kind* may travel in *direction* (spec §7.4). Mirrors TypeScript `isDirectionPermitted`. |
| static [KindOf](TransportContract/KindOf.md)(…) | Maps a [`JsonRpcMessage`](../Stackific.Mcp.JsonRpc/JsonRpcMessage.md) to its [`DirectionalKind`](./DirectionalKind.md) for directionality enforcement. Because the message is already a classified record, this is a pure pattern match — the C# analogue of running TypeScript's `classifyMessage` then collapsing the two response kinds. |
| static [RequestCarriesMetaEnvelope](TransportContract/RequestCarriesMetaEnvelope.md)(…) | Returns `true` when a request carries the inline `_meta` envelope with the three reserved `io.modelcontextprotocol/*` keys (spec §7.4; R-7.4-d, R-7.4-f). Mirrors TypeScript `requestCarriesMetaEnvelope`. |

## See Also

* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
