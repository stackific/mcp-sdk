# IMcpSubscriptionHandler interface

A handler that can open long-lived `subscriptions/listen` streams (spec §10). A transport that supports server-to-client streaming registers a subscription and pumps the delivered notifications onto the stream until the client disconnects.

```csharp
public interface IMcpSubscriptionHandler
```

## Members

| name | description |
| --- | --- |
| [SupportsSubscriptions](IMcpSubscriptionHandler/SupportsSubscriptions.md) { get; } | Whether this handler can serve subscription streams. |
| [OpenSubscription](IMcpSubscriptionHandler/OpenSubscription.md)(…) | Registers a subscription stream (spec §10.3) and returns the filter the server agreed to honor plus a teardown handle that unregisters the subscription when the stream closes. |

## See Also

* namespace [Stackific.Mcp.Transport](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
