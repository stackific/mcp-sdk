# IServerNotifier interface

The sink a request handler uses to emit request-scoped notifications (progress, logging) on the originating request's response stream (spec §9.6.2, §10.6). The transport decides how to deliver them: on Streamable HTTP they ride the request's SSE stream; in memory they are handed straight to the client. A notification emitted here MUST relate to the request being processed.

```csharp
public interface IServerNotifier
```

## Members

| name | description |
| --- | --- |
| [NotifyAsync](IServerNotifier/NotifyAsync.md)(…) | Emits a request-scoped notification on the originating request's stream. |

## See Also

* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
