# RequestCorrelator class

Correlates inbound responses to outstanding requests by id only — never by delivery order, connection, stream, or position (spec §7.2; R-7.2-e – R-7.2-g, R-7.2-o). The C# counterpart of the TypeScript `RequestCorrelator`.

```csharp
public sealed class RequestCorrelator
```

## Public Members

| name | description |
| --- | --- |
| [RequestCorrelator](RequestCorrelator/RequestCorrelator.md)() | The default constructor. |
| [Outstanding](RequestCorrelator/Outstanding.md) { get; } | A snapshot of the currently outstanding ids. |
| [Size](RequestCorrelator/Size.md) { get; } | The number of currently outstanding requests. |
| [Deliver](RequestCorrelator/Deliver.md)(…) | Delivers an inbound response, resolving the matching outstanding request's Task (spec §7.2). Matching is purely by id; the order in which responses are delivered is irrelevant (R-7.2-m, R-7.2-n, R-7.2-p). Mirrors TypeScript `RequestCorrelator.deliver`. |
| [Fail](RequestCorrelator/Fail.md)(…) | Fails a single outstanding request with a transport-level error, faulting its Task so the caller can observe the failure rather than waiting forever (spec §7.5; R-7.5-d, R-7.5-e). Mirrors TypeScript `RequestCorrelator.fail`. |
| [FailAll](RequestCorrelator/FailAll.md)(…) | Fails every outstanding request — the action a transport takes on abrupt or clean disconnection so no in-flight request can hang (spec §7.5; R-7.5-c, R-7.5-d, R-7.5-e). Mirrors TypeScript `RequestCorrelator.failAll`. |
| [Has](RequestCorrelator/Has.md)(…) | Returns `true` when *id* is currently outstanding. |
| [Issue](RequestCorrelator/Issue.md)(…) | Registers *id* as outstanding and returns a Task that completes when a matching response is delivered or the request is failed (spec §7.2). |

## Remarks

Typical use by a sender: issue a Task per request id (without blocking), wire the transport's inbound-message handler to [`Deliver`](./RequestCorrelator/Deliver.md), and wire its close handler to [`FailAll`](./RequestCorrelator/FailAll.md). Awaiting the issued Task resolves whenever the matching id arrives, even out of order.

A string `"1"` and a number `1` are kept distinct because they are different JSON types — matching S03's id rules (R-3.2-f, R-3.2-g). This is guaranteed by [`RequestId`](../Stackific.Mcp.JsonRpc/RequestId.md)'s own type-fidelity equality and hashing, so the correlator keys directly on the [`RequestId`](../Stackific.Mcp.JsonRpc/RequestId.md).

The class is safe for concurrent use: issuing, delivering, and failing requests from multiple threads is supported via the backing ConcurrentDictionary and atomic removals, so no two callers can resolve the same entry twice.

## See Also

* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
