# StdioClientTransport class

The client side of a stdio connection: launches/holds a server subprocess, writes requests/notifications to its stdin, and reads responses/notifications from its stdout (spec §8 client role). The C# counterpart of the TypeScript `StdioClientTransport`.

```csharp
public sealed class StdioClientTransport : StdioEndpoint
```

## Public Members

| name | description |
| --- | --- |
| [StdioClientTransport](StdioClientTransport/StdioClientTransport.md)(…) | Launches/holds the subprocess and wires its streams and exit handling. |
| [Correlator](StdioClientTransport/Correlator.md) { get; } | Sender-side correlator; reused across a restart so ids may be retried (spec §8.6.4-b). |
| [SupportCache](StdioClientTransport/SupportCache.md) { get; } | Per-endpoint protocol-support cache for the §5.7 probe (spec §5.7-e). |
| [CapturedStderr](StdioClientTransport/CapturedStderr.md)() | A copy of the captured stderr bytes (the client MAY forward/ignore them) (spec §8.4-c). |
| [CapturedStderrText](StdioClientTransport/CapturedStderrText.md)() | The captured stderr decoded as UTF-8 text, for convenient inspection (spec §8.4-a/c). |
| override [CloseAsync](StdioClientTransport/CloseAsync.md)(…) | Graceful shutdown (spec §8.6.2-a): (1) close the child's stdin (EOF — the only portable graceful signal), (2) wait for the process to exit, (3) forcibly terminate it if it does not exit within `ShutdownGraceMs` (R-8.6.3-a). Resolves once the process has exited (or been force-terminated). The close is observable via [`OnClose`](./StdioEndpoint/OnClose.md) with `Clean: true`. |
| [DeliverResponse](StdioClientTransport/DeliverResponse.md)(…) | Delivers an inbound response to the correlator and returns whether it matched an outstanding request — a convenience for callers wiring [`OnMessage`](./StdioEndpoint/OnMessage.md) to the reused [`Correlator`](./StdioClientTransport/Correlator.md). |
| [OnRestart](StdioClientTransport/OnRestart.md)(…) | Registers a handler invoked with the fresh child after a restart. |
| [ProbeProtocol](StdioClientTransport/ProbeProtocol.md)(…) | Sends a `server/discover` probe response through the §5.7 classifier and caches the per-endpoint determination (spec §5.7; R-8.7-d, R-8.7-h). Mirrors the TypeScript `StdioClientTransport.probeProtocol`. |
| static [ProbeMethod](StdioClientTransport/ProbeMethod.md) { get; } | The method a `server/discover` probe carries (for building the probe request). |

## Remarks

Responsibilities beyond framing:

* Stream-role enforcement: only requests/notifications, and only valid MCP messages, may go to stdin (R-8.3-a, R-8.5-c).
* stderr handling: captured/forwarded/ignored, never parsed as protocol, never assumed to mean an error (R-8.4-c, R-8.4-d, R-8.4-e, R-8.1-a).
* Graceful shutdown: close stdin (EOF), await exit, force-terminate on timeout (R-8.6.2-a, R-8.6.3-a).
* Unexpected-exit restart (SHOULD) and lost in-flight retry (MAY) (R-8.6.4-a, R-8.6.4-b).

## See Also

* class [StdioEndpoint](./StdioEndpoint.md)
* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
