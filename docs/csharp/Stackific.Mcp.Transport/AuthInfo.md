# AuthInfo record

The validated authorization identity a transport threads into request processing when a request carried a bearer token (spec §23). It is per-request input (never connection state, §4.4) and is surfaced to tool handlers via the request context.

```csharp
public record AuthInfo
```

## Public Members

| name | description |
| --- | --- |
| [AuthInfo](AuthInfo/AuthInfo.md)(…) | The validated authorization identity a transport threads into request processing when a request carried a bearer token (spec §23). It is per-request input (never connection state, §4.4) and is surfaced to tool handlers via the request context. |
| [Audience](AuthInfo/Audience.md) { get; set; } | The token's bound audience (the protected resource), if known (§23.6). |
| [ClientId](AuthInfo/ClientId.md) { get; set; } | The authenticated client identifier, if known. |
| [ExpiresAt](AuthInfo/ExpiresAt.md) { get; set; } | The token expiry as a Unix timestamp in seconds, if known. |
| [Scopes](AuthInfo/Scopes.md) { get; set; } | The granted OAuth scopes. |
| [Token](AuthInfo/Token.md) { get; set; } | The raw bearer token presented on the request. |

## See Also

* namespace [Stackific.Mcp.Transport](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
