# StreamableHttpServer class

The server-side Streamable HTTP adapter (spec §9): it parses an HTTP POST into a JSON-RPC message, validates the required request and routing headers (§9.3/§9.4), runs an optional authorization gate (§23), dispatches to an [`IMcpRequestHandler`](./IMcpRequestHandler.md), and writes back either a single JSON response (§9.6.1) or a server-sent event stream that carries request-scoped notifications before the final response (§9.6.2). It is stateless and never mints a session id (§9.9).

```csharp
public static class StreamableHttpServer
```

## Public Members

| name | description |
| --- | --- |
| const [LegacyEndpointEvent](StreamableHttpServer/LegacyEndpointEvent.md) | The SSE event name that, as the first event of a legacy `GET` stream, marks the deprecated HTTP+SSE transport. (R-9.12-h) |
| const [LoopbackBindAddress](StreamableHttpServer/LoopbackBindAddress.md) | The loopback interface a locally-run server SHOULD bind to. (R-9.11-d) |
| static [HandleAsync](StreamableHttpServer/HandleAsync.md)(…) | Handles a single HTTP request against *handler* (spec §9). |
| static [InterpretPostForFallback](StreamableHttpServer/InterpretPostForFallback.md)(…) | Interprets the outcome of a modern POST for a client that also supports an earlier `initialize`-handshake revision. (§9.12) |
| static [IsIgnoredStatelessHeader](StreamableHttpServer/IsIgnoredStatelessHeader.md)(…) | Returns `true` when *name* is a header this stateless transport MUST ignore: a session-identifier header (`Mcp-Session-Id`/`X-Session-Id`/`Session-Id`) or the `Last-Event-ID` resumption header. (R-9.9-b/c/d/g, R-9.6.2-h) Comparison is case-insensitive. |
| static [IsLegacyHttpSseServer](StreamableHttpServer/IsLegacyHttpSseServer.md)(…) | Returns `true` when the first event of a fallback `GET` stream is the legacy `endpoint` event, marking the deprecated HTTP+SSE transport. (R-9.12-h) |
| static [MapMcp](StreamableHttpServer/MapMcp.md)(…) | Maps the MCP endpoint at *pattern* onto *handler*. |
| static [OriginAccepted](StreamableHttpServer/OriginAccepted.md)(…) | Decides whether a present `Origin` is accepted (DNS-rebinding defense, §9.11). When *allowedOrigins* is `null`, only loopback-shaped origins are accepted (the loopback-safe default for a local server); when it is a set, the match is exact. (R-9.11-a/b) |
| enum [PostFallbackAction](StreamableHttpServer.PostFallbackAction.md) | The decision a dual-revision client makes after a modern POST. (§9.12) |
| record [PostFallbackDecision](StreamableHttpServer.PostFallbackDecision.md) | The outcome of [`InterpretPostForFallback`](./StreamableHttpServer/InterpretPostForFallback.md). |

## See Also

* namespace [Stackific.Mcp.Transport](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
