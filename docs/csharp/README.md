# Stackific.Mcp assembly

## Stackific.Mcp namespace

| public type | description |
| --- | --- |
| static class [McpJson](./Stackific.Mcp/McpJson.md) | The single, shared JsonSerializerOptions used for every MCP wire (de)serialization in this SDK. |

## Stackific.Mcp.Client namespace

| public type | description |
| --- | --- |
| class [McpClient](./Stackific.Mcp.Client/McpClient.md) | The MCP client host (spec §4, §5): it stamps every request with the required per-request `_meta` envelope (protocol revision, client identity, client capabilities), correlates each response to its request, performs discovery and revision selection, and exposes typed convenience methods for the server features. It is transport-agnostic — give it any [`ClientTransport`](./Stackific.Mcp.Transport/ClientTransport.md). |
| static class [OAuth](./Stackific.Mcp.Client/OAuth.md) | An OAuth 2.1 client flow (spec §23) for an MCP [`StreamableHttpClientTransport`](./Stackific.Mcp.Transport/StreamableHttpClientTransport.md): PKCE (`S256`) generation and the §28.5 support gate, two-stage protected-resource → authorization-server metadata discovery with the RFC 8414/OIDC fallback ordering and the RFC 9207 issuer mix-up check, parsing of the `WWW-Authenticate` challenge on a `401`, dynamic client registration (RFC 7591) with retryable-failure surfacing, the authorize-URL builder, the authorization-code token exchange and the refresh-token exchange (RFC 8707 audience binding), and redirect verification (CSRF/mix-up defenses). The C# counterpart of ts-sdk's `client/oauth.ts`. |

## Stackific.Mcp.Json namespace

| public type | description |
| --- | --- |
| static class [JsonValues](./Stackific.Mcp.Json/JsonValues.md) | The JSON value model and numeric-handling helpers for MCP (spec §2.3, §2.5). |
| static class [MetaKeys](./Stackific.Mcp.Json/MetaKeys.md) | The reserved `_meta` keys defined by the protocol, plus the structural naming rules for any `_meta` key (spec §2.6.2 / §4.2, Appendix C). |

## Stackific.Mcp.JsonRpc namespace

| public type | description |
| --- | --- |
| static class [Dispatch](./Stackific.Mcp.JsonRpc/Dispatch.md) | Method dispatch for JSON-RPC request handling (§3.3, R-3.3-i, R-3.3-j, R-3.3-k). |
| struct [DispatchOutcome](./Stackific.Mcp.JsonRpc/DispatchOutcome.md) | The result of attempting to dispatch a request: either success, or a failure carrying the error response that should be sent back to the requester. |
| static class [ErrorCodes](./Stackific.Mcp.JsonRpc/ErrorCodes.md) | The complete registry of JSON-RPC error codes used by MCP (spec §22.2/§22.3 and Appendix B). The numeric values are part of the wire contract and are matched exactly. |
| static class [Framing](./Stackific.Mcp.JsonRpc/Framing.md) | Identifier-echo validation and in-flight request-id tracking for JSON-RPC framing (spec §3.2). |
| class [InFlightTracker](./Stackific.Mcp.JsonRpc/InFlightTracker.md) | Tracks in-flight request identifiers for a single sender on a single connection, enforcing the uniqueness rules of §3.2. |
| static class [JsonRpcConstants](./Stackific.Mcp.JsonRpc/JsonRpcConstants.md) | The exact string an MCP message's `jsonrpc` member must carry (spec §3.1). |
| record [JsonRpcError](./Stackific.Mcp.JsonRpc/JsonRpcError.md) | The JSON-RPC `error` object carried by an error response (spec §3.8/§22.1): an integer [`Code`](./Stackific.Mcp.JsonRpc/JsonRpcError/Code.md), a human-readable [`Message`](./Stackific.Mcp.JsonRpc/JsonRpcError/Message.md), and OPTIONAL structured [`Data`](./Stackific.Mcp.JsonRpc/JsonRpcError/Data.md). |
| record [JsonRpcErrorResponse](./Stackific.Mcp.JsonRpc/JsonRpcErrorResponse.md) | A JSON-RPC error response (§3.5.2/§22.1): carries an [`Error`](./Stackific.Mcp.JsonRpc/JsonRpcErrorResponse/Error.md) and, normally, the answered [`Id`](./Stackific.Mcp.JsonRpc/JsonRpcErrorResponse/Id.md). The id MAY be absent only when the originating request's id could not be determined (§22.6). |
| abstract record [JsonRpcMessage](./Stackific.Mcp.JsonRpc/JsonRpcMessage.md) | Base type for the three kinds of framed JSON-RPC message — request, notification, and response (success or error) — defined in §3. A message's kind is determined structurally (§3.1); see [`JsonRpcMessageSerializer`](./Stackific.Mcp.JsonRpc/JsonRpcMessageSerializer.md) for classification and (de)serialization. |
| static class [JsonRpcMessageSerializer](./Stackific.Mcp.JsonRpc/JsonRpcMessageSerializer.md) | Classifies raw JSON into a [`JsonRpcMessage`](./Stackific.Mcp.JsonRpc/JsonRpcMessage.md) per the structural rules of §3.1, and renders messages back to wire text. Malformed input is rejected with the correct JSON-RPC error (parse → -32700, structurally invalid → -32600). |
| record [JsonRpcNotification](./Stackific.Mcp.JsonRpc/JsonRpcNotification.md) | A JSON-RPC notification (§3.4): a one-way message with a [`Method`](./Stackific.Mcp.JsonRpc/JsonRpcNotification/Method.md) but no id, and no response. |
| record [JsonRpcRequest](./Stackific.Mcp.JsonRpc/JsonRpcRequest.md) | A JSON-RPC request (§3.3): carries an [`Id`](./Stackific.Mcp.JsonRpc/JsonRpcRequest/Id.md) and a [`Method`](./Stackific.Mcp.JsonRpc/JsonRpcRequest/Method.md), and expects exactly one response. |
| record [JsonRpcSuccessResponse](./Stackific.Mcp.JsonRpc/JsonRpcSuccessResponse.md) | A JSON-RPC success response (§3.5.1): carries the answered [`Id`](./Stackific.Mcp.JsonRpc/JsonRpcSuccessResponse/Id.md) and a [`Result`](./Stackific.Mcp.JsonRpc/JsonRpcSuccessResponse/Result.md) object. |
| class [MalformedMessageError](./Stackific.Mcp.JsonRpc/MalformedMessageError.md) | Thrown when a received message is structurally malformed and must be rejected (spec §3.1). |
| class [McpError](./Stackific.Mcp.JsonRpc/McpError.md) | A protocol-level failure that maps to a JSON-RPC error response (spec §22). Throwing an [`McpError`](./Stackific.Mcp.JsonRpc/McpError.md) anywhere in request processing lets the transport translate it into the correct `error` object — and, on HTTP, the correct status code. |
| class [MethodDescriptor](./Stackific.Mcp.JsonRpc/MethodDescriptor.md) | Describes a method that a receiver recognizes, for use by [`DispatchRequest`](./Stackific.Mcp.JsonRpc/Dispatch/DispatchRequest.md). |
| class [MethodRegistry](./Stackific.Mcp.JsonRpc/MethodRegistry.md) | Maps a method name to its [`MethodDescriptor`](./Stackific.Mcp.JsonRpc/MethodDescriptor.md) for every method the receiver handles. |
| static class [Payload](./Stackific.Mcp.JsonRpc/Payload.md) | S04 payload-shape helpers — `resultType` interpretation plus reusable validators for the base `Result`, `RequestParams`, `NotificationParams`, `ProgressToken`, `Cursor`, `EmptyResult`, and the `error` object (§3.6–§3.9). |
| struct [RequestId](./Stackific.Mcp.JsonRpc/RequestId.md) | A JSON-RPC request identifier (spec §3.2): either a JSON string or a JSON number, and never `null`. The wire type is preserved exactly — a numeric id round-trips as a number and a string id as a string, and the two are never coerced into one another. Integral numbers are kept as a 64-bit integer so large ids round-trip without the precision loss a Double would introduce. |
| struct [ResultTypeInterpretation](./Stackific.Mcp.JsonRpc/ResultTypeInterpretation.md) | The outcome of interpreting a received result's `resultType` field (§3.6). |
| static class [ResultTypeNames](./Stackific.Mcp.JsonRpc/ResultTypeNames.md) | The two `resultType` values defined by this specification (§3.6, R-3.6-e). |

## Stackific.Mcp.Lifecycle namespace

| public type | description |
| --- | --- |
| static class [DeprecatedRegistry](./Stackific.Mcp.Lifecycle/DeprecatedRegistry.md) | The registry of currently deprecated MCP features (spec §27.3) plus the out-of-band deprecation warning emitter (§27.4). The C# counterpart of the TypeScript `lifecycle/registry.ts` module. |
| record [DeprecatedRegistryEntry](./Stackific.Mcp.Lifecycle/DeprecatedRegistryEntry.md) | One row of the derived registry of deprecated features (spec §27.3). The registry is a consolidated, derived view; the per-feature notices at the authoritative defining sections resolve any conflict. |
| static class [LifecyclePolicy](./Stackific.Mcp.Lifecycle/LifecyclePolicy.md) | Deprecation-policy enforcement for the feature lifecycle state machine (spec §27.2): the legal state transitions and the minimum deprecation windows. This is the C# counterpart of the TypeScript `lifecycle/policy.ts` module. |
| record [LifecycleRecord](./Stackific.Mcp.Lifecycle/LifecycleRecord.md) | Per-feature lifecycle bookkeeping (spec §27.1, §27.2). This is a conceptual governance record, not a wire type — it captures where a feature sits in its lifecycle and the metadata the policy rules consume (deprecation date, earliest-removal revision, migration note, expedited flag). |
| enum [LifecycleState](./Stackific.Mcp.Lifecycle/LifecycleState.md) | The three lifecycle states a governed protocol feature can be in (spec §27.1). Every governed feature is in exactly one of these states at any time, and the allowed transitions between them (enforced by `Stackific.Mcp.Lifecycle.LifecyclePolicy`) encode the deprecation policy of §27.2. |

## Stackific.Mcp.Protocol namespace

| public type | description |
| --- | --- |
| static class [AccessTokenUsage](./Stackific.Mcp.Protocol/AccessTokenUsage.md) | Resource Indicators / audience binding (§23.6) and access-token usage / server-side per-request validation (§23.8): the `resource` parameter, audience validation (string OR array), token selection, the bearer header, and the `401`/`403` validation state machine. |
| record [AccessTokenValidationResult](./Stackific.Mcp.Protocol/AccessTokenValidationResult.md) | The outcome of [`ValidateRequest`](./Stackific.Mcp.Protocol/AccessTokenUsage/ValidateRequest.md): authorized, or a challenge to return. |
| record [Annotations](./Stackific.Mcp.Protocol/Annotations.md) | Optional, untrusted hints about a piece of content or a resource (spec §14.6). A consumer MUST NOT rely on annotation values for security or correctness decisions (R-14.6-f). |
| class [AnnotationsConverter](./Stackific.Mcp.Protocol/AnnotationsConverter.md) | (De)serialiser for [`Annotations`](./Stackific.Mcp.Protocol/Annotations.md) that validates the [`Priority`](./Stackific.Mcp.Protocol/Annotations/Priority.md) 0..1 range (R-14.6-d) and round-trips unknown fields through [`Extensions`](./Stackific.Mcp.Protocol/Annotations/Extensions.md) (the TS `.passthrough()`). |
| enum [ApplicationType](./Stackific.Mcp.Protocol/ApplicationType.md) | The DCR `application_type` (spec §23.4, R-23.4-m – R-23.4-o). |
| record [AudioContent](./Stackific.Mcp.Protocol/AudioContent.md) | Base64-encoded audio content with a MIME type (spec §14.4.3). |
| static class [AuthorizationApplicability](./Stackific.Mcp.Protocol/AuthorizationApplicability.md) | Applicability predicates for the §23 authorization flow: which transports it governs, which are forbidden from using it, and how each conveys credentials (spec §23.1, R-23.1-a – R-23.1-c). |
| record [AuthorizationChallenge](./Stackific.Mcp.Protocol/AuthorizationChallenge.md) | The built `401`/`403` challenge a failed access-token validation yields (spec §23.8). |
| record [AuthorizationCodeTokenRequest](./Stackific.Mcp.Protocol/AuthorizationCodeTokenRequest.md) | The form-encoded token-request body for the authorization-code grant (spec §23.5 Step 4, R-23.5-n – R-23.5-p, R-23.6-b). |
| static class [AuthorizationConstants](./Stackific.Mcp.Protocol/AuthorizationConstants.md) | The HTTP status codes and header/scheme constants for the §23 authorization layer (spec §23.1). |
| record [AuthorizationErrorDetails](./Stackific.Mcp.Protocol/AuthorizationErrorDetails.md) | The displayable error details from an authorization error response (spec §23.7). |
| record [AuthorizationFlowRecord](./Stackific.Mcp.Protocol/AuthorizationFlowRecord.md) | Client-side bookkeeping captured in Step 1, associated with the `code_verifier` (and `state`, if used), to validate the redirect later (spec §23.5, R-23.5-c). |
| static class [AuthorizationRedirect](./Stackific.Mcp.Protocol/AuthorizationRedirect.md) | The §23.7 issuer-validation decision table, state verification, and the end-to-end redirect handler (spec §23.5 Step 3, §23.7). |
| record [AuthorizationRedirectResult](./Stackific.Mcp.Protocol/AuthorizationRedirectResult.md) | The outcome of [`Process`](./Stackific.Mcp.Protocol/AuthorizationRedirect/Process.md): whether the code may be redeemed. |
| static class [AuthorizationRequest](./Stackific.Mcp.Protocol/AuthorizationRequest.md) | The authorization-request and authorization-URL builders for Step 2 (spec §23.5). |
| record [AuthorizationRequestParams](./Stackific.Mcp.Protocol/AuthorizationRequestParams.md) | The authorization-request query parameters directing the user agent to the `authorization_endpoint` (spec §23.5, R-23.5-d – R-23.5-j). |
| record [AuthorizationResponseParams](./Stackific.Mcp.Protocol/AuthorizationResponseParams.md) | The redirect query parameters the authorization server returns (spec §23.5, §23.7). On success [`Code`](./Stackific.Mcp.Protocol/AuthorizationResponseParams/Code.md) is present; [`State`](./Stackific.Mcp.Protocol/AuthorizationResponseParams/State.md) echoes the request `state`; [`Iss`](./Stackific.Mcp.Protocol/AuthorizationResponseParams/Iss.md) identifies the authorization server (SHOULD). On error, the [`Error`](./Stackific.Mcp.Protocol/AuthorizationResponseParams/Error.md) fields are present and MUST NOT be acted on when `iss` validation fails (R-23.7-h). |
| struct [AuthorizationResult](./Stackific.Mcp.Protocol/AuthorizationResult.md) | The result of a validation that either succeeds or fails with a human-readable reason — the idiomatic C# rendering of the TypeScript SDK's `{ ok: true } | { ok: false; reason }` discriminated bag, used pervasively across the authorization layer (spec §23). |
| static class [AuthorizationScopes](./Stackific.Mcp.Protocol/AuthorizationScopes.md) | Authorization-request scope resolution (the scope-priority rule) and `offline_access` handling (spec §23.5 R-23.5-f, §23.9 R-23.9-b, R-23.9-g). |
| static class [AuthorizationSecurity](./Stackific.Mcp.Protocol/AuthorizationSecurity.md) | Consolidated §23.19 authorization-security predicates: audience binding, exact issuer validation, the per-request record invariant, token confidentiality/redaction, header-only token presentation, and refresh-token handling (spec §23.19, R-23.19-a – R-23.19-u). |
| record [AuthorizationServerMetadata](./Stackific.Mcp.Protocol/AuthorizationServerMetadata.md) | The metadata document an authorization server publishes (spec §23.3, RFC 8414 / OIDC Discovery). |
| record [AuthorizationServerRegistration](./Stackific.Mcp.Protocol/AuthorizationServerRegistration.md) | Registration state held for a single authorization server, keyed by its `issuer` (spec §23.1). |
| static class [Base64](./Stackific.Mcp.Protocol/Base64.md) | Base64 validation shared by content, resource-contents, and icon payloads (spec §14.5). |
| struct [CacheGetResult&lt;T&gt;](./Stackific.Mcp.Protocol/CacheGetResult-1.md) | The outcome of a [`ResponseCache`](./Stackific.Mcp.Protocol/ResponseCache-1.md) lookup (§13). |
| record [CacheHints](./Stackific.Mcp.Protocol/CacheHints.md) | Response-cache hints carried by a cacheable result (spec §13.1): a client-cache time-to-live in milliseconds and the sharing scope. Both are REQUIRED on a cacheable result. |
| enum [CacheScope](./Stackific.Mcp.Protocol/CacheScope.md) | The intended sharing scope of a cacheable result (spec §13). `public` means any client or intermediary MAY cache and serve the response to any user; `private` means only the requesting user's client MAY cache it. |
| static class [Caching](./Stackific.Mcp.Protocol/Caching.md) | The §13 response-caching behavioral layer — the C# counterpart of the TypeScript `protocol/caching.ts` utilities. The wire records ([`CacheScope`](./Stackific.Mcp.Protocol/CacheScope.md), [`CacheHints`](./Stackific.Mcp.Protocol/CacheHints.md)) carry the advisory hints; this static class adds the runtime rules the spec layers on top: hint validation ([`IsCacheHintValid`](./Stackific.Mcp.Protocol/Caching/IsCacheHintValid.md)), the privacy-default scope resolution ([`ResolveCacheScope`](./Stackific.Mcp.Protocol/Caching/ResolveCacheScope.md), R-13.1-e), the client-local freshness computation ([`IsFresh`](./Stackific.Mcp.Protocol/Caching/IsFresh.md), R-13.2), cross-page scope consistency (R-13.5-h), and the method→notification invalidation map (R-13.5-j). The cache itself is [`ResponseCache`](./Stackific.Mcp.Protocol/ResponseCache-1.md). |
| record [CallToolResult](./Stackific.Mcp.Protocol/CallToolResult.md) | The result of a completed `tools/call` (spec §16.5): unstructured [`Content`](./Stackific.Mcp.Protocol/CallToolResult/Content.md) blocks, optional [`StructuredContent`](./Stackific.Mcp.Protocol/CallToolResult/StructuredContent.md) (any JSON value), and an [`IsError`](./Stackific.Mcp.Protocol/CallToolResult/IsError.md) flag for tool-execution failures (§16.6 — distinct from a JSON-RPC protocol error). The base `resultType` is supplied by the runtime. |
| enum [CancelEffect](./Stackific.Mcp.Protocol/CancelEffect.md) | The effect a `tasks/cancel` has on a stored task's state (spec §25.9). |
| static class [Cancellation](./Stackific.Mcp.Protocol/Cancellation.md) | The §15.2 request-cancellation machinery — the C# counterpart of the cancellation utilities in the TypeScript `protocol/progress.ts` module. Cancellation is a best-effort, same-direction-only mechanism: a party MAY cancel only requests IT issued, never requests it received, and races are tolerated. The pieces here are: |
| class [CancellationHandler](./Stackific.Mcp.Protocol/CancellationHandler.md) | A receiver-side registry mapping in-flight request ids to abort callbacks — the C# counterpart of the TypeScript `CancellationHandler`. When a valid `notifications/cancelled` arrives, the receiver SHOULD stop processing the matching request, free resources, and suppress the response; this type wires that behavior (R-15.2.2-d / RC-4). |
| record [CancellationValidationResult](./Stackific.Mcp.Protocol/CancellationValidationResult.md) | The outcome of [`ValidateCancellationTarget`](./Stackific.Mcp.Protocol/Cancellation/ValidateCancellationTarget.md) (§15.2.1). Mirrors TS `CancellationValidationResult`. |
| record [CancelledNotificationParams](./Stackific.Mcp.Protocol/CancelledNotificationParams.md) | Parameters of the `notifications/cancelled` notification (spec §15.2.1), sent by a party to cancel an in-flight request that it issued earlier in the same direction. Cancellation is best-effort and races are tolerated: a receiver MAY ignore it when the request is unknown, already complete, or uncancellable, and the canceller SHOULD ignore any late response (§15.2.2/§15.2.3). |
| class [CancelledRequestSet](./Stackific.Mcp.Protocol/CancelledRequestSet.md) | A sender-side set of request ids for which a `notifications/cancelled` was sent but whose response has not yet arrived — the C# counterpart of the TypeScript `CancelledRequestSet`. A sender SHOULD distinctly ignore (not merely tolerate) late responses to cancelled requests so the race is detectable rather than silently processed (R-15.2.3-e / RC-6). |
| record [CancelTaskRequestParams](./Stackific.Mcp.Protocol/CancelTaskRequestParams.md) | The parameters of a `tasks/cancel` request (spec §25.9): requests cooperative cancellation of an in-progress task. |
| record [CancelTaskResult](./Stackific.Mcp.Protocol/CancelTaskResult.md) | The result of a `tasks/cancel` request (spec §25.9): an empty acknowledgement. The runtime supplies the base `resultType` as `"complete"` (see [`Complete`](./Stackific.Mcp.Protocol/ResultTypes/Complete.md)). Cancellation is cooperative and eventually consistent: acknowledgement does not guarantee the task will reach the Cancelled terminal status. |
| static class [CanonicalResourceIdentifier](./Stackific.Mcp.Protocol/CanonicalResourceIdentifier.md) | Canonical-resource-identifier construction, validation, and comparison helpers (spec §23.1, R-23.1-m – R-23.1-s): the absolute `https` (or loopback `http`) URI, with no fragment, in lowercase canonical form, that identifies an MCP server as a token audience. |
| static class [CapabilityNegotiation](./Stackific.Mcp.Protocol/CapabilityNegotiation.md) | The per-request, stateless capability-negotiation rules that gate every optional feature (spec §6.1–§6.4) — the C# counterpart of the TypeScript `protocol/capability-negotiation.ts` module. Because MCP is stateless, a feature is usable only when BOTH peers declare the governing capability/sub-flag, and capabilities are read from the current request only — never inferred from a prior request (R-6.4-c). |
| static class [Cimd](./Stackific.Mcp.Protocol/Cimd.md) | Client ID Metadata Document validation — the `https`-with-path `client_id` URL rule, the schema, the `client_id == URL` identity rule, and the redirect-URI membership check (spec §23.4, R-23.4-d – R-23.4-l). |
| record [CimdCacheControl](./Stackific.Mcp.Protocol/CimdCacheControl.md) | The HTTP caching directives an authorization server honours when caching a fetched CIMD document (spec §23.12, R-23.12-k). |
| class [CimdDocumentCache](./Stackific.Mcp.Protocol/CimdDocumentCache.md) | An authorization-server-side cache for fetched CIMD documents that respects HTTP cache headers and applies a host-domain trust policy (spec §23.12, R-23.12-k, R-23.12-l). |
| static class [CimdHosting](./Stackific.Mcp.Protocol/CimdHosting.md) | Client ID Metadata Document client/AS-side hosting predicates: the `https`-with-path `client_id` URL rule, the preference path, and `private_key_jwt` support (spec §23.12, R-23.12-a – R-23.12-f). |
| record [ClientCapabilities](./Stackific.Mcp.Protocol/ClientCapabilities.md) | The capabilities a client advertises, per request, in the `io.modelcontextprotocol/clientCapabilities` metadata key (spec §6.2). A capability is declared by the presence of its field — an empty object `{}` still means "supported"; absence (here, `null`) means "not supported". Unknown fields are ignored on receipt (§6.6). |
| static class [ClientIdAcquisition](./Stackific.Mcp.Protocol/ClientIdAcquisition.md) | `client_id` mechanism selection (the static priority order) and pre-registration credential checking (spec §23.4, R-23.4-a – R-23.4-c). |
| enum [ClientIdMechanism](./Stackific.Mcp.Protocol/ClientIdMechanism.md) | The ways a client obtains a `client_id`, plus the user-prompt fallback (spec §23.4, R-23.4-a). |
| record [ClientIdMetadataDocument](./Stackific.Mcp.Protocol/ClientIdMetadataDocument.md) | A Client ID Metadata Document (CIMD): a JSON document hosted at an HTTPS URL that is the client's `client_id` (spec §23.4, R-23.4-f, R-23.4-g). |
| struct [CodeRange](./Stackific.Mcp.Protocol/CodeRange.md) | A closed numeric range `[Min, Max]` inclusive, used to classify error codes (§22.2, §22.7). |
| record [CompleteRequestParams](./Stackific.Mcp.Protocol/CompleteRequestParams.md) | The parameters of a `completion/complete` request (spec §19.2). |
| record [CompleteResult](./Stackific.Mcp.Protocol/CompleteResult.md) | The result of a `completion/complete` request (spec §19.4). |
| static class [Completion](./Stackific.Mcp.Protocol/Completion.md) | The §19.4–§19.5 normative completion helpers ported from the TypeScript SDK's `completion.ts`: the 100-item cap with truncation signalling ([`ComputeCompletion`](./Stackific.Mcp.Protocol/Completion/ComputeCompletion.md), only emitting `total`/`hasMore` when matches are dropped), the prefix matcher, the context key-exclusion guard (R-19.2-k), and the unknown-ref / unknown-argument resolution that maps to `-32602` rather than a not-found result (R-19.5-r). |
| record [CompletionArgument](./Stackific.Mcp.Protocol/CompletionArgument.md) | The single argument being completed in a `completion/complete` request (spec §19.2). |
| record [CompletionContext](./Stackific.Mcp.Protocol/CompletionContext.md) | Additional disambiguating context for completion (spec §19.2). |
| abstract record [CompletionReference](./Stackific.Mcp.Protocol/CompletionReference.md) | The reference identifying what a `completion/complete` request is completing (spec §19.3): either a prompt (`ref/prompt`) or a resource template (`ref/resource`). The closed union is discriminated by the `type` field. |
| struct [CompletionTargetResolution](./Stackific.Mcp.Protocol/CompletionTargetResolution.md) | The discriminated outcome of [`ResolveCompletionTarget`](./Stackific.Mcp.Protocol/Completion/ResolveCompletionTarget.md) (spec §19.5, R-19.5-r). |
| record [CompletionValues](./Stackific.Mcp.Protocol/CompletionValues.md) | The ranked candidate values returned by a completion (spec §19.4). |
| static class [ConformanceRequirements](./Stackific.Mcp.Protocol/ConformanceRequirements.md) | S45 — Conformance Requirements &amp; References (spec §29–§30). |
| abstract record [ContentBlock](./Stackific.Mcp.Protocol/ContentBlock.md) | The discriminated union of content blocks carried by tool results and prompt messages (spec §14.4). The wire discriminator is the case-sensitive `type` field (R-14.4-a). |
| class [ContentBlockConverter](./Stackific.Mcp.Protocol/ContentBlockConverter.md) | The custom polymorphic (de)serialiser for [`ContentBlock`](./Stackific.Mcp.Protocol/ContentBlock.md). It dispatches on the case-sensitive `type` field, tolerates unknown types as [`UnsupportedContentBlock`](./Stackific.Mcp.Protocol/UnsupportedContentBlock.md) (R-14.4-b), and rejects the forbidden sampling types (R-14.8-a, R-14.8-b). |
| static class [ContentBlocks](./Stackific.Mcp.Protocol/ContentBlocks.md) | Ergonomic factory helpers for building [`ContentBlock`](./Stackific.Mcp.Protocol/ContentBlock.md) values (spec §14.4). |
| static class [ContentBlockTypes](./Stackific.Mcp.Protocol/ContentBlockTypes.md) | The set of `type` discriminators from the deprecated sampling capability that MUST NOT appear where a [`ContentBlock`](./Stackific.Mcp.Protocol/ContentBlock.md) is expected (R-14.8-a, R-14.8-b). |
| static class [ContinuationId](./Stackific.Mcp.Protocol/ContinuationId.md) | S06 — Stateless Per-Request Model &amp; Cross-Call Continuity (spec §4.4–§4.7). Exposes the opaque continuation-identifier validators; runtime enforcement of the self-describing `_meta` envelope lives in [`RequestMeta`](./Stackific.Mcp.Protocol/RequestMeta.md) (S05). |
| class [ContinuationTokenStore&lt;TState&gt;](./Stackific.Mcp.Protocol/ContinuationTokenStore-1.md) | A server-side store for `requestState` continuation tokens that protects their integrity and confidentiality and guards against replay — the §28.6 handling profile (spec §28.6, R-28.6-a, R-28.6-b, R-28.6-c; AC-44.18). |
| record [CreateMessageResult](./Stackific.Mcp.Protocol/CreateMessageResult.md) | The completion delivered back to the server, on retry, in response to a `sampling/createMessage` input request (spec §21.2.8) for the Deprecated Sampling capability (§21.2). The base `resultType` discriminator (§3) is supplied by the runtime. |
| record [CreateTaskResult](./Stackific.Mcp.Protocol/CreateTaskResult.md) | The augmented result a server returns in place of a request's ordinary result to signal that a task handle was created (spec §25.3). On the wire it is a `Result` whose `resultType` is the literal string `"task"` (supplied by the runtime; see [`Task`](./Stackific.Mcp.Protocol/ResultTypes/Task.md)) and which carries all [`McpTask`](./Stackific.Mcp.Protocol/McpTask.md) fields directly. A client that declared the Tasks capability MUST inspect `resultType` on each eligible response and handle the task case. |
| static class [CredentialBinding](./Stackific.Mcp.Protocol/CredentialBinding.md) | Issuer-keyed credential binding with the CIMD exemption and surface-error behaviour, by exact string comparison (spec §23.16, R-23.16-a – R-23.16-g). |
| enum [CredentialBindingAction](./Stackific.Mcp.Protocol/CredentialBindingAction.md) | The action a client takes for a discovered issuer, per the credential-binding rules (spec §23.16). |
| record [CredentialBindingDecision](./Stackific.Mcp.Protocol/CredentialBindingDecision.md) | The outcome of [`Decide`](./Stackific.Mcp.Protocol/CredentialBinding/Decide.md): the action and a human-readable reason (spec §23.16). |
| enum [CredentialConveyance](./Stackific.Mcp.Protocol/CredentialConveyance.md) | How a client conveys credentials for a given [`TransportFamily`](./Stackific.Mcp.Protocol/TransportFamily.md) (spec §23.1). |
| class [CredentialStore](./Stackific.Mcp.Protocol/CredentialStore.md) | A per-authorization-server credential store keyed by `issuer`, enforcing the §23.1 isolation rules (R-23.1-i – R-23.1-l): registration state is kept separate per `issuer`, a lookup never returns another server's credentials, and [`NeedsReregistration`](./Stackific.Mcp.Protocol/CredentialStore/NeedsReregistration.md) reports when the indicated authorization server changes so the client re-registers/re-discovers against the new one. |
| static class [Dcr](./Stackific.Mcp.Protocol/Dcr.md) | Dynamic Client Registration response handling and per-issuer credential persistence (spec §23.4, R-23.4-m – R-23.4-t). |
| static class [DcrRetry](./Stackific.Mcp.Protocol/DcrRetry.md) | Loopback-aware `application_type` classification and bounded DCR retry that flips the `application_type` on a retryable rejection (spec §23.15, R-23.15-a – R-23.15-f). |
| record [DcrRetryResult](./Stackific.Mcp.Protocol/DcrRetryResult.md) | The outcome of [`RegisterWithRetryAsync`](./Stackific.Mcp.Protocol/DcrRetry/RegisterWithRetryAsync.md): the final result and the attempts made (spec §23.15). |
| enum [DegradationDecision](./Stackific.Mcp.Protocol/DegradationDecision.md) | What a peer should do when the other peer lacks an optional behavior (§6.4). Mirrors TS `DegradationDecision`. |
| record [DetailedTask](./Stackific.Mcp.Protocol/DetailedTask.md) | A [`McpTask`](./Stackific.Mcp.Protocol/McpTask.md) that additionally conveys the terminal payload (or pending input requests) inline (spec §25.4). It is the shape returned by `tasks/get` and carried on `notifications/tasks`, modelled here as a single record whose status-specific members are populated according to `status`: [`InputRequests`](./Stackific.Mcp.Protocol/DetailedTask/InputRequests.md) for `input_required`, [`Result`](./Stackific.Mcp.Protocol/DetailedTask/Result.md) for `completed`, and [`Error`](./Stackific.Mcp.Protocol/DetailedTask/Error.md) for `failed`. A non-terminal task carries neither [`Result`](./Stackific.Mcp.Protocol/DetailedTask/Result.md) nor [`Error`](./Stackific.Mcp.Protocol/DetailedTask/Error.md) (§25.5). |
| record [DiscoverResult](./Stackific.Mcp.Protocol/DiscoverResult.md) | The result of the `server/discover` request (spec §5.3.2): the protocol revisions the server supports, its advertised capabilities, its identity, optional natural-language instructions, and optional result-level metadata. It is a `Result`, so on the wire it also carries the base `resultType` discriminator (normally `complete`); that is added by the runtime. |
| static class [DiscoveryRobustness](./Stackific.Mcp.Protocol/DiscoveryRobustness.md) | Discovery-robustness wrappers over S35's well-known ordering, plus the `authorization_servers` requirement and per-AS issuer-identity validation (spec §23.17, R-23.17-a – R-23.17-i). |
| static class [DisplayName](./Stackific.Mcp.Protocol/DisplayName.md) | Display-name resolution for §14.1 identity types (`BaseMetadata`). |
| record [DynamicClientRegistrationCredential](./Stackific.Mcp.Protocol/DynamicClientRegistrationCredential.md) | Persisted DCR credentials, bound to the issuing authorization server's `issuer` (spec §23.4, R-23.4-s). |
| record [DynamicClientRegistrationResponse](./Stackific.Mcp.Protocol/DynamicClientRegistrationResponse.md) | A Dynamic Client Registration response body (Deprecated) (spec §23.4). [`ClientId`](./Stackific.Mcp.Protocol/DynamicClientRegistrationResponse/ClientId.md) is REQUIRED; [`ClientSecret`](./Stackific.Mcp.Protocol/DynamicClientRegistrationResponse/ClientSecret.md) is issued only for confidential clients. |
| record [DynamicClientRegistrationResult](./Stackific.Mcp.Protocol/DynamicClientRegistrationResult.md) | The outcome of a DCR registration attempt, modelling the failure cases a client MUST be prepared to handle (spec §23.4, R-23.4-p, R-23.4-q, R-23.4-r). On failure the client surfaces [`Reason`](./Stackific.Mcp.Protocol/DynamicClientRegistrationResult/Reason.md) rather than crashing; [`Retryable`](./Stackific.Mcp.Protocol/DynamicClientRegistrationResult/Retryable.md) flags redirect-URI/application-type rejections the client MAY retry. |
| class [DynamicClientRegistrationStore](./Stackific.Mcp.Protocol/DynamicClientRegistrationStore.md) | A store for persisted DCR credentials, each keyed by the issuing authorization server's `issuer`, that re-registers when the authorization server changes (spec §23.4, R-23.4-s, R-23.4-t). |
| static class [Elicitation](./Stackific.Mcp.Protocol/Elicitation.md) | S30 — Elicitation I: the front-half capability/delivery/mode logic (spec §20.1–§20.3). The C# counterpart of the TypeScript `protocol/elicitation.ts` module: mode constants and resolution, server-side capability gating (R-20.1-d/e), URL validity, and the request builders. |
| enum [ElicitationAction](./Stackific.Mcp.Protocol/ElicitationAction.md) | The user's response action to an elicitation request (spec §20.5). Exactly one of these literals is returned in [`Action`](./Stackific.Mcp.Protocol/ElicitResult/Action.md); the distinctions apply to both form and URL modes. |
| record [ElicitationCapability](./Stackific.Mcp.Protocol/ElicitationCapability.md) | Client elicitation capability with its mode sub-flags (spec §6.2/§20). |
| record [ElicitationCompleteNotificationParams](./Stackific.Mcp.Protocol/ElicitationCompleteNotificationParams.md) | The parameters of the `notifications/elicitation/complete` notification (spec §20.6), which a server MAY send to signal that a URL-mode out-of-band interaction has completed. A client MUST ignore a notification referencing an unknown or already-completed [`ElicitationId`](./Stackific.Mcp.Protocol/ElicitationCompleteNotificationParams/ElicitationId.md). |
| static class [ElicitationForm](./Stackific.Mcp.Protocol/ElicitationForm.md) | S31 — Elicitation II: the restricted form-schema system, result-action semantics, the §20.6 completion notification handler, and the §20.7 consent/security helpers (spec §20.4–§20.8). The C# counterpart of the TypeScript `protocol/elicitation-form.ts` module. |
| record [ElicitRequestFormParams](./Stackific.Mcp.Protocol/ElicitRequestFormParams.md) | Form-mode elicitation parameters (spec §20.3, `mode: "form"`): in-band structured data collection against a restricted JSON Schema. The collected data IS exposed to the client, so a server MUST NOT use form mode for sensitive information such as passwords, API keys, tokens, or payment credentials (§20.7). |
| abstract record [ElicitRequestParams](./Stackific.Mcp.Protocol/ElicitRequestParams.md) | The parameters of an `elicitation/create` input request (spec §20.2). Elicitation lets a server request structured input from the user through the client; it is delivered as an input-required result and answered by retrying the originating request (§11). This is a closed union of two mode-specific shapes (spec §20.3) discriminated by the `mode` field: [`ElicitRequestFormParams`](./Stackific.Mcp.Protocol/ElicitRequestFormParams.md) (`"form"`) and [`ElicitRequestURLParams`](./Stackific.Mcp.Protocol/ElicitRequestURLParams.md) (`"url"`). |
| record [ElicitRequestURLParams](./Stackific.Mcp.Protocol/ElicitRequestURLParams.md) | URL-mode elicitation parameters (spec §20.3, `mode: "url"`): out-of-band interaction via navigation to a URL. Data other than the URL itself is NOT exposed to the client, making this the mode a server MUST use for sensitive flows such as authorization or payment (§20.7). |
| record [ElicitResult](./Stackific.Mcp.Protocol/ElicitResult.md) | The client's response to an elicitation request, supplied as the input on retry (spec §20.5). |
| record [EmbeddedResource](./Stackific.Mcp.Protocol/EmbeddedResource.md) | Resource contents embedded directly into a result or message (spec §14.4.5). |
| enum [ErrorCodeClass](./Stackific.Mcp.Protocol/ErrorCodeClass.md) | The three (plus extension) classes a JSON-RPC error code can fall into, per §22. The numeric code is authoritative; this taxonomy lets a receiver reason about a code it has never seen (R-22.1-h, R-22.7-a, R-22.7-e). |
| record [ErrorCodeRegistryEntry](./Stackific.Mcp.Protocol/ErrorCodeRegistryEntry.md) | One row of the §22 error-code registry (§6.5). |
| enum [ErrorDataPolicy](./Stackific.Mcp.Protocol/ErrorDataPolicy.md) | Whether a code's `data` shape is normative (fixed by the spec) or sender-defined (the sender MAY attach any structure). (R-22.1-k, R-22.3-a) |
| static class [ErrorRegistry](./Stackific.Mcp.Protocol/ErrorRegistry.md) | The single, authoritative model for §22 error handling: the full registry of error codes, the classification taxonomy and code ranges, the reserved-code set and extension-code validator, the HTTP-status overlay, the inbound-failure-stage mapping, the canonical resource-not-found mapping, and the firm boundary between a protocol-level JSON-RPC error and a feature-level error result (a tool that ran and failed). |
| record [ExcludedRoot](./Stackific.Mcp.Protocol/ExcludedRoot.md) | An excluded candidate root and the reason it was dropped during assembly (§21.1.5). |
| enum [ExtensionCodeRejection](./Stackific.Mcp.Protocol/ExtensionCodeRejection.md) | The reason an extension error code was rejected by [`ValidateExtensionErrorCode`](./Stackific.Mcp.Protocol/ErrorRegistry/ValidateExtensionErrorCode.md). |
| struct [ExtensionCodeValidation](./Stackific.Mcp.Protocol/ExtensionCodeValidation.md) | The outcome of validating a candidate extension error code (R-22.7-a..c). |
| class [ExtensionMethodRouter](./Stackific.Mcp.Protocol/ExtensionMethodRouter.md) | Routes extension-defined methods to their handlers, enforcing the two framework rules that govern dispatch (spec §24.5): method strings are namespaced under the registering extension (R-24.5-b), and a handler is invoked ONLY when its extension is in the active set for the interaction (R-24.5-c). |
| static class [Extensions](./Stackific.Mcp.Protocol/Extensions.md) | S11 — The Extensions Map &amp; Forward Compatibility (spec §6.5–§6.7) combined with S38 — The Extension Mechanism (spec §24). |
| enum [FeatureStatus](./Stackific.Mcp.Protocol/FeatureStatus.md) | Feature lifecycle status (spec §27, R-1.3-b, R-2.2-f – R-2.2-h). A feature whose status is Deprecated remains defined and MUST still be accepted by receivers, but SHOULD NOT be relied upon by new implementations. |
| record [FetchIconOptions](./Stackific.Mcp.Protocol/FetchIconOptions.md) | Options for [`FetchIconAsync`](./Stackific.Mcp.Protocol/IconSecurity/FetchIconAsync.md). |
| record [FetchIconResult](./Stackific.Mcp.Protocol/FetchIconResult.md) | The result of [`FetchIconAsync`](./Stackific.Mcp.Protocol/IconSecurity/FetchIconAsync.md): the validated bytes, detected type, and final URL. |
| record [GetPromptResponseDiscrimination](./Stackific.Mcp.Protocol/GetPromptResponseDiscrimination.md) | The result of [`DiscriminateGetPromptResponse`](./Stackific.Mcp.Protocol/Prompts/DiscriminateGetPromptResponse.md): which branch a `prompts/get` reply fell into, plus the parsed payload for the matched branch (spec §18.4). |
| enum [GetPromptResponseKind](./Stackific.Mcp.Protocol/GetPromptResponseKind.md) | The discriminated outcome of inspecting a `prompts/get` reply (spec §18.4): a completed [`GetPromptResult`](./Stackific.Mcp.Protocol/GetPromptResult.md), an `input_required` continuation, or a malformed/unrecognized body. |
| record [GetPromptResult](./Stackific.Mcp.Protocol/GetPromptResult.md) | The result of a completed `prompts/get` (spec §18.4). |
| record [GetTaskRequestParams](./Stackific.Mcp.Protocol/GetTaskRequestParams.md) | The parameters of a `tasks/get` request (spec §25.7): the polling primitive that retrieves a task's current state. |
| record [GetTaskResult](./Stackific.Mcp.Protocol/GetTaskResult.md) | The result of a `tasks/get` request (spec §25.7): a [`DetailedTask`](./Stackific.Mcp.Protocol/DetailedTask.md) whose status-specific payload fields are inlined for the task's current status. The runtime supplies the base `resultType` as `"complete"` (see [`Complete`](./Stackific.Mcp.Protocol/ResultTypes/Complete.md)). |
| interface [ICompletionCatalog](./Stackific.Mcp.Protocol/ICompletionCatalog.md) | The set of valid argument names a completion `ref` may complete (spec §19.5, R-19.5-r). |
| record [Icon](./Stackific.Mcp.Protocol/Icon.md) | A single icon descriptor (spec §14.2): a source URI with optional MIME type, sizes, and theme. |
| static class [IconSecurity](./Stackific.Mcp.Protocol/IconSecurity.md) | Security validation and credential-free fetching for [`Icon`](./Stackific.Mcp.Protocol/Icon.md) sources (spec §14.2). |
| enum [IconTheme](./Stackific.Mcp.Protocol/IconTheme.md) | The background theme an [`Icon`](./Stackific.Mcp.Protocol/Icon.md) is designed for (spec §14.2). |
| class [IconValidationError](./Stackific.Mcp.Protocol/IconValidationError.md) | An error raised when an icon URI or its content is rejected for security reasons (§14.2). |
| record [ImageContent](./Stackific.Mcp.Protocol/ImageContent.md) | Base64-encoded image content with a MIME type (spec §14.4.2). |
| record [Implementation](./Stackific.Mcp.Protocol/Implementation.md) | Identifies a client or server software implementation (spec §14.3). Carried as `clientInfo` in every request's `_meta` (§4.3) and as `serverInfo` in the discovery result (§5.3.2). [`Name`](./Stackific.Mcp.Protocol/Implementation/Name.md) and [`Version`](./Stackific.Mcp.Protocol/Implementation/Version.md) are REQUIRED. |
| enum [InboundFailureStage](./Stackific.Mcp.Protocol/InboundFailureStage.md) | The stage at which an inbound message failed validation, used to select the authoritative `error.code` per the §22.6 classification pipeline. |
| class [IncompatibleProtocolError](./Stackific.Mcp.Protocol/IncompatibleProtocolError.md) | An actionable error a client surfaces to its caller when no protocol revision is mutually supported (§5.4-d, §5.5-j). It carries both sides' revision sets for diagnostics and, unlike an [`McpError`](./Stackific.Mcp.JsonRpc/McpError.md), is NOT a wire error — it never goes on the wire. Mirrors TS `IncompatibleProtocolError`. |
| record [InputRequest](./Stackific.Mcp.Protocol/InputRequest.md) | A single input request a server embeds in an [`InputRequiredResult`](./Stackific.Mcp.Protocol/InputRequiredResult.md) for the client to fulfill (spec §11.2). The kind is discriminated by [`Method`](./Stackific.Mcp.Protocol/InputRequest/Method.md): `elicitation/create` (§20), `sampling/createMessage` (§21), or `roots/list` (§21). The runtime dispatches on the method name, so [`Params`](./Stackific.Mcp.Protocol/InputRequest/Params.md) is kept as raw JSON. |
| record [InputRequiredParse](./Stackific.Mcp.Protocol/InputRequiredParse.md) | The outcome of [`ParseInputRequiredResult`](./Stackific.Mcp.Protocol/MultiRoundTrip/ParseInputRequiredResult.md) (spec §11.2). |
| record [InputRequiredResult](./Stackific.Mcp.Protocol/InputRequiredResult.md) | The `input_required` result a server returns to solicit client input while processing a request (spec §11.2). At least one of [`InputRequests`](./Stackific.Mcp.Protocol/InputRequiredResult/InputRequests.md) or [`RequestState`](./Stackific.Mcp.Protocol/InputRequiredResult/RequestState.md) MUST be present; a result with only [`RequestState`](./Stackific.Mcp.Protocol/InputRequiredResult/RequestState.md) is the load-shedding (retry-later) signal (§11.5). The base `resultType` is supplied by the runtime as `input_required`. |
| record [InputResponseKindError](./Stackific.Mcp.Protocol/InputResponseKindError.md) | One kind-correlation failure reported by [`ValidateInputResponseKinds`](./Stackific.Mcp.Protocol/MultiRoundTrip/ValidateInputResponseKinds.md) (spec §11.4). |
| record [InputResponseKindValidation](./Stackific.Mcp.Protocol/InputResponseKindValidation.md) | The outcome of [`ValidateInputResponseKinds`](./Stackific.Mcp.Protocol/MultiRoundTrip/ValidateInputResponseKinds.md) (spec §11.4). |
| record [IssuerBoundCredentials](./Stackific.Mcp.Protocol/IssuerBoundCredentials.md) | Persisted client credentials bound to the issuing authorization server (spec §23.16, R-23.16-a). |
| class [IssuerBoundCredentialStore](./Stackific.Mcp.Protocol/IssuerBoundCredentialStore.md) | An issuer-keyed store for persisted, issuer-bound client credentials, keeping separate registration state per authorization server (spec §23.16, R-23.16-a, R-23.16-b; §23.17, R-23.17-d). |
| enum [IssuerValidationDecision](./Stackific.Mcp.Protocol/IssuerValidationDecision.md) | The decision the §23.7 issuer-validation table yields for the `iss` parameter (spec §23.7, R-23.7-d). |
| interface [ITokenRequest](./Stackific.Mcp.Protocol/ITokenRequest.md) | A token request of either grant; renders its `application/x-www-form-urlencoded` body (spec §23.5/§23.9). |
| record [ListPromptsResult](./Stackific.Mcp.Protocol/ListPromptsResult.md) | The paginated, cacheable result of `prompts/list` (spec §18.2). |
| record [ListResourcesResult](./Stackific.Mcp.Protocol/ListResourcesResult.md) | The paginated, cacheable result of `resources/list` (spec §17.2). |
| record [ListResourceTemplatesResult](./Stackific.Mcp.Protocol/ListResourceTemplatesResult.md) | The paginated, cacheable result of `resources/templates/list` (spec §17.3). |
| record [ListToolsResult](./Stackific.Mcp.Protocol/ListToolsResult.md) | The paginated, cacheable result of `tools/list` (spec §16.2). |
| static class [LoggingFilter](./Stackific.Mcp.Protocol/LoggingFilter.md) | The §15.3 logging-filter behavioral layer — the C# counterpart of the TypeScript `protocol/logging.ts` helpers — for the Deprecated logging-message mechanism. The [`LoggingLevel`](./Stackific.Mcp.Protocol/LoggingLevel.md) enum (and its [`LoggingLevelExtensions`](./Stackific.Mcp.Protocol/LoggingLevelExtensions.md) ordering) carries the severities; this class adds the per-request opt-in validation (`-32602` on a bad value, R-15.3.3-g), the min-emit-level resolution (absent/invalid ⇒ emit nothing, R-15.3.3-a), and the global [`LogRateLimiter`](./Stackific.Mcp.Protocol/LogRateLimiter.md). |
| enum [LoggingLevel](./Stackific.Mcp.Protocol/LoggingLevel.md) | A structured log severity (spec §15.3.1), mapping to the standard syslog message severities. The members are declared from least to most severe; that ordering is significant because a request opts in at a minimum level and the emitter MUST emit only messages at or above it. |
| static class [LoggingLevelExtensions](./Stackific.Mcp.Protocol/LoggingLevelExtensions.md) | Severity-ordering helpers for the (existing) [`LoggingLevel`](./Stackific.Mcp.Protocol/LoggingLevel.md) enum (spec §4.3, R-4.3-m). The C# counterpart of the TypeScript `loggingLevelIndex` / `isAtOrAboveLogLevel` functions and the `LOGGING_LEVELS` ascending-severity ordering. |
| record [LogLevelValidationResult](./Stackific.Mcp.Protocol/LogLevelValidationResult.md) | The outcome of [`ValidateLogLevelOptIn`](./Stackific.Mcp.Protocol/LoggingFilter/ValidateLogLevelOptIn.md) (§15.3.3-g). Mirrors TS `LogLevelValidationResult`. |
| class [LogRateLimiter](./Stackific.Mcp.Protocol/LogRateLimiter.md) | A global rate limiter for `notifications/message` log emissions (RC-3 / SHOULD) — the C# counterpart of the TypeScript `LogRateLimiter`. Unlike [`ProgressRateLimiter`](./Stackific.Mcp.Protocol/ProgressRateLimiter.md), a single shared throttle window applies to the entire log stream (all messages share the one `notifications/message` channel). The current time is injected, so behavior is deterministic. |
| static class [McpMethods](./Stackific.Mcp.Protocol/McpMethods.md) | The complete registry of JSON-RPC method and notification names defined by the protocol and its in-scope extensions (spec Appendix A). Names are case-sensitive and reproduced verbatim. |
| enum [McpRole](./Stackific.Mcp.Protocol/McpRole.md) | The two wire-level JSON-RPC endpoint roles (§1.1, §2.2): the two ends of a one-to-one client↔server pairing. |
| record [McpTask](./Stackific.Mcp.Protocol/McpTask.md) | The handle and status record for a long-running operation (spec §25.4). A [`McpTask`](./Stackific.Mcp.Protocol/McpTask.md) is returned immediately in place of a blocking result; the client polls `tasks/get` to observe progress until the task reaches a terminal status. Identifiers are server-minted and opaque, and the durable record survives client disconnects and restarts (§25.6). Named `McpTask` to avoid clashing with Task; the wire object is unaffected. |
| enum [McpTaskStatus](./Stackific.Mcp.Protocol/McpTaskStatus.md) | The lifecycle state of a task (spec §25.5). A task is created non-terminal (typically Working) and moves among Working and InputRequired until it reaches one of the terminal states Completed, Failed, or Cancelled, after which its status and inline payload are immutable. |
| record [MessageValidationResult](./Stackific.Mcp.Protocol/MessageValidationResult.md) | The outcome of a §21.2.7 message-content validation. Mirrors the TS `{ ok, reason?, index? }` shape. |
| record [ModelHint](./Stackific.Mcp.Protocol/ModelHint.md) | A hint guiding model selection within [`ModelPreferences`](./Stackific.Mcp.Protocol/ModelPreferences.md) (spec §21.2.9). All hints are advisory; the client MAY ignore them. If multiple hints are specified, the client MUST evaluate them in order, taking the first match. |
| record [ModelHintMatch](./Stackific.Mcp.Protocol/ModelHintMatch.md) | The first [`ModelHint`](./Stackific.Mcp.Protocol/ModelHint.md) matched against a candidate model name (§21.2.9). |
| record [ModelPreferences](./Stackific.Mcp.Protocol/ModelPreferences.md) | A server's advisory preferences for which model the client should select for a sampling request (spec §21.2.9). All preferences are advisory: the client (or host) makes the final selection and MAY ignore them. |
| static class [MultiRoundTrip](./Stackific.Mcp.Protocol/MultiRoundTrip.md) | Pure, side-effect-free helpers for the multi-round-trip mechanism (spec §11): the recognized input-request kinds and their required client capabilities (§11.2), duplicate-`inputRequests`-key detection and strict parsing (§11.2, R-11.2-f), the opaque `requestState` continuation-token codec (§11.3 — attacker-controlled, validated on decode), input-response key/kind correlation (§11.4), the client-facing result-type discrimination (§11.5), and the load-shedding / loop-guard / backoff / re-request helpers (§11.5). The C# counterpart of the exported functions in the TypeScript `protocol/multi-round-trip.ts` (S17). |
| struct [NonFileDispositionOutcome](./Stackific.Mcp.Protocol/NonFileDispositionOutcome.md) | The outcome of [`ApplyNonFileDisposition`](./Stackific.Mcp.Protocol/RootsValidation/ApplyNonFileDisposition.md): whether the root is kept under the chosen disposition. |
| enum [NonFileRootDisposition](./Stackific.Mcp.Protocol/NonFileRootDisposition.md) | A receiver's permitted disposition of a root whose `uri` is not `file`-scheme (R-21.1.5-c). |
| enum [NotificationStreamPlacement](./Stackific.Mcp.Protocol/NotificationStreamPlacement.md) | Which stream a notification kind belongs on (spec §10.6). |
| static class [OAuthValues](./Stackific.Mcp.Protocol/OAuthValues.md) | The fixed OAuth token values and PKCE constants for the authorization-code-with-PKCE flow (spec §23.4–§23.10). |
| class [OffsetPaginator&lt;T&gt;](./Stackific.Mcp.Protocol/OffsetPaginator-1.md) | A reference cursor-based paginator over an in-memory list — the C# counterpart of the TypeScript `OffsetPaginator`. Cursors are deterministic DECIMAL offset strings (matching TS, not base64): the same position always yields the same cursor token (RC-2: stability). An unrecognized or malformed cursor is returned as a structured error rather than thrown (RC-3/RC-4), so the server remains operational after a bad cursor. |
| static class [PaginationUtilities](./Stackific.Mcp.Protocol/PaginationUtilities.md) | The §12 cursor-based pagination behavioral layer — the C# counterpart of the TypeScript `protocol/pagination.ts` module. A `cursor` is an OPAQUE string: the empty string `""` is a PRESENT cursor (not absence), and only the absence of a cursor (`null`) means "first page" / "last page". This class adds the cursor predicates, the per-cursor cache-key helper (R-12.5-a), the invalid-cursor error builder (`-32602`), and the reference [`OffsetPaginator`](./Stackific.Mcp.Protocol/OffsetPaginator-1.md) that NEVER throws on a bad cursor. |
| record [PaginatorPageResult&lt;T&gt;](./Stackific.Mcp.Protocol/PaginatorPageResult-1.md) | The result of [`GetPage`](./Stackific.Mcp.Protocol/OffsetPaginator-1/GetPage.md): either a successful page (with items and an optional next cursor) or a structured invalid-cursor error. The paginator NEVER throws on an unrecognized cursor (RC-3/RC-4). Mirrors TS `PaginatorPageResult`. |
| static class [Pkce](./Stackific.Mcp.Protocol/Pkce.md) | PKCE `S256` generation, derivation, and verification (spec §23.5, R-23.5-a, R-23.5-b), backed by RandomNumberGenerator and SHA256. Randomness is injectable so callers can produce a deterministic verifier in tests. |
| record [PkceChallenge](./Stackific.Mcp.Protocol/PkceChallenge.md) | A generated PKCE pair: the secret verifier and its derived public challenge (spec §23.5). |
| static class [PkceSupport](./Stackific.Mcp.Protocol/PkceSupport.md) | The §28.5 PKCE-support gate: a client MUST confirm, from authorization-server metadata, that the AS advertises PKCE `S256` before proceeding, refusing otherwise (spec §28.5, R-28.5-k). |
| class [PkceSupportException](./Stackific.Mcp.Protocol/PkceSupportException.md) | Thrown when a client refuses to proceed because PKCE `S256` support cannot be confirmed from authorization-server metadata (spec §28.5, R-28.5-k). |
| record [PresentedToken](./Stackific.Mcp.Protocol/PresentedToken.md) | The validated facts about a presented token, supplied by signature/introspection (spec §23.8). |
| abstract record [ProbeOutcome](./Stackific.Mcp.Protocol/ProbeOutcome.md) | The outcome of interpreting a probe (`server/discover`) response (§5.7). Mirrors TS `ProbeOutcome`. Modeled as a closed record hierarchy — one derived type per [`ProbeOutcomeKind`](./Stackific.Mcp.Protocol/ProbeOutcomeKind.md) carrying ONLY the data valid for that case — so illegal combinations (for example a "not-this-protocol" outcome that nonetheless carries a parsed result) cannot be constructed. Match on the concrete type with a `switch` expression, or branch on [`Kind`](./Stackific.Mcp.Protocol/ProbeOutcome/Kind.md). |
| enum [ProbeOutcomeKind](./Stackific.Mcp.Protocol/ProbeOutcomeKind.md) | The three classes a probe (`server/discover`) response falls into (§5.7). |
| record [ProgressNotificationParams](./Stackific.Mcp.Protocol/ProgressNotificationParams.md) | Parameters of the `notifications/progress` notification (spec §15.1.3), sent by the party that is processing a request to report incremental progress on it. The notification is request-scoped: it MUST reference a token the peer supplied in an active request's `_meta.progressToken`, and MUST be sent before that request's final response (§15.1.4). |
| class [ProgressRateLimiter](./Stackific.Mcp.Protocol/ProgressRateLimiter.md) | A per-token rate limiter for `notifications/progress` emissions (RC-3 / SHOULD) — the C# counterpart of the TypeScript `ProgressRateLimiter`. Each token has an independent last-emit time so a slow token is not penalized by a fast one; string and number tokens are tracked independently. The current time is injected (no wall-clock), so behavior is deterministic. |
| struct [ProgressToken](./Stackific.Mcp.Protocol/ProgressToken.md) | A progress-correlation token (spec §15.1.1): a value that is either a JSON string or a JSON number, never `null`. The token is carried in an originating request's `_meta.progressToken` (§15.1.2) and is echoed on every [`ProgressNotificationParams`](./Stackific.Mcp.Protocol/ProgressNotificationParams.md) so that out-of-band progress can be matched to the request that opted in. |
| class [ProgressTracker](./Stackific.Mcp.Protocol/ProgressTracker.md) | Tracks active progress tokens for a single sender, enforcing the §15.1 uniqueness and monotonicity rules — the C# counterpart of the TypeScript `ProgressTracker`. A token's identity preserves the JSON type, so string `"1"` and number `1` are distinct (R-15.1.1-c). |
| record [Prompt](./Stackific.Mcp.Protocol/Prompt.md) | A prompt or prompt template offered by the server (spec §18.3): a named, optionally argument-accepting template that renders into conversation messages via `prompts/get`. |
| record [PromptArgument](./Stackific.Mcp.Protocol/PromptArgument.md) | A single argument a prompt accepts (spec §18.3). |
| record [PromptMessage](./Stackific.Mcp.Protocol/PromptMessage.md) | One message within a resolved prompt (spec §18.5): a role paired with a single content block. |
| record [PromptReference](./Stackific.Mcp.Protocol/PromptReference.md) | A completion reference identifying a prompt by name (spec §19.3, `type: "ref/prompt"`). |
| static class [Prompts](./Stackific.Mcp.Protocol/Prompts.md) | The §18.4 normative prompt-response helpers ported from the TypeScript SDK's `prompts.ts`: the `resultType` discrimination that tolerates an ABSENT discriminator (absent ⇒ complete) and recognises the `input_required` continuation, plus the required-argument extractor. |
| record [PromptsCapability](./Stackific.Mcp.Protocol/PromptsCapability.md) | Server prompts capability (spec §6.3/§18). |
| record [ProtectedResourceMetadata](./Stackific.Mcp.Protocol/ProtectedResourceMetadata.md) | The OAuth 2.0 Protected Resource Metadata document the MCP server publishes (spec §23.2, RFC 9728). |
| static class [ProtocolRevision](./Stackific.Mcp.Protocol/ProtocolRevision.md) | The MCP protocol revision implemented by this SDK (spec §5.1). Revision identifiers are opaque, exactly-matched strings — never compared lexically or chronologically (§5.1). |
| class [ProtocolSupportCache](./Stackific.Mcp.Protocol/ProtocolSupportCache.md) | Caches the protocol-support determination per server endpoint (§5.7, R-5.7-e). The determination is a property of the endpoint, not of an individual request, so a client SHOULD cache it for the lifetime of the connected server; it MAY persist it across restarts of the same server configuration (via [`Entries`](./Stackific.Mcp.Protocol/ProtocolSupportCache/Entries.md) / [`FromEntries`](./Stackific.Mcp.Protocol/ProtocolSupportCache/FromEntries.md)) and re-probe — via [`Invalidate`](./Stackific.Mcp.Protocol/ProtocolSupportCache/Invalidate.md) — if a cached assumption later proves wrong (R-5.7-f). Mirrors TS `ProtocolSupportCache`. |
| record [ProtocolSupportDetermination](./Stackific.Mcp.Protocol/ProtocolSupportDetermination.md) | A per-endpoint conclusion about whether a server speaks this protocol family (§5.7). Mirrors TS `ProtocolSupportDetermination`. |
| record [ReadResourceResponseDiscrimination](./Stackific.Mcp.Protocol/ReadResourceResponseDiscrimination.md) | The result of [`DiscriminateReadResourceResponse`](./Stackific.Mcp.Protocol/Resources/DiscriminateReadResourceResponse.md): which branch a `resources/read` reply fell into, plus the parsed payload for the matched branch (spec §17.5). |
| enum [ReadResourceResponseKind](./Stackific.Mcp.Protocol/ReadResourceResponseKind.md) | The discriminated outcome of inspecting a `resources/read` reply (spec §17.5): a completed [`ReadResourceResult`](./Stackific.Mcp.Protocol/ReadResourceResult.md), an `input_required` continuation, or a malformed body. |
| record [ReadResourceResult](./Stackific.Mcp.Protocol/ReadResourceResult.md) | The cacheable result of `resources/read` (spec §17.5). |
| record [RefreshTokenRequest](./Stackific.Mcp.Protocol/RefreshTokenRequest.md) | The form-encoded token-request body for the refresh-token grant (spec §23.9, R-23.9-e, R-23.9-f). |
| static class [RegistrationMechanism](./Stackific.Mcp.Protocol/RegistrationMechanism.md) | Metadata-driven `client_id` mechanism selection: choosing the first applicable mechanism after inspecting the validated authorization-server metadata, gating CIMD on `client_id_metadata_document_supported` and DCR on `registration_endpoint` (spec §23.11, R-23.11-a – R-23.11-e). |
| record [RegistrationMechanismSelection](./Stackific.Mcp.Protocol/RegistrationMechanismSelection.md) | The result of [`Select`](./Stackific.Mcp.Protocol/RegistrationMechanism/Select.md): the chosen mechanism and why it applied (spec §23.11). |
| static class [Registries](./Stackific.Mcp.Protocol/Registries.md) | S46 — Consolidated Registries: Methods, Errors, `_meta` Keys, Capabilities, and Types (spec Appendices A–E). |
| record [RequestMeta](./Stackific.Mcp.Protocol/RequestMeta.md) | The protocol-defined per-request `_meta` envelope (spec §4.3) that makes every client request self-describing: the protocol revision, the client identity, and the client's per-request capabilities, plus optional passthrough keys (progress token, trace context, third-party metadata). This is what enables the stateless model of §4.4. |
| struct [ResolvedToolAnnotationHints](./Stackific.Mcp.Protocol/ResolvedToolAnnotationHints.md) | The four boolean [`ToolAnnotations`](./Stackific.Mcp.Protocol/ToolAnnotations.md) hints resolved to concrete values (spec §16.7). |
| record [Resource](./Stackific.Mcp.Protocol/Resource.md) | A concrete, directly readable resource identified by a URI (spec §17.4). Composes the `BaseMetadata` name/title pair and the optional icons array. |
| record [ResourceContents](./Stackific.Mcp.Protocol/ResourceContents.md) | The concrete contents of a resource (spec §14.5). A value is the text variant if and only if it carries [`Text`](./Stackific.Mcp.Protocol/ResourceContents/Text.md), and the blob variant if and only if it carries [`Blob`](./Stackific.Mcp.Protocol/ResourceContents/Blob.md); a value MUST NOT carry both (R-14.5-h). Use [`OfText`](./Stackific.Mcp.Protocol/ResourceContents/OfText.md)/[`OfBlob`](./Stackific.Mcp.Protocol/ResourceContents/OfBlob.md) to construct a well-formed variant. |
| class [ResourceContentsConverter](./Stackific.Mcp.Protocol/ResourceContentsConverter.md) | (De)serialiser for [`ResourceContents`](./Stackific.Mcp.Protocol/ResourceContents.md) that enforces the §14.5 variant rules on read: a value MUST carry exactly one of `text` or `blob` (R-14.5-d, R-14.5-g), MUST NOT carry both (R-14.5-h), and any `blob` MUST be valid Base64 (R-14.5-f). Writing is the default record projection. |
| record [ResourceLink](./Stackific.Mcp.Protocol/ResourceLink.md) | A reference to a resource by URI rather than its contents (spec §14.4.4). |
| static class [Resources](./Stackific.Mcp.Protocol/Resources.md) | The §17.4–§17.6 normative resource helpers ported from the TypeScript SDK's `resources.ts` / `resources-read.ts`: RFC 3986 / RFC 6570 validation (delegating the grammar to [`UriTemplate`](./Stackific.Mcp.Protocol/UriTemplate.md)), the not-found error model (modern `-32602` plus legacy `-32002` client acceptance), the empty-`contents` guard, and the read-reply discrimination that tolerates an absent `resultType` and recognises the `input_required` continuation. |
| record [ResourcesCapability](./Stackific.Mcp.Protocol/ResourcesCapability.md) | Server resources capability with its sub-flags (spec §6.3/§17). |
| record [ResourceTemplate](./Stackific.Mcp.Protocol/ResourceTemplate.md) | A family of resources whose concrete URIs are produced by expanding an RFC 6570 URI Template (spec §17.4). Has no `size` (size is a property of a concrete resource). |
| record [ResourceTemplateReference](./Stackific.Mcp.Protocol/ResourceTemplateReference.md) | A completion reference identifying a resource template by URI (spec §19.3, `type: "ref/resource"`). |
| record [ResourceUiMeta](./Stackific.Mcp.Protocol/ResourceUiMeta.md) | Presentation and security hints carried under a UI resource `contents` entry's own `_meta.ui` object (spec §26.4). When present on the resource these hints take effect for rendering; all fields are OPTIONAL. |
| record [ResourceUpdatedNotificationParams](./Stackific.Mcp.Protocol/ResourceUpdatedNotificationParams.md) | Parameters of the `notifications/resources/updated` notification (spec §17.7). |
| class [ResponseCache&lt;T&gt;](./Stackific.Mcp.Protocol/ResponseCache-1.md) | A minimal in-memory client response cache wired to [`IsFresh`](./Stackific.Mcp.Protocol/Caching/IsFresh.md), [`ResolveCacheScope`](./Stackific.Mcp.Protocol/Caching/ResolveCacheScope.md), and the method→notification invalidation map (§13, R-13.5-j) — the C# counterpart of the TypeScript `ResponseCache`. |
| record [ResultDiscrimination](./Stackific.Mcp.Protocol/ResultDiscrimination.md) | The outcome of [`DiscriminateResultType`](./Stackific.Mcp.Protocol/MultiRoundTrip/DiscriminateResultType.md) (spec §11.5). |
| enum [ResultDiscriminationAction](./Stackific.Mcp.Protocol/ResultDiscriminationAction.md) | What a client should do after [`DiscriminateResultType`](./Stackific.Mcp.Protocol/MultiRoundTrip/DiscriminateResultType.md) (spec §11.5). |
| static class [ResultTypes](./Stackific.Mcp.Protocol/ResultTypes.md) | The values of the base result discriminator `resultType` (spec §3.6, §11.2). The set is open, but two values are defined by the core protocol; extensions (for example Tasks) mint more. |
| static class [RevisionNegotiation](./Stackific.Mcp.Protocol/RevisionNegotiation.md) | Revision selection and the recovery paths for the two negotiation errors (spec §5.4–§5.7) — the C# counterpart of the TypeScript `protocol/negotiation.ts` module. It turns the raw materials discovery produces (a set of advertised revisions and capabilities) into a chosen protocol revision, and defines what a client does when a request is rejected. |
| enum [RevisionNegotiationFailure](./Stackific.Mcp.Protocol/RevisionNegotiationFailure.md) | Why §5.4 revision selection failed. The rule has a single failure mode. |
| record [RevisionNegotiationResult](./Stackific.Mcp.Protocol/RevisionNegotiationResult.md) | The two possible outcomes of the §5.4 revision-selection rule. Mirrors TS `RevisionNegotiationResult`. |
| enum [Role](./Stackific.Mcp.Protocol/Role.md) | The author/recipient role of a message or the intended audience of content (spec §14.7). The only permitted wire values are `user` and `assistant`. |
| record [RootCandidate](./Stackific.Mcp.Protocol/RootCandidate.md) | A candidate root a client is considering exposing, paired with its consent and scope state (§21.1.5). |
| enum [RootExclusionReason](./Stackific.Mcp.Protocol/RootExclusionReason.md) | Why a candidate root was excluded by [`AssembleListRootsResult`](./Stackific.Mcp.Protocol/RootsValidation/AssembleListRootsResult.md) (§21.1.5). |
| record [RootsAssembly](./Stackific.Mcp.Protocol/RootsAssembly.md) | The outcome of [`AssembleListRootsResult`](./Stackific.Mcp.Protocol/RootsValidation/AssembleListRootsResult.md): the validated listing plus the excluded candidates. |
| static class [RootsValidation](./Stackific.Mcp.Protocol/RootsValidation.md) | The §21.1.5 behavioral layer for the Deprecated Roots capability — the C# counterpart of the TypeScript `protocol/roots.ts` validation helpers. The wire records (Root, ListRootsResult) stay permissive so any well-formed payload round-trips; this static class adds the §21.1.5 MUST/SHOULD checks the spec layers on top: the `file://` + RFC 3986 `uri` constraint (R-21.1.5-b/d), the path-traversal guard (R-21.1.5-i), the consent/scope assembly pipeline (R-21.1.5-g/h), the non-`file`-scheme disposition (R-21.1.5-c), and the server-side derived-path containment check (R-21.1.5-k). |
| record [SamplingAudioContent](./Stackific.Mcp.Protocol/SamplingAudioContent.md) | Base64-encoded audio content with a MIME type within a sampling message (spec §21.2.6; field definition §14.4.3). |
| record [SamplingCapability](./Stackific.Mcp.Protocol/SamplingCapability.md) | Deprecated client sampling capability with its sub-flags (spec §6.2/§21). |
| record [SamplingConsentObligations](./Stackific.Mcp.Protocol/SamplingConsentObligations.md) | The §21.2.10 consent &amp; safety obligations a conforming client/host claims to honor around sampling, surfaced as a structured checklist. Mirrors TS `SamplingConsentObligations`. |
| static class [SamplingContentBlocks](./Stackific.Mcp.Protocol/SamplingContentBlocks.md) | Ergonomic factory helpers for building [`SamplingMessageContentBlock`](./Stackific.Mcp.Protocol/SamplingMessageContentBlock.md) values (spec §21.2.6) for the Deprecated Sampling capability (§21.2). |
| record [SamplingGateResult](./Stackific.Mcp.Protocol/SamplingGateResult.md) | The outcome of a sampling capability gate (§21.2.3). Mirrors TS `SamplingGateResult`. |
| record [SamplingImageContent](./Stackific.Mcp.Protocol/SamplingImageContent.md) | Base64-encoded image content with a MIME type within a sampling message (spec §21.2.6; field definition §14.4.2). |
| record [SamplingMessage](./Stackific.Mcp.Protocol/SamplingMessage.md) | A single message in a sampling conversation (spec §21.2.6) for the Deprecated Sampling capability (§21.2). |
| abstract record [SamplingMessageContentBlock](./Stackific.Mcp.Protocol/SamplingMessageContentBlock.md) | The discriminated union of content blocks carried by a [`SamplingMessage`](./Stackific.Mcp.Protocol/SamplingMessage.md) (spec §21.2.6). The wire discriminator is the `type` field. |
| record [SamplingTextContent](./Stackific.Mcp.Protocol/SamplingTextContent.md) | Plain text content within a sampling message (spec §21.2.6; field definition §14.4.1). |
| enum [SamplingToolField](./Stackific.Mcp.Protocol/SamplingToolField.md) | Which sampling member triggered a tool-use gate rejection (§21.2.3). |
| static class [SamplingValidation](./Stackific.Mcp.Protocol/SamplingValidation.md) | The §21.2 sampling behavioral layer — the C# counterpart of the TypeScript `protocol/sampling.ts` helpers — for the Deprecated Sampling capability. The wire records above carry the data shapes; this static class adds the rules the spec layers on top: the capability gate ([`GateSamplingToolUse`](./Stackific.Mcp.Protocol/SamplingValidation/GateSamplingToolUse.md) / [`MayServerSendSamplingRequest`](./Stackific.Mcp.Protocol/SamplingValidation/MayServerSendSamplingRequest.md), rejecting tool use without `sampling.tools` with `-32602`; gating the Deprecated `includeContext` values via `sampling.context`), the §21.2.7/§21.2.6 message-content constraints (ordering, user tool_result exclusivity, tool_result back-references), the `ModelPreferences` priority 0..1 validation (R-21.2.9-e), and the various resolvers/defaults. |
| struct [SchemaLimits](./Stackific.Mcp.Protocol/SchemaLimits.md) | The resource bounds an implementation imposes on schema processing (spec §16.4(6), R-16.4-m). |
| record [SchemaValueValidation](./Stackific.Mcp.Protocol/SchemaValueValidation.md) | The outcome of validating a JSON value against a JSON Schema document (spec §16.4, R-16.4-o/p). |
| static class [ScopeStepUp](./Stackific.Mcp.Protocol/ScopeStepUp.md) | Least-privilege scope selection, the scope union that never drops already-granted scopes, and the bounded step-up re-authorization plan (spec §23.18, R-23.18-a – R-23.18-r, R-23.1-ae – R-23.1-ag). |
| record [ScopeUpgradeKey](./Stackific.Mcp.Protocol/ScopeUpgradeKey.md) | A scope-upgrade attempt key: the resource-and-operation combination being upgraded (spec §23.18, R-23.18-r). |
| class [ScopeUpgradeTracker](./Stackific.Mcp.Protocol/ScopeUpgradeTracker.md) | Tracks bounded step-up retry attempts per resource-and-operation combination, so a client retries no more than a few times and treats persistent failure as a permanent authorization failure (spec §23.18, R-23.18-q, R-23.18-r, R-23.1-af, R-23.1-ag). |
| record [SecureAuthorizationRequestRecord](./Stackific.Mcp.Protocol/SecureAuthorizationRequestRecord.md) | The per-request record that MUST hold the recorded issuer, PKCE code verifier, and `state` together (spec §23.19, R-23.19-e, R-23.19-j, R-23.19-k, R-23.19-l). |
| static class [Security](./Stackific.Mcp.Protocol/Security.md) | S44 — Security Considerations (spec §28). |
| record [ServerCapabilities](./Stackific.Mcp.Protocol/ServerCapabilities.md) | The capabilities a server advertises in its `server/discover` result (spec §6.3). As with client capabilities, presence declares support and unknown fields are ignored (§6.6). |
| enum [StepUpAction](./Stackific.Mcp.Protocol/StepUpAction.md) | The next action a step-up driver should take, from [`NextAction`](./Stackific.Mcp.Protocol/ScopeUpgradeTracker/NextAction.md) (spec §23.18). |
| enum [StepUpActor](./Stackific.Mcp.Protocol/StepUpActor.md) | Who the client is acting for, governing whether a step-up flow is attempted (spec §23.18, R-23.18-m, R-23.18-n). |
| record [StepUpPlan](./Stackific.Mcp.Protocol/StepUpPlan.md) | A plan for one step-up re-authorization, from [`Plan`](./Stackific.Mcp.Protocol/ScopeStepUp/Plan.md) (spec §23.18). |
| class [Subscription](./Stackific.Mcp.Protocol/Subscription.md) | Tracks the request-scoped lifecycle of a single client-side subscription (spec §10.7). The state is scoped to the `subscriptions/listen` request, NOT to the connection: once closed the subscription is gone and retains NO resumable state — re-establishment is a NEW `subscriptions/listen` request yielding a NEW id (R-10.7-d, R-10.7-f). The C# counterpart of the TypeScript `Subscription` class. |
| enum [SubscriptionCloseReason](./Stackific.Mcp.Protocol/SubscriptionCloseReason.md) | How a subscription stream ended (spec §10.7). |
| record [SubscriptionFilter](./Stackific.Mcp.Protocol/SubscriptionFilter.md) | The notification kinds a client opts in to on a `subscriptions/listen` stream (spec §10.2). All fields are OPTIONAL; an omitted/false field means "not subscribed". The server echoes the subset it will honor in the acknowledgement (§10.3). |
| class [SubscriptionRegistry](./Stackific.Mcp.Protocol/SubscriptionRegistry.md) | Routes incoming subscription notifications to the correct active [`Subscription`](./Stackific.Mcp.Protocol/Subscription.md) by `io.modelcontextprotocol/subscriptionId` — essential on stdio where all subscriptions share one channel, and supported on HTTP where the key is still present (§10.4, R-10.4-c, R-10.4-d). Holds NO state across connections; closing a subscription removes it. The C# counterpart of the TypeScript `SubscriptionRegistry`. |
| static class [Subscriptions](./Stackific.Mcp.Protocol/Subscriptions.md) | Pure, side-effect-free helpers for server-to-client subscriptions (spec §10): the four change-notification kinds and the request-scoped kinds (§10.5, §10.6), absolute-URI validation, sub-resource (container) URI coverage with boundary safety (§10.5, R-10.5-j), the honored-subset computation including the `taskIds` gate (§10.3, §25.10), stream-boundary violation detection (§10.6), and the declined-kinds report (§10.3). The C# counterpart of the exported functions in the TypeScript `protocol/streaming.ts` (S16). The client-facing [`Subscription`](./Stackific.Mcp.Protocol/Subscription.md) / [`SubscriptionRegistry`](./Stackific.Mcp.Protocol/SubscriptionRegistry.md) lifecycle objects live alongside this class. |
| record [SubscriptionsAcknowledgedNotificationParams](./Stackific.Mcp.Protocol/SubscriptionsAcknowledgedNotificationParams.md) | Parameters of the `notifications/subscriptions/acknowledged` notification (spec §10.3) — the first message on every subscription stream. The subscription identifier is carried in the notification's `_meta` under `io.modelcontextprotocol/subscriptionId` (§10.4). |
| record [SubscriptionsListenRequestParams](./Stackific.Mcp.Protocol/SubscriptionsListenRequestParams.md) | Parameters of the `subscriptions/listen` request (spec §10.2). |
| enum [SubscriptionState](./Stackific.Mcp.Protocol/SubscriptionState.md) | The lifecycle states of a subscription (spec §10.7). |
| enum [TaskExecutionOutcomeKind](./Stackific.Mcp.Protocol/TaskExecutionOutcomeKind.md) | How a finished augmented request maps onto a terminal task status (spec §25.11). |
| static class [Tasks](./Stackific.Mcp.Protocol/Tasks.md) | Pure, side-effect-free helpers for the Tasks extension (spec §25): the case-sensitive extension identifier and its matching rule (§25.1), the five-state lifecycle and its legal transitions (§25.5), inline-outcome consistency (§25.5), `ttlMs` expiry / backstop eligibility (§25.4, §25.6, §25.11), polling-cadence decisions (§25.7), the protocol-vs-application error classification (§25.11), the `notifications/tasks` push gating and builder (§25.10), and the task-error builders/codes (§22). The C# counterpart of the exported functions in the TypeScript `protocol/tasks.ts` (S39) and the lifecycle helpers of `protocol/tasks-lifecycle.ts` (S40) that operate on the task model. The model records ([`McpTask`](./Stackific.Mcp.Protocol/McpTask.md), [`DetailedTask`](./Stackific.Mcp.Protocol/DetailedTask.md), …) live alongside this class. |
| record [TaskStatusNotificationParams](./Stackific.Mcp.Protocol/TaskStatusNotificationParams.md) | The parameters of the `notifications/tasks` notification (spec §25.10), by which a server pushes a task state change. The params are a complete [`DetailedTask`](./Stackific.Mcp.Protocol/DetailedTask.md) for the task's current status — identical to what `tasks/get` would have returned at that moment — so a subscribed client need not issue an extra `tasks/get`. Delivery is opt-in via a `taskIds` subscription filter on `subscriptions/listen` (§10, §25.10). |
| record [TextContent](./Stackific.Mcp.Protocol/TextContent.md) | Plain text content (spec §14.4.1). |
| static class [TokenRequests](./Stackific.Mcp.Protocol/TokenRequests.md) | Token-request body builders, the encoder, and the Step-2/Step-4 audience invariant (spec §23.5, §23.6, §23.9). |
| record [TokenResponse](./Stackific.Mcp.Protocol/TokenResponse.md) | The token-endpoint JSON response (spec §23.5 Step 4, §23.9). [`AccessToken`](./Stackific.Mcp.Protocol/TokenResponse/AccessToken.md) and [`TokenType`](./Stackific.Mcp.Protocol/TokenResponse/TokenType.md) (`Bearer`) are REQUIRED; the rest are OPTIONAL — a client MUST NOT assume a refresh token will be issued (R-23.9-d). |
| record [Tool](./Stackific.Mcp.Protocol/Tool.md) | A tool definition (spec §16.3): a programmatic [`Name`](./Stackific.Mcp.Protocol/Tool/Name.md), a REQUIRED JSON Schema for its arguments, and optional output schema, annotations, and display metadata. |
| static class [ToolAnnotationRules](./Stackific.Mcp.Protocol/ToolAnnotationRules.md) | The §16.7 untrusted-annotation surface: applies the spec defaults to absent hint fields and exposes the fail-closed trust gate that guards safety decisions made on annotations from an untrusted server. |
| record [ToolAnnotations](./Stackific.Mcp.Protocol/ToolAnnotations.md) | Untrusted, human- and model-oriented hints about a tool's behavior (spec §16.7). A client MUST NOT make safety decisions based on annotations from an untrusted server. |
| enum [ToolCallFailureSituation](./Stackific.Mcp.Protocol/ToolCallFailureSituation.md) | The situations a `tools/call` failure can arise from, used to pick the reporting mechanism (§22.5). |
| class [ToolCallRateLimiter](./Stackific.Mcp.Protocol/ToolCallRateLimiter.md) | A sliding-window rate limiter a server applies to `tools/call` so a hostile or malfunctioning client cannot drive unbounded execution or downstream load (spec §28.3, R-28.3-g, R-28.3-h; AC-44.9). |
| record [ToolChoice](./Stackific.Mcp.Protocol/ToolChoice.md) | Controls the model's tool-use behavior for a sampling request (spec §21.2.5). A client MUST return an error if this is provided but the client did not declare the `sampling.tools` capability (§21.2.4). |
| enum [ToolChoiceMode](./Stackific.Mcp.Protocol/ToolChoiceMode.md) | Controls how the model uses tools during sampling (spec §21.2.5). The default behavior when the request omits `toolChoice` is `{ "mode": "auto" }`. |
| enum [ToolFailureMechanism](./Stackific.Mcp.Protocol/ToolFailureMechanism.md) | The two distinct mechanisms for reporting that something went wrong with a `tools/call`. Choosing the correct one is a MUST (R-22.5-a, AC-34.18). |
| static class [ToolNames](./Stackific.Mcp.Protocol/ToolNames.md) | Tool name-convention and display-name helpers (spec §16.3). |
| record [ToolReferenceValidationResult](./Stackific.Mcp.Protocol/ToolReferenceValidationResult.md) | The outcome of a tool_result back-reference validation (§21.2.6-d). Mirrors the TS `{ ok, reason?, toolUseId? }` shape. |
| record [ToolResultContent](./Stackific.Mcp.Protocol/ToolResultContent.md) | The result of a tool use, provided by the user back to the assistant, carried as a sampling content block (spec §21.2.6, `type: "tool_result"`). |
| record [ToolsCapability](./Stackific.Mcp.Protocol/ToolsCapability.md) | Server tools capability (spec §6.3/§16). |
| enum [ToolSchemaRole](./Stackific.Mcp.Protocol/ToolSchemaRole.md) | The schema slot being validated: governs the root-`type` rule (spec §16.4). |
| static class [ToolSchemas](./Stackific.Mcp.Protocol/ToolSchemas.md) | The §16.4 JSON-Schema hardening gate and the §16.7 tool-annotation resolution for the Tools feature, ported from the TypeScript SDK's `tools.ts` / `tools-call.ts`. These are the normative pure helpers that protect schema registration against resource exhaustion and SSRF and that apply the spec defaults to untrusted annotation hints. |
| struct [ToolSchemaValidation](./Stackific.Mcp.Protocol/ToolSchemaValidation.md) | The discriminated outcome of [`ValidateToolSchema`](./Stackific.Mcp.Protocol/ToolSchemas/ValidateToolSchema.md) (spec §16.4): either the schema is safe to validate against / register (with its resolved dialect), or it is rejected with a human-readable reason. |
| record [ToolUiMeta](./Stackific.Mcp.Protocol/ToolUiMeta.md) | The object placed under a tool's `_meta.ui` key to declare its associated interactive user interface (spec §26.3). A receiver that has not negotiated the extension MUST ignore this key (§24). |
| record [ToolUseContent](./Stackific.Mcp.Protocol/ToolUseContent.md) | A request from the assistant to call a tool, carried as a sampling content block (spec §21.2.6, `type: "tool_use"`). |
| static class [TraceContext](./Stackific.Mcp.Protocol/TraceContext.md) | The §15.4 W3C trace-context propagation helpers — the C# counterpart of the TypeScript `protocol/logging.ts` trace utilities. The three bare keys (`traceparent`, `tracestate`, `baggage`) ride in `_meta`; receivers MUST treat them as opaque and intermediaries SHOULD propagate them UNCHANGED (R-15.4.2-h). This class uses the format validators already defined on [`MetaKeys`](./Stackific.Mcp.Json/MetaKeys.md). |
| enum [TransportFamily](./Stackific.Mcp.Protocol/TransportFamily.md) | The transport families relevant to authorization applicability (spec §23.1, R-23.1-a – R-23.1-c). |
| static class [Ui](./Stackific.Mcp.Protocol/Ui.md) | S41 — Interactive UI Extension I: the server-facing, static behavior of the apps extension (spec §26.1–§26.4). The C# counterpart of the TypeScript `protocol/ui.ts` module: MIME/scheme predicates, host-advertisement reading, server gating (R-26.2-f/g), the visibility model (R-26.3-e/f), CSP/permission logic, and the UI-resource builders. |
| record [UiContentSecurityPolicy](./Stackific.Mcp.Protocol/UiContentSecurityPolicy.md) | A content-security-policy descriptor for a UI resource (spec §26.4). Each member lists origin strings; an origin not present in the applicable member MUST be blocked by the host. When the whole [`Csp`](./Stackific.Mcp.Protocol/ResourceUiMeta/Csp.md) is omitted the host applies a deny-by-default policy (§26.7). |
| static class [UiHost](./Stackific.Mcp.Protocol/UiHost.md) | S42 — Interactive UI Extension II: the UI-to-host message dialect, its verbatim method/notification registry, the handshake ordering, message validation, the §26.8 error contract, the host mediation/consent gates, sandbox reporting, and the data-exposure guard (spec §26.5–§26.9). The C# counterpart of the TypeScript `protocol/ui-host.ts` module. |
| record [UiHostExtensionCapability](./Stackific.Mcp.Protocol/UiHostExtensionCapability.md) | The value a host advertises under the `io.modelcontextprotocol/ui` key of its `extensions` capability map (spec §26.2). A server MUST NOT declare UI associations unless a host has advertised this with a [`MimeTypes`](./Stackific.Mcp.Protocol/UiHostExtensionCapability/MimeTypes.md) array that includes [`MimeType`](./Stackific.Mcp.Protocol/UiResource/MimeType.md). |
| record [UiPermissions](./Stackific.Mcp.Protocol/UiPermissions.md) | The sandbox permissions a UI resource requests (spec §26.4). Each member's presence requests that capability and its value is an empty object; the host MUST NOT grant a capability that is not requested and MAY decline a requested one (§26.7). Absent members are `null`. |
| static class [UiResource](./Stackific.Mcp.Protocol/UiResource.md) | Constants for the Interactive User-Interface extension (spec §26): the UI resource MIME type (§26.4) and the extension identifier used to negotiate the extension (§26.2). |
| enum [UiVisibility](./Stackific.Mcp.Protocol/UiVisibility.md) | The audiences a tool with a declared UI is exposed to (spec §26.3 `visibility`). When the `visibility` array is omitted it is treated as both Model and App. |
| record [UnknownErrorDescriptor](./Stackific.Mcp.Protocol/UnknownErrorDescriptor.md) | A descriptor for an error response carrying an unrecognised code, surfaced as a failed request rather than rejected as malformed (R-22.7-e, AC-34.24). |
| record [UnsupportedContentBlock](./Stackific.Mcp.Protocol/UnsupportedContentBlock.md) | A content block with an unrecognised `type`, preserved verbatim so a forward-compatible receiver can carry it without failing the enclosing message (R-14.4-b). |
| class [UnsupportedDialectException](./Stackific.Mcp.Protocol/UnsupportedDialectException.md) | Raised when a tool schema declares a JSON Schema dialect this implementation cannot validate against (spec §16.4(9), R-16.4-t). The implementation MUST handle an unsupported dialect by signalling an error rather than silently ignoring the declaration or treating the schema as permissive. |
| record [UpdateTaskRequestParams](./Stackific.Mcp.Protocol/UpdateTaskRequestParams.md) | The parameters of a `tasks/update` request (spec §25.8): supplies the responses to the outstanding input requests of a task in the InputRequired status. |
| record [UpdateTaskResult](./Stackific.Mcp.Protocol/UpdateTaskResult.md) | The result of a `tasks/update` request (spec §25.8): an empty acknowledgement. The runtime supplies the base `resultType` as `"complete"` (see [`Complete`](./Stackific.Mcp.Protocol/ResultTypes/Complete.md)). The acknowledgement is eventually consistent: it MAY be returned before the task's observable status reflects the responses. |
| static class [UriTemplate](./Stackific.Mcp.Protocol/UriTemplate.md) | RFC 3986 resource-URI and RFC 6570 URI-Template validation, variable extraction, expansion, and matching for the Resources feature (spec §17.4). This is the C# counterpart of the TypeScript SDK's `isResourceUri` / `isUriTemplate` / `uriTemplateVariables` helpers, plus the expand/match machinery the server runtime uses to turn a concrete `resources/read` URI back into the variables of a registered template. |
| class [UriTemplateMatcher](./Stackific.Mcp.Protocol/UriTemplateMatcher.md) | A compiled matcher for a single URI template that recovers variable values from a concrete URI (spec §17.4). Produced by [`CompileMatcher`](./Stackific.Mcp.Protocol/UriTemplate/CompileMatcher.md). |
| class [UriTemplateValues](./Stackific.Mcp.Protocol/UriTemplateValues.md) | A simple, immutable set of variable bindings for [`Expand`](./Stackific.Mcp.Protocol/UriTemplate/Expand.md) (spec §17.4): each variable maps to a single scalar string or a list of strings (for the explode modifier). |
| static class [WellKnownDiscovery](./Stackific.Mcp.Protocol/WellKnownDiscovery.md) | The well-known `.well-known` URI construction orders for protected-resource and authorization-server metadata discovery (spec §23.2 R-23.2-c – R-23.2-g; §23.3 R-23.3-b, R-23.3-c). |
| static class [WwwAuthenticate](./Stackific.Mcp.Protocol/WwwAuthenticate.md) | Builders, the parser, and scope helpers for the `Bearer``WWW-Authenticate` challenge (spec §23.1, R-23.1-t – R-23.1-ad). |
| record [WwwAuthenticateChallenge](./Stackific.Mcp.Protocol/WwwAuthenticateChallenge.md) | The structured fields of a `Bearer``WWW-Authenticate` challenge (spec §23.1) — the parameter set carried in the HTTP response header, not a JSON object. |

## Stackific.Mcp.Server namespace

| public type | description |
| --- | --- |
| delegate [ArgumentCompleter](./Stackific.Mcp.Server/ArgumentCompleter.md) | Computes ranked completion candidates for a partial argument value (spec §19). |
| static class [AuthGates](./Stackific.Mcp.Server/AuthGates.md) | Server-side authorization glue (spec §23): turns a token-validation callback into an [`IMcpAuthGate`](./Stackific.Mcp.Transport/IMcpAuthGate.md) for the Streamable HTTP adapter — emitting the `401` + `WWW-Authenticate` challenge (carrying `resource_metadata` and, when known, `scope`) and the `403``insufficient_scope` challenge the spec requires — and builds the RFC 9728 protected-resource metadata document. The C# counterpart of ts-sdk's `server/auth.ts`. |
| record [BearerAuthGateOptions](./Stackific.Mcp.Server/BearerAuthGateOptions.md) | Configures the server bearer-auth gate (spec §23): the protected-resource metadata URI advertised on the `401` challenge, this resource's canonical identifier (the expected token audience), the scopes a request must carry, and the token-validation callback. |
| class [InMemoryTaskStore](./Stackific.Mcp.Server/InMemoryTaskStore.md) | An in-memory, thread-safe store for the Tasks extension (spec §25). It mints opaque task ids, tracks lifecycle state with terminal-state immutability (§25.5), holds the inline result/error of terminal tasks, and answers `tasks/get` with a [`DetailedTask`](./Stackific.Mcp.Protocol/DetailedTask.md). A `now` clock is injectable for deterministic tests. Durability beyond the process lifetime is out of scope for this in-memory implementation; a server needing it supplies its own store. |
| class [McpServer](./Stackific.Mcp.Server/McpServer.md) | A spec-conformant MCP server runtime (spec §5, §16–§19): a dispatcher onto which features are registered (tools, resources, resource templates, prompts, completion) and which turns a parsed JSON-RPC request into the correct response. It owns no transport — the in-memory bridge and the Streamable HTTP adapter call [`HandleRequestAsync`](./Stackific.Mcp.Server/McpServer/HandleRequestAsync.md) — so the same server runs unchanged over any binding. Processing is stateless: every request is validated and served on its own (§4.4). |
| delegate [PromptGetHandler](./Stackific.Mcp.Server/PromptGetHandler.md) | Resolves a prompt with supplied arguments into messages (spec §18.4). |
| delegate [ResourceReadHandler](./Stackific.Mcp.Server/ResourceReadHandler.md) | Reads a concrete resource by URI (spec §17.5). |
| delegate [ResourceTemplateReadHandler](./Stackific.Mcp.Server/ResourceTemplateReadHandler.md) | Reads a resource produced by expanding a template (spec §17.5). |
| class [StdioServerHost](./Stackific.Mcp.Server/StdioServerHost.md) | Serves an [`McpServer`](./Stackific.Mcp.Server/McpServer.md) over a byte-channel transport — the stdio transport in practice (spec §8). It reads inbound client requests/notifications from the transport, dispatches each through [`HandleRequestAsync`](./Stackific.Mcp.Server/McpServer/HandleRequestAsync.md), and writes the responses and request-scoped notifications back over the same channel. |
| class [SubscriptionManager](./Stackific.Mcp.Server/SubscriptionManager.md) | Tracks active `subscriptions/listen` streams and fans server-initiated change notifications out to the ones that opted into each kind (spec §10.5). Each delivered notification is tagged with the subscription identifier in `_meta` (§10.4), and the server only honors the filter kinds it actually supports (§10.3). |
| class [ToolContext](./Stackific.Mcp.Server/ToolContext.md) | The context handed to a [`ToolHandler`](./Stackific.Mcp.Server/ToolHandler.md) for a single `tools/call` (spec §16.5). It exposes the call arguments, the inbound request metadata (including trace context, §15.4), the authenticated identity (§23), a cooperative cancellation signal (§9.6.2/§15.2), and sinks for request-scoped notifications (progress §15.1, logging §15.3) and subscription fan-out (§10). |
| delegate [ToolHandler](./Stackific.Mcp.Server/ToolHandler.md) | Handles a `tools/call`: receives the call context and returns the tool result (spec §16.5). |
| static class [UiHelpers](./Stackific.Mcp.Server/UiHelpers.md) | Helpers for the Interactive User-Interface extension (spec §26): a server returns an interactive UI by embedding a `ui://` resource (`text/html;profile=mcp-app`) in a tool result, which the host renders sandboxed. The C# counterpart of the SDK's `uiToolResult`. |

## Stackific.Mcp.Testing namespace

| public type | description |
| --- | --- |
| static class [InMemory](./Stackific.Mcp.Testing/InMemory.md) | A test-kit that wires an [`McpClient`](./Stackific.Mcp.Client/McpClient.md) directly to an [`McpServer`](./Stackific.Mcp.Server/McpServer.md) over the in-memory transport (spec-faithful request/response shape, no HTTP). Useful for end-to-end SDK tests and for embedding a server in the same process as its client. |

## Stackific.Mcp.Transport namespace

| public type | description |
| --- | --- |
| record [AuthGateResult](./Stackific.Mcp.Transport/AuthGateResult.md) | The result of an authorization gate (spec §23): either an authorized identity, or a challenge to return to the client (an HTTP status and a `WWW-Authenticate` header). |
| record [AuthInfo](./Stackific.Mcp.Transport/AuthInfo.md) | The validated authorization identity a transport threads into request processing when a request carried a bearer token (spec §23). It is per-request input (never connection state, §4.4) and is surfaced to tool handlers via the request context. |
| delegate [ChildProcessLauncher](./Stackific.Mcp.Transport/ChildProcessLauncher.md) | A factory that (re)launches a fresh child process — used for restart (spec §8.6.4; R-8.6.4-a). |
| abstract class [ClientTransport](./Stackific.Mcp.Transport/ClientTransport.md) | The client-side transport abstraction: it sends a single JSON-RPC request or notification and, for a request, returns the final response — delivering any interim request-scoped notifications through [`OnNotification`](./Stackific.Mcp.Transport/RequestOptions/OnNotification.md). Concrete transports are the in-memory bridge ([`InMemoryClientTransport`](./Stackific.Mcp.Transport/InMemoryClientTransport.md)) and Streamable HTTP. Every outbound and inbound frame is offered to [`OnSend`](./Stackific.Mcp.Transport/ClientTransport/OnSend.md)/[`OnReceive`](./Stackific.Mcp.Transport/ClientTransport/OnReceive.md) so a host can tap the wire (§9). |
| static class [CustomTransportObligations](./Stackific.Mcp.Transport/CustomTransportObligations.md) | The obligations on a custom transport (spec §7.3). Mirrors TypeScript `CUSTOM_TRANSPORT_OBLIGATIONS`. |
| enum [DirectionalKind](./Stackific.Mcp.Transport/DirectionalKind.md) | The structural kind of a message, as used by directionality enforcement (spec §7.4). Both response forms (success and error) share the same directionality, so they collapse to a single value. |
| interface [IByteChannelTransport](./Stackific.Mcp.Transport/IByteChannelTransport.md) | The abstract byte-channel transport contract every defined or custom transport satisfies (spec §7.1, §7.2). |
| interface [IByteSink](./Stackific.Mcp.Transport/IByteSink.md) | A sink for outbound bytes — the C# analogue of a Node `Writable` stream (spec §8 topology). The client writes framed requests/notifications to the child's stdin sink; the server writes framed responses/notifications to its stdout sink. [`End`](./Stackific.Mcp.Transport/IByteSink/End.md) closes the sink (EOF), the portable graceful-shutdown signal (R-8.6.2-a step 1). |
| interface [IByteSource](./Stackific.Mcp.Transport/IByteSource.md) | A push-based source of inbound bytes — the C# analogue of a Node `Readable` stream's `'data'`/`'end'` events (spec §8 topology). A real process adapter raises [`DataReceived`](./Stackific.Mcp.Transport/IByteSource/DataReceived.md) for each chunk read from a child stream and [`Ended`](./Stackific.Mcp.Transport/IByteSource/Ended.md) when the stream reaches EOF; an in-memory test pipe raises the same events synchronously. |
| interface [IChildProcess](./Stackific.Mcp.Transport/IChildProcess.md) | The minimal view of a child process the stdio transport needs (spec §8 topology). Modeled so the three streams can be in-memory pipes in tests (no real OS process), while a real Process is adapted to the same shape by [`ProcessChild`](./Stackific.Mcp.Transport/ProcessChild.md). The C# counterpart of the TypeScript `ChildProcessLike`. |
| interface [IFrameDecoder](./Stackific.Mcp.Transport/IFrameDecoder.md) | Splits a byte stream back into the byte boundaries of individual messages, using framing alone — the decoder MUST NOT parse the JSON body to find where one message ends and the next begins (spec §7.2; R-7.2-b, R-7.2-c, R-7.2-d). |
| interface [IMcpAuthGate](./Stackific.Mcp.Transport/IMcpAuthGate.md) | A pluggable authorization gate the Streamable HTTP adapter consults before dispatching a request (spec §23). An unauthenticated request is answered with a 401 challenge carrying `WWW-Authenticate`; an authenticated request yields the validated [`AuthInfo`](./Stackific.Mcp.Transport/AuthInfo.md). |
| interface [IMcpRequestHandler](./Stackific.Mcp.Transport/IMcpRequestHandler.md) | The transport-facing surface of an MCP server: it accepts a parsed request and produces the final JSON-RPC response, emitting any request-scoped notifications through the supplied [`IServerNotifier`](./Stackific.Mcp.Transport/IServerNotifier.md). Implemented by `McpServer`; consumed by every server transport (the in-memory bridge and the Streamable HTTP adapter), which handle framing, headers, and routing. |
| interface [IMcpSubscriptionHandler](./Stackific.Mcp.Transport/IMcpSubscriptionHandler.md) | A handler that can open long-lived `subscriptions/listen` streams (spec §10). A transport that supports server-to-client streaming registers a subscription and pumps the delivered notifications onto the stream until the client disconnects. |
| interface [IMessageFramer](./Stackific.Mcp.Transport/IMessageFramer.md) | Encodes messages to delimited byte units and produces decoders that recover them. An [`IMessageFramer`](./Stackific.Mcp.Transport/IMessageFramer.md) is the §7.2 framing guarantee made concrete. The C# counterpart of the TypeScript `MessageFramer`. |
| class [InMemoryByteChannelTransport](./Stackific.Mcp.Transport/InMemoryByteChannelTransport.md) | The S12 in-memory reference byte-channel transport (spec §7.1–§7.6) — a fully in-process [`IByteChannelTransport`](./Stackific.Mcp.Transport/IByteChannelTransport.md) used to demonstrate and test that the §7.2 guarantees can be met by a conforming transport. The C# counterpart of the TypeScript `InMemoryTransport` in `transport/in-memory.ts`. |
| class [InMemoryClientTransport](./Stackific.Mcp.Transport/InMemoryClientTransport.md) | A client transport that bridges directly to an in-process [`IMcpRequestHandler`](./Stackific.Mcp.Transport/IMcpRequestHandler.md) (an `McpServer`) with no HTTP or serialization round-trip. It mirrors the Streamable HTTP request shape — each request gets its own logical response stream over which interim notifications are delivered before the final response (spec §9.6) — which makes it ideal for tests and for embedding a server in the same process. Frames are still offered to the wire taps. |
| interface [IServerNotifier](./Stackific.Mcp.Transport/IServerNotifier.md) | The sink a request handler uses to emit request-scoped notifications (progress, logging) on the originating request's response stream (spec §9.6.2, §10.6). The transport decides how to deliver them: on Streamable HTTP they ride the request's SSE stream; in memory they are handed straight to the client. A notification emitted here MUST relate to the request being processed. |
| enum [KillSignal](./Stackific.Mcp.Transport/KillSignal.md) | The two OS-appropriate termination escalation levels (spec §8.6.3; R-8.6.3-a). |
| static class [MalformedIdError](./Stackific.Mcp.Transport/MalformedIdError.md) | The malformed-id error response — the single permitted id exception (spec §7.2; R-7.2-h). An error reply to a request whose id could not be read MAY carry a `null` id or omit it entirely. The C# counterpart of the TypeScript `buildParseErrorResponse` / `MalformedIdErrorResponseSchema` / `isAcceptableMalformedIdErrorResponse` helpers. |
| enum [MessageDirection](./Stackific.Mcp.Transport/MessageDirection.md) | The two directions a message may travel at the JSON-RPC layer (spec §7.4). |
| static class [MessageUnit](./Stackific.Mcp.Transport/MessageUnit.md) | Decoding one framed unit's bytes back into a [`JsonRpcMessage`](./Stackific.Mcp.JsonRpc/JsonRpcMessage.md), with fatal UTF-8 and single-JSON-value enforcement (spec §7.1, §7.6). The C# counterpart of the TypeScript `decodeMessageUnit` / `tryDecodeMessageUnit` / `encodeMessageUnit` functions. |
| class [NewlineFrameDecoder](./Stackific.Mcp.Transport/NewlineFrameDecoder.md) | A stateful newline-delimited frame decoder: buffers partial frames across [`Push`](./Stackific.Mcp.Transport/NewlineFrameDecoder/Push.md) calls and emits each complete unit as soon as its `\n` delimiter arrives (spec §7.2). The C# counterpart of the TypeScript `NewlineFrameDecoder`. |
| class [NewlineFramer](./Stackific.Mcp.Transport/NewlineFramer.md) | Newline-delimited JSON-RPC framing over a byte stream (spec §7.2, §7.3, §8 framing). The C# counterpart of the TypeScript `NewlineFramer`. |
| class [ProcessChild](./Stackific.Mcp.Transport/ProcessChild.md) | Adapts a real Process into an [`IChildProcess`](./Stackific.Mcp.Transport/IChildProcess.md) (spec §8 launch). The C# counterpart of the TypeScript real-`node:child_process` convenience. |
| class [PushByteSource](./Stackific.Mcp.Transport/PushByteSource.md) | An in-memory, push-based [`IByteSource`](./Stackific.Mcp.Transport/IByteSource.md) + [`IByteSink`](./Stackific.Mcp.Transport/IByteSink.md) pipe used to drive the stdio transport in tests without spawning a real OS process. Writing bytes synchronously raises [`DataReceived`](./Stackific.Mcp.Transport/PushByteSource/DataReceived.md); [`End`](./Stackific.Mcp.Transport/PushByteSource/End.md) raises [`Ended`](./Stackific.Mcp.Transport/PushByteSource/Ended.md) exactly once. |
| record [RequestContext](./Stackific.Mcp.Transport/RequestContext.md) | The per-request context a server derives solely from a request's `_meta` (spec §7.6). |
| class [RequestCorrelator](./Stackific.Mcp.Transport/RequestCorrelator.md) | Correlates inbound responses to outstanding requests by id only — never by delivery order, connection, stream, or position (spec §7.2; R-7.2-e – R-7.2-g, R-7.2-o). The C# counterpart of the TypeScript `RequestCorrelator`. |
| class [RequestOptions](./Stackific.Mcp.Transport/RequestOptions.md) | Per-request options for a client transport send: the sink for request-scoped notifications (progress/logging) that arrive on this request's stream (spec §9.6.2), an optional progress token to opt into progress, extra `_meta` to carry (for example W3C trace context, §15.4), a timeout, and a cancellation signal that closes the request's stream (§9.6.2/§15.2). |
| static class [StatelessTransportRules](./Stackific.Mcp.Transport/StatelessTransportRules.md) | The statelessness rules a transport and the server above it MUST honor (spec §7.6). Mirrors TypeScript `STATELESS_TRANSPORT_RULES`. |
| class [StdioClientTransport](./Stackific.Mcp.Transport/StdioClientTransport.md) | The client side of a stdio connection: launches/holds a server subprocess, writes requests/notifications to its stdin, and reads responses/notifications from its stdout (spec §8 client role). The C# counterpart of the TypeScript `StdioClientTransport`. |
| class [StdioClientTransportOptions](./Stackific.Mcp.Transport/StdioClientTransportOptions.md) | Options for [`StdioClientTransport`](./Stackific.Mcp.Transport/StdioClientTransport.md). |
| static class [StdioDisconnectPolicy](./Stackific.Mcp.Transport/StdioDisconnectPolicy.md) | Stdio-specific disconnection policy referenced by §7.5 and realized by S13 (spec §8). Mirrors TypeScript `STDIO_DISCONNECT_POLICY`. |
| abstract class [StdioEndpoint](./Stackific.Mcp.Transport/StdioEndpoint.md) | Common stdio plumbing for both endpoints: newline framing over a byte sink, a stateful decoder over a byte source, malformed-line tolerance, and the observable message/error/close surface of [`IByteChannelTransport`](./Stackific.Mcp.Transport/IByteChannelTransport.md). Subclasses supply the concrete subprocess lifecycle. The C# counterpart of the TypeScript `StdioEndpoint` abstract base. |
| class [StdioServerTransport](./Stackific.Mcp.Transport/StdioServerTransport.md) | The server side of a stdio connection: reads client requests/notifications from stdin and writes responses/notifications to stdout (spec §8 server role). The C# counterpart of the TypeScript `StdioServerTransport`. |
| class [StdioServerTransportOptions](./Stackific.Mcp.Transport/StdioServerTransportOptions.md) | Options for [`StdioServerTransport`](./Stackific.Mcp.Transport/StdioServerTransport.md). |
| class [StreamableHttpClientTransport](./Stackific.Mcp.Transport/StreamableHttpClientTransport.md) | The client-side Streamable HTTP transport (spec §9): each request or notification is its own HTTP POST to the MCP endpoint, carrying the required headers (§9.3) and routing headers (§9.4). A request response is read as either a single JSON object (§9.6.1) or a server-sent event stream that delivers request-scoped notifications before the final response (§9.6.2). An optional token provider supplies the bearer credential for authorized resources (§23). |
| static class [StreamableHttpServer](./Stackific.Mcp.Transport/StreamableHttpServer.md) | The server-side Streamable HTTP adapter (spec §9): it parses an HTTP POST into a JSON-RPC message, validates the required request and routing headers (§9.3/§9.4), runs an optional authorization gate (§23), dispatches to an [`IMcpRequestHandler`](./Stackific.Mcp.Transport/IMcpRequestHandler.md), and writes back either a single JSON response (§9.6.1) or a server-sent event stream that carries request-scoped notifications before the final response (§9.6.2). It is stateless and never mints a session id (§9.9). |
| record [StreamableHttpServerOptions](./Stackific.Mcp.Transport/StreamableHttpServerOptions.md) | Configures the Streamable HTTP adapter's transport hardening (spec §9.3, §9.5, §9.9, §9.11). All members are optional; the defaults preserve the adapter's pre-hardening behaviour except for the security checks the spec mandates (Origin validation, Content-Type/Accept validation), which are loopback-safe by default. |
| class [SubscriptionHandle](./Stackific.Mcp.Transport/SubscriptionHandle.md) | A handle to an open subscription stream (spec §10): the filter the server agreed to honor (§10.3), and an action to close the stream and stop delivery (§10.7). |
| struct [TransportCloseInfo](./Stackific.Mcp.Transport/TransportCloseInfo.md) | Why a transport channel became unusable, surfaced to `OnClose` handlers (spec §7.2/§7.5). |
| static class [TransportContract](./Stackific.Mcp.Transport/TransportContract.md) | Transport-contract helpers: directionality (§7.4) and the per-request statelessness model (§7.4, §7.6). The C# counterpart of the free functions in the TypeScript `transport/contract.ts`. |
| class [TransportError](./Stackific.Mcp.Transport/TransportError.md) | A failure of the transport channel itself — distinct from a JSON-RPC error response (spec §7.5). |
| static class [TransportGuarantees](./Stackific.Mcp.Transport/TransportGuarantees.md) | Documentation-constant anchors mapping the §7 transport guarantees, custom-transport obligations, stdio disconnection policy, and statelessness rules to their normative atoms. The C# counterpart of the TypeScript `TRANSPORT_GUARANTEES` / `CUSTOM_TRANSPORT_OBLIGATIONS` / `STDIO_DISCONNECT_POLICY` / `STATELESS_TRANSPORT_RULES` maps. |

## Stackific.Mcp.Transport.Http namespace

| public type | description |
| --- | --- |
| static class [ParamEncoding](./Stackific.Mcp.Transport.Http/ParamEncoding.md) | S14 — `Mcp-Param-*` value encoding (spec §9.5.3). |
| static class [ParamHeaders](./Stackific.Mcp.Transport.Http/ParamHeaders.md) | S14 — Tool parameters surfaced as `Mcp-Param-*` headers (spec §9.5). |

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
