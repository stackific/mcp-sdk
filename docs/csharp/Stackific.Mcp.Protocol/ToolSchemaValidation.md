# ToolSchemaValidation structure

The discriminated outcome of [`ValidateToolSchema`](./ToolSchemas/ValidateToolSchema.md) (spec §16.4): either the schema is safe to validate against / register (with its resolved dialect), or it is rejected with a human-readable reason.

```csharp
public struct ToolSchemaValidation : IEquatable<ToolSchemaValidation>
```

| parameter | description |
| --- | --- |
| Ok | Whether the schema passed every §16.4 hardening check. |
| Dialect | The resolved schema dialect when [`Ok`](./ToolSchemaValidation/Ok.md) is `true`. |
| Reason | The rejection reason when [`Ok`](./ToolSchemaValidation/Ok.md) is `false`. |

## Public Members

| name | description |
| --- | --- |
| [ToolSchemaValidation](ToolSchemaValidation/ToolSchemaValidation.md)(…) | The discriminated outcome of [`ValidateToolSchema`](./ToolSchemas/ValidateToolSchema.md) (spec §16.4): either the schema is safe to validate against / register (with its resolved dialect), or it is rejected with a human-readable reason. |
| static [Invalid](ToolSchemaValidation/Invalid.md)(…) | Builds a failed validation carrying the rejection *reason*. |
| static [Valid](ToolSchemaValidation/Valid.md)(…) | Builds a successful validation carrying the resolved *dialect*. |
| [Dialect](ToolSchemaValidation/Dialect.md) { get; set; } | The resolved schema dialect when [`Ok`](./ToolSchemaValidation/Ok.md) is `true`. |
| [Ok](ToolSchemaValidation/Ok.md) { get; set; } | Whether the schema passed every §16.4 hardening check. |
| [Reason](ToolSchemaValidation/Reason.md) { get; set; } | The rejection reason when [`Ok`](./ToolSchemaValidation/Ok.md) is `false`. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
