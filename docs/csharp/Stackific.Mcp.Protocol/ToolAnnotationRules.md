# ToolAnnotationRules class

The §16.7 untrusted-annotation surface: applies the spec defaults to absent hint fields and exposes the fail-closed trust gate that guards safety decisions made on annotations from an untrusted server.

```csharp
public static class ToolAnnotationRules
```

## Public Members

| name | description |
| --- | --- |
| static [Defaults](ToolAnnotationRules/Defaults.md) { get; } | The §16.7 default values for the four boolean annotation hints (R-16.7-b – R-16.7-e). |
| static [MayTrustToolAnnotations](ToolAnnotationRules/MayTrustToolAnnotations.md)(…) | The untrusted-annotations rule: a client MUST treat tool annotations as untrusted and MUST NOT make tool-use or safety decisions based on annotations received from a server it does not trust. Returns `true` ONLY when the server is explicitly trusted, so a caller gating a safety decision on annotations fails closed for any untrusted server. (§16.7, R-16.7-f, R-16.7-g) |
| static [Resolve](ToolAnnotationRules/Resolve.md)(…) | Resolves the four boolean [`ToolAnnotations`](./ToolAnnotations.md) hints to concrete values, applying the §16.7 defaults for any absent field: `readOnlyHint ⇒ false`, `destructiveHint ⇒ true`, `idempotentHint ⇒ false`, `openWorldHint ⇒ true`. Note `destructiveHint` and `idempotentHint` are meaningful only when `readOnlyHint` is `false`. (R-16.7-b, R-16.7-c, R-16.7-d, R-16.7-e) |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
