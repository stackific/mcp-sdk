# WellKnownDiscovery.AuthorizationServerUris method

Builds the ordered list of authorization-server metadata well-known URIs to try for an *issuer*, in the exact specified priority order (R-23.3-b, R-23.3-c).

```csharp
public static IReadOnlyList<string> AuthorizationServerUris(string issuer)
```

| parameter | description |
| --- | --- |
| issuer | The authorization server's issuer identifier URL. |

## Return Value

The ordered well-known URIs.

## Exceptions

| exception | condition |
| --- | --- |
| ArgumentException | When *issuer* is not an absolute URI. |

## Remarks

For an issuer WITH a path (e.g. `https://auth.example.com/tenant1`): (1) OAuth AS Metadata, path insertion `/.well-known/oauth-authorization-server/tenant1`; (2) OIDC Discovery, path insertion `/.well-known/openid-configuration/tenant1`; (3) OIDC Discovery, path appending `/tenant1/.well-known/openid-configuration`. For an issuer WITHOUT a path: (1) `/.well-known/oauth-authorization-server`; (2) `/.well-known/openid-configuration`.

## See Also

* class [WellKnownDiscovery](../WellKnownDiscovery.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
