# AuthorizationRedirect class

The §23.7 issuer-validation decision table, state verification, and the end-to-end redirect handler (spec §23.5 Step 3, §23.7).

```csharp
public static class AuthorizationRedirect
```

## Public Members

| name | description |
| --- | --- |
| static [Decision](AuthorizationRedirect/Decision.md)(…) | Applies the §23.7 four-row decision table to determine how to treat the `iss` parameter (R-23.7-d, R-23.7-e, R-23.7-f). A present `iss` is ALWAYS compared, regardless of advertisement (R-23.7-f). |
| static [Process](AuthorizationRedirect/Process.md)(…) | Processes a Step-3 authorization redirect end to end: parses the response, verifies `state`, validates `iss` per §23.7, and only then yields the code for redemption (spec §23.5 Step 3, R-23.5-h, R-23.5-l, R-23.5-m, R-23.7-a, R-23.7-h). |
| static [SafeError](AuthorizationRedirect/SafeError.md)(…) | Returns the displayable error details from an authorization redirect ONLY when `iss` validation succeeds, withholding them on mismatch (R-23.7-h). Returns `null` when there is no error, or when the details must be withheld. |
| static [ValidateIssuer](AuthorizationRedirect/ValidateIssuer.md)(…) | Validates the authorization response's `iss` against the recorded issuer per §23.7 — the check a client MUST perform BEFORE transmitting the authorization code to any token endpoint (R-23.7-a, R-23.7-d – R-23.7-g). When the decision is Compare, the present `iss` is compared by EXACT string match (no normalization; R-23.7-g). |
| static [VerifyState](AuthorizationRedirect/VerifyState.md)(…) | Verifies the redirect `state` against the value sent in Step 1 — the check a client MUST pass before redeeming the code (R-23.5-h, R-23.5-l). When a `state` was sent, the returned `state` MUST be present and equal it (exact match). When none was sent, a returned `state` is ignored. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
