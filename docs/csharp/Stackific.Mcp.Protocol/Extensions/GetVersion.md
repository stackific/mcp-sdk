# Extensions.GetVersion method

Reads an extension's version from the settings object it advertised, making the version discoverable purely through negotiation (spec R-24.6-a, R-24.6-b). The version is taken from the settings' *versionKey* field when it is a string or a finite number (numbers are normalized to their string form). It is NEVER inferred out-of-band — when the extension is not advertised or carries no version, this returns `null`.

```csharp
public static string? GetVersion(JsonObject? extensionsMap, string identifier, 
    string versionKey = "version")
```

| parameter | description |
| --- | --- |
| extensionsMap | A peer's advertised `extensions` map (raw). |
| identifier | The extension whose version to read. |
| versionKey | The settings key carrying the version (default `version`). |

## Return Value

The version string, or `null`.

## See Also

* class [Extensions](../Extensions.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
