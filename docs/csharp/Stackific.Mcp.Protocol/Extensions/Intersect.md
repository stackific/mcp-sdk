# Extensions.Intersect method

Returns the set of extension identifiers ACTIVE for an interaction: those advertised (validly) by BOTH peers — the intersection of the two maps (spec R-6.5-l, R-24.3-d). Each raw map is normalized first, so `null`/malformed entries (R-6.5-j) and unknown one-sided identifiers (R-6.6-d) fall outside the intersection. The result is a sorted (ordinal) array for deterministic output. An empty or absent map on either side yields an empty active set.

```csharp
public static IReadOnlyList<string> Intersect(JsonObject? clientExtensions, 
    JsonObject? serverExtensions)
```

| parameter | description |
| --- | --- |
| clientExtensions | The client's advertised `extensions` map (raw). |
| serverExtensions | The server's advertised `extensions` map (raw). |

## Return Value

The sorted intersection of validly-advertised identifiers.

## See Also

* class [Extensions](../Extensions.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
