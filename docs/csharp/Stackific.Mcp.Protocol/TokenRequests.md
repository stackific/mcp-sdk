# TokenRequests class

Token-request body builders, the encoder, and the Step-2/Step-4 audience invariant (spec §23.5, §23.6, §23.9).

```csharp
public static class TokenRequests
```

## Public Members

| name | description |
| --- | --- |
| static [AssertResourceMatchesStep2](TokenRequests/AssertResourceMatchesStep2.md)(…) | Asserts that a token request's `resource` is byte-identical to the value sent in Step 2 — the audience-binding invariant (R-23.5-p, R-23.9-e). |
| static [BuildAuthorizationCode](TokenRequests/BuildAuthorizationCode.md)(…) | Builds the authorization-code token-request body (Step 4), fixing `grant_type=authorization_code` and carrying the PKCE `code_verifier` plus the REQUIRED `resource` parameter (R-23.5-n – R-23.5-p, R-23.6-b). |
| static [BuildRefresh](TokenRequests/BuildRefresh.md)(…) | Builds the refresh-token token-request body, fixing `grant_type=refresh_token` and carrying the same `resource` parameter so the refreshed token stays audience-bound (R-23.9-e, R-23.9-f). |
| static [EncodeBody](TokenRequests/EncodeBody.md)(…) | Serializes a token request into an `application/x-www-form-urlencoded` body (spec §23.5/§23.9). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
