# DiscoverResult.Validated method

Validates that this result is structurally well-formed per §5.3.2 — in particular that [`SupportedVersions`](./SupportedVersions.md) is non-empty (R-5.3.2-b). A server MUST advertise at least one accepted revision; an empty list is rejected, mirroring the TypeScript `superRefine` in `DiscoverResultSchema` and the `buildDiscoverResult``RangeError` guard.

```csharp
public DiscoverResult Validated()
```

## Return Value

This same instance, to allow fluent use after construction.

## Exceptions

| exception | condition |
| --- | --- |
| ArgumentException | When [`SupportedVersions`](./SupportedVersions.md) is empty. |

## See Also

* record [DiscoverResult](../DiscoverResult.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
