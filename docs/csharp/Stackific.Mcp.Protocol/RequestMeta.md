# RequestMeta record

The protocol-defined per-request `_meta` envelope (spec §4.3) that makes every client request self-describing: the protocol revision, the client identity, and the client's per-request capabilities, plus optional passthrough keys (progress token, trace context, third-party metadata). This is what enables the stateless model of §4.4.

```csharp
public record RequestMeta
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](RequestMeta/Parse.md)(…) | Parses and validates the per-request `_meta` envelope from request `params` (server side). A request that omits any REQUIRED key is malformed and is rejected with `-32602` (Invalid params) per §4.3. |
| [Additional](RequestMeta/Additional.md) { get; set; } | Any additional `_meta` keys beyond the protocol-defined ones — for example `progressToken`, `traceparent`/`tracestate`/`baggage`, or third-party keys. Carried through unchanged so receivers can echo or act on them (§4.2). |
| [ClientCapabilities](RequestMeta/ClientCapabilities.md) { get; set; } | REQUIRED. The client's capabilities for this request (§4.3); empty means none. |
| [ClientInfo](RequestMeta/ClientInfo.md) { get; set; } | REQUIRED. The client software identity (§4.3). |
| [LogLevel](RequestMeta/LogLevel.md) { get; set; } | OPTIONAL, Deprecated. The minimum log severity the server may emit for this request (§4.3/§15.3). |
| [ProtocolVersion](RequestMeta/ProtocolVersion.md) { get; set; } | REQUIRED. The protocol revision this request declares (§5.2). |
| [ToJsonObject](RequestMeta/ToJsonObject.md)() | Builds the wire `_meta` object carrying the protocol-defined keys plus any [`Additional`](./RequestMeta/Additional.md). |
| static [IsValidRevisionFormat](RequestMeta/IsValidRevisionFormat.md)(…) | Returns `true` when *revision* matches the `YYYY-MM-DD` revision-format (§5.1, R-5.2-b). Mirrors the TypeScript `isValidRevisionFormat` / `PROTOCOL_REVISION_FORMAT_RE`. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
