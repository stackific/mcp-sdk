# UiPermissions record

The sandbox permissions a UI resource requests (spec §26.4). Each member's presence requests that capability and its value is an empty object; the host MUST NOT grant a capability that is not requested and MAY decline a requested one (§26.7). Absent members are `null`.

```csharp
public record UiPermissions
```

## Public Members

| name | description |
| --- | --- |
| [UiPermissions](UiPermissions/UiPermissions.md)() | The default constructor. |
| [Camera](UiPermissions/Camera.md) { get; set; } | OPTIONAL. Present (an empty object) to request camera access (§26.4). |
| [ClipboardWrite](UiPermissions/ClipboardWrite.md) { get; set; } | OPTIONAL. Present (an empty object) to request clipboard-write access (§26.4). |
| [Geolocation](UiPermissions/Geolocation.md) { get; set; } | OPTIONAL. Present (an empty object) to request geolocation access (§26.4). |
| [Microphone](UiPermissions/Microphone.md) { get; set; } | OPTIONAL. Present (an empty object) to request microphone access (§26.4). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
