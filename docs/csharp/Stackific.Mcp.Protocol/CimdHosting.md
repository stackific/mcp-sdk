# CimdHosting class

Client ID Metadata Document client/AS-side hosting predicates: the `https`-with-path `client_id` URL rule, the preference path, and `private_key_jwt` support (spec §23.12, R-23.12-a – R-23.12-f).

```csharp
public static class CimdHosting
```

## Public Members

| name | description |
| --- | --- |
| const [PrivateKeyJwtAuthMethod](CimdHosting/PrivateKeyJwtAuthMethod.md) | The `private_key_jwt` token-endpoint authentication method a CIMD client MAY use (R-23.12-f). |
| static [IsHostingValid](CimdHosting/IsHostingValid.md)(…) | Returns `true` when *clientIdUrl* satisfies the CIMD client-side hosting rules: hosted at an `https` URL with a path component (R-23.12-b, R-23.12-c). Delegates to S36's [`IsValidClientIdUrl`](./Cimd/IsValidClientIdUrl.md); surfaced here under the §23.12 atom. |
| static [IsPreferredPath](CimdHosting/IsPreferredPath.md)(…) | Returns `true` when both a client and an authorization server should prefer CIMD as the registration path — both SHOULD support the mechanism (R-23.12-a). |
| static [SupportsPrivateKeyJwt](CimdHosting/SupportsPrivateKeyJwt.md)(…) | Returns `true` when a CIMD client MAY authenticate to the token endpoint with `private_key_jwt`: the document declares that method and conveys an appropriate `jwks`/`jwks_uri` (R-23.12-f). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
