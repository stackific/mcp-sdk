# ContentBlockTypes class

The set of `type` discriminators from the deprecated sampling capability that MUST NOT appear where a [`ContentBlock`](./ContentBlock.md) is expected (R-14.8-a, R-14.8-b).

```csharp
public static class ContentBlockTypes
```

## Public Members

| name | description |
| --- | --- |
| static [Forbidden](ContentBlockTypes/Forbidden.md) { get; } | The forbidden sampling content discriminators: `tool_use` and `tool_result`. |
| static [Known](ContentBlockTypes/Known.md) { get; } | The five known, supported content discriminators, in wire order. |
| static [IsForbidden](ContentBlockTypes/IsForbidden.md)(…) | Returns `true` when *type* is a forbidden sampling discriminator (R-14.8-a/b). |
| static [IsKnown](ContentBlockTypes/IsKnown.md)(…) | Returns `true` when *type* is a known, supported content discriminator. A receiver SHOULD treat unknown types as unsupported content, not as errors (R-14.4-b). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
