# CanonicalResourceIdentifier class

Canonical-resource-identifier construction, validation, and comparison helpers (spec §23.1, R-23.1-m – R-23.1-s): the absolute `https` (or loopback `http`) URI, with no fragment, in lowercase canonical form, that identifies an MCP server as a token audience.

```csharp
public static class CanonicalResourceIdentifier
```

## Public Members

| name | description |
| --- | --- |
| static [Equal](CanonicalResourceIdentifier/Equal.md)(…) | Compares *a* and *b* as canonical resource identifiers, accepting an uppercase scheme/host on either side (R-23.1-p). Path, query, and port are compared case-sensitively; only scheme and host are case-insensitive. Returns `false` when either side is not a valid identifier. |
| static [IsLoopbackHost](CanonicalResourceIdentifier/IsLoopbackHost.md)(…) | Returns `true` when *host* denotes loopback / local development, for which the `http` scheme is permitted on a canonical resource identifier (R-23.1-n). |
| static [IsValid](CanonicalResourceIdentifier/IsValid.md)(…) | Returns `true` when *endpointUrl* is a valid canonical resource identifier (R-23.1-m – R-23.1-o). |
| static [StripDefaultTrailingSlash](CanonicalResourceIdentifier/StripDefaultTrailingSlash.md)(…) | Returns *uri* with a single trailing slash removed when the slash is not semantically significant (R-23.1-s). A path of just `"/"` (the bare-host root) is left intact. |
| static [TryCanonicalize](CanonicalResourceIdentifier/TryCanonicalize.md)(…) | Validates and canonicalizes an MCP server endpoint URL into its canonical resource identifier (spec §23.1, R-23.1-m – R-23.1-s), writing the lowercase-scheme/host, fragment-free form to *canonical* on success. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
