# Ui.BuildUiResourceReadResult method

Builds the result object a server returns from `resources/read` for a UI resource: a complete, cacheable result carrying the single UI `contents` entry, with the REQUIRED `ttlMs` / `cacheScope` cache fields (spec §26.4). Mirrors the S27 `ReadResourceResult` shape used in the §26.4 wire example.

```csharp
public static JsonObject BuildUiResourceReadResult(JsonObject contents, long ttlMs, 
    string cacheScope)
```

| parameter | description |
| --- | --- |
| contents | The UI resource `contents` entry (e.g. from [`BuildUiResourceContents`](./BuildUiResourceContents.md)). |
| ttlMs | The cache TTL; MUST be a non-negative integer. |
| cacheScope | The cache scope (`"public"` or `"private"`). |

## Return Value

The read result as a JSON object.

## Exceptions

| exception | condition |
| --- | --- |
| ArgumentOutOfRangeException | When *ttlMs* is negative. |

## See Also

* class [Ui](../Ui.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
