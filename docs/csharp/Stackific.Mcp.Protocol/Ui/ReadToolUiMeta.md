# Ui.ReadToolUiMeta method

Reads a tool's UI declaration ONLY when the extension is active for the interaction; returns `null` when the extension is not active, modeling "a receiver that does not negotiate this extension MUST ignore the `_meta.ui` key" (spec §26.3, R-26.3-g, R-26.2-i). When inactive the tool is treated as a normal tool and the key is ignored — its presence MUST NOT change the behavior of an ordinary `tools/call` (R-26.3-h).

```csharp
public static ToolUiMeta? ReadToolUiMeta(JsonNode? tool, IEnumerable<string> activeSet)
```

| parameter | description |
| --- | --- |
| tool | The tool object (raw). |
| activeSet | Identifiers active for this interaction. |

## Return Value

The declaration when active, or `null`.

## See Also

* record [ToolUiMeta](../ToolUiMeta.md)
* class [Ui](../Ui.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
