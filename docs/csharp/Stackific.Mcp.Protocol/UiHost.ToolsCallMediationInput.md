# UiHost.ToolsCallMediationInput structure

The host's per-request mediation policy inputs for a UI-initiated `tools/call` (spec §26.5.3, §26.7).

```csharp
public struct ToolsCallMediationInput : IEquatable<ToolsCallMediationInput>
```

| parameter | description |
| --- | --- |
| UiMeta | The tool's UI declaration (S41 `_meta.ui`), or `null` if it has none. |
| UserConsented | Whether the user has granted consent for this invocation (R-26.7-j). |
| PolicyAllows | Whether the host's tool-execution policy permits this invocation (R-26.7-j). |

## Public Members

| name | description |
| --- | --- |
| [ToolsCallMediationInput](UiHost.ToolsCallMediationInput/ToolsCallMediationInput.md)(…) | The host's per-request mediation policy inputs for a UI-initiated `tools/call` (spec §26.5.3, §26.7). |
| [PolicyAllows](UiHost.ToolsCallMediationInput/PolicyAllows.md) { get; set; } | Whether the host's tool-execution policy permits this invocation (R-26.7-j). |
| [UiMeta](UiHost.ToolsCallMediationInput/UiMeta.md) { get; set; } | The tool's UI declaration (S41 `_meta.ui`), or `null` if it has none. |
| [UserConsented](UiHost.ToolsCallMediationInput/UserConsented.md) { get; set; } | Whether the user has granted consent for this invocation (R-26.7-j). |

## See Also

* class [UiHost](./UiHost.md)
* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
