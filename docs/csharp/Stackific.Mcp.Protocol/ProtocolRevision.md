# ProtocolRevision class

The MCP protocol revision implemented by this SDK (spec §5.1). Revision identifiers are opaque, exactly-matched strings — never compared lexically or chronologically (§5.1).

```csharp
public static class ProtocolRevision
```

## Public Members

| name | description |
| --- | --- |
| static [Supported](ProtocolRevision/Supported.md) { get; } | The revisions this SDK is willing to accept, most-preferred first. A server advertises this list as `supportedVersions` in discovery (§5.3.2); a client selects from it (§5.4). |
| const [Current](ProtocolRevision/Current.md) | The wire value of the revision this SDK speaks: `2026-07-28`. |
| static [IsSupported](ProtocolRevision/IsSupported.md)(…) | Returns `true` if *revision* is one this SDK supports (exact match, §5.1). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
