# ClientIdAcquisition class

`client_id` mechanism selection (the static priority order) and pre-registration credential checking (spec §23.4, R-23.4-a – R-23.4-c).

```csharp
public static class ClientIdAcquisition
```

## Public Members

| name | description |
| --- | --- |
| static [Priority](ClientIdAcquisition/Priority.md) { get; } | The SHOULD priority order for selecting a `client_id` mechanism: pre-registration → CIMD → DCR → user prompt (R-23.4-b). |
| static [ApplicationTypeFor](ClientIdAcquisition/ApplicationTypeFor.md)(…) | Returns the `application_type` a client SHOULD register based on whether it runs as a native (desktop/mobile/CLI/localhost) or a remote browser-based app (R-23.4-n, R-23.4-o). |
| static [CheckPreRegisteredCredentials](ClientIdAcquisition/CheckPreRegisteredCredentials.md)(…) | Verifies that pre-registered credentials' authorization server matches the one indicated by protected-resource metadata, by exact string match, surfacing an error on mismatch rather than silently using mismatched credentials (R-23.4-c). |
| static [Select](ClientIdAcquisition/Select.md)(…) | Selects the `client_id` mechanism to use from those a client supports, applying the priority order (R-23.4-a, R-23.4-b). Returns the highest-priority supported mechanism, or Prompt when *supported* is empty. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
