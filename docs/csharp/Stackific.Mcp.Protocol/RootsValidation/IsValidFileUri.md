# RootsValidation.IsValidFileUri method

Returns `true` when *uri* is a syntactically valid URI per RFC 3986 AND uses the `file` scheme (begins with `file://`). A non-`file` scheme, an empty value, or a malformed URI all return `false`. Mirrors TS `isValidFileUri`. (R-21.1.5-b, R-21.1.5-d; AC-32.11)

```csharp
public static bool IsValidFileUri(string? uri)
```

| parameter | description |
| --- | --- |
| uri | The candidate URI. |

## Return Value

`true` when the value is a valid `file://` URI.

## Remarks

The check mirrors the TypeScript WHATWG `URL` path: the value MUST literally begin with `file://` (scheme + authority marker), then parse as an absolute URI whose scheme is `file`. Uri@) with Absolute is RFC 3986-compatible and rejects malformed inputs (for example an unescaped space) just as the WHATWG parser does.

## See Also

* class [RootsValidation](../RootsValidation.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
