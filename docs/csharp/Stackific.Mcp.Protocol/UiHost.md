# UiHost class

S42 — Interactive UI Extension II: the UI-to-host message dialect, its verbatim method/notification registry, the handshake ordering, message validation, the §26.8 error contract, the host mediation/consent gates, sandbox reporting, and the data-exposure guard (spec §26.5–§26.9). The C# counterpart of the TypeScript `protocol/ui-host.ts` module.

```csharp
public static class UiHost
```

## Public Members

| name | description |
| --- | --- |
| static [AllowedUiExposureKeys](UiHost/AllowedUiExposureKeys.md) { get; } | The ONLY data categories a host MAY make available to the rendered UI: the tool input and result it was rendered for, and host context explicitly delivered through the dialect (spec §26.7, R-26.7-m). |
| static [DeclinableUiRequests](UiHost/DeclinableUiRequests.md) { get; } | The set of UI-initiated requests that a host, when it declines them (for lack of consent, policy, or an unknown method), MUST answer with a §22 error rather than silently dropping (spec §26.8, R-26.8-b). |
| static [DialectRegistry](UiHost/DialectRegistry.md) { get; } | The complete §26.6 registry, in spec order: all 19 distinct names with their kind and direction. The host validates a dialect message's `method` against this table byte-for-byte (spec §26.6, R-26.5-a). |
| static [DisplayModes](UiHost/DisplayModes.md) { get; } | The three display modes a UI may run in / request, in spec order (spec §26.5.1, §26.5.3). |
| static [ForbiddenUiExposureKeys](UiHost/ForbiddenUiExposureKeys.md) { get; } | The keys a host MUST NOT expose to the UI: credentials, authorization tokens (§23), and unrelated conversation/context data (spec §26.7, R-26.7-m). This list is illustrative of the categories a host must withhold; the authoritative rule is the inclusion test [`UiExposureIsClean`](./UiHost/UiExposureIsClean.md), which keys off the allow-list rather than this deny-list. |
| static [HostOnlyConcerns](UiHost/HostOnlyConcerns.md) { get; } | The host/client-only concerns that are NOT obligations of a server SDK (spec §26.9, R-26.9-d). |
| static [SandboxDeniedAccess](UiHost/SandboxDeniedAccess.md) { get; } | The access categories a sandboxed UI MUST be denied: the embedding document's DOM, cookies, storage, and navigation (spec §26.7, R-26.7-a, R-26.7-b). A host renders the UI in an isolated browsing context that blocks every one of these. |
| static [ServerSdkObligations](UiHost/ServerSdkObligations.md) { get; } | The server-side obligations of this extension; a server-side implementation MUST support all three (spec §26.9, R-26.9-a..c). |
| const [DialectChannelPath](UiHost/DialectChannelPath.md) | The single permitted path between UI and host: the §26.5 dialect channel (spec R-26.7-c). |
| const [DialectProtocolVersion](UiHost/DialectProtocolVersion.md) | The exact, case-sensitive protocol-version string carried in this dialect's initialization handshake (spec §26.5, R-26.5-b). It identifies the MESSAGE-DIALECT revision and is INDEPENDENT of the core protocol revision negotiated at `server/discover`; conflating them is a conformance error. |
| static [BuildDeclineErrorResponse](UiHost/BuildDeclineErrorResponse.md)(…) | Builds the §22 error response a host returns when it DECLINES a UI-initiated request, instead of silently dropping it (spec §26.8, R-26.8-b). The code is selected from *reason* by [`DeclineErrorCode`](./UiHost/DeclineErrorCode.md). |
| static [BuildDialectErrorResponse](UiHost/BuildDialectErrorResponse.md)(…) | Builds a JSON-RPC error response for a failed dialect request, per §3 and §22 (spec §26.8, R-26.8-a). Reuses the foundation's [`JsonRpcError`](../Stackific.Mcp.JsonRpc/JsonRpcError.md) / [`JsonRpcErrorResponse`](../Stackific.Mcp.JsonRpc/JsonRpcErrorResponse.md) so the `error` shape is the single authoritative one. |
| static [BuildDisplayModeResult](UiHost/BuildDisplayModeResult.md)(…) | Applies a `ui/request-display-mode` request: the host MAY grant a mode different from the one requested, and the result reports the mode actually applied (spec §26.5.3, R-26.5.3-e). Returns the result params object carrying the applied mode. |
| static [BuildPingResponse](UiHost/BuildPingResponse.md)(…) | Builds the prompt success response to a `ping`: an empty result `{}` (spec §26.5.3, R-26.5.3-f, R-26.5.3-g). The receiver MUST respond promptly so the sender can confirm the peer is live. |
| static [BuildSandboxReport](UiHost/BuildSandboxReport.md)(…) | Builds the `hostCapabilities.sandbox` report for the initialize result as a JSON object: the EFFECTIVE CSP the host applied and the GRANTED permission set (spec §26.7, R-26.7-g, R-26.7-h). The CSP is resolved via [`ResolveCsp`](./Ui/ResolveCsp.md) (declared `csp`, else deny-by-default), and the permissions via [`GrantedPermissions`](./UiHost/GrantedPermissions.md). |
| static [BuildTeardownResponse](UiHost/BuildTeardownResponse.md)(…) | Builds the empty `{}` success response a UI returns to a `ui/resource-teardown` request after releasing its resources (spec §26.5.4, R-26.5.4-a). |
| static [CheckHandshakeOrder](UiHost/CheckHandshakeOrder.md)(…) | Conformance check for the handshake-ordering rule (spec §26.5.1, R-26.5.1-a): given the channel *phase* and the *method* the UI is attempting to send, returns `Ok` when the message is allowed, or a premature-message violation when the UI emits anything other than `ui/initialize` before the init response. |
| static [DeclineErrorCode](UiHost/DeclineErrorCode.md)(…) | Maps a [`DeclineReason`](./UiHost.DeclineReason.md) to the §22 error code a host returns when it declines a UI-initiated request (spec §26.8, R-26.8-b): `unknown-method` → `-32601`, `invalid-params` → `-32602`, `no-consent`/`policy` → `-32603`. Whichever reason applies, the host MUST return an error — never a silent drop. |
| static [DedicatedRenderOrigin](UiHost/DedicatedRenderOrigin.md)(…) | Returns the dedicated render origin a UI resource declared via its `domain` UI-metadata field (spec §26.4, R-26.4-l), or `null` when none was declared. A host SHOULD render such a resource under its own isolated origin so it cannot reach the cookies/storage of other UI surfaces. |
| static [DialectIsOnlyChannel](UiHost/DialectIsOnlyChannel.md)(…) | Returns `true` when the §26.5 dialect channel is the ONLY path granted between the rendered UI and the host — i.e. no other ambient path to host or user data exists (spec §26.7, R-26.7-c). |
| static [DialectRegistryEntryFor](UiHost/DialectRegistryEntryFor.md)(…) | Returns the §26.6 registry entry for *name*, or `null` when not a dialect name. |
| static [GrantedPermissions](UiHost/GrantedPermissions.md)(…) | Computes the GRANTED permission set for a UI resource, enforcing R-26.7-h: the host MUST NOT grant any permission the resource did not request, and MAY decline a requested one (spec §26.7, R-26.7-h). The result is exactly what `hostCapabilities.sandbox.permissions` reports. Starts from the resource's requested set, keeps only members the resource requested, and drops any the host chose to decline. |
| static [IsIsolatedUnderDedicatedOrigin](UiHost/IsIsolatedUnderDedicatedOrigin.md)(…) | Returns `true` when a UI is isolated under its declared dedicated origin (spec §26.4, R-26.4-l): either no *declaredDomain* was declared (no isolation constraint applies), or the *renderOrigin* equals the declared domain AND is not shared with any other UI surface in *otherUiOrigins*. Origins are compared byte-for-byte (never case-folded). |
| static [IsServerSdkObligation](UiHost/IsServerSdkObligation.md)(…) | Returns `true` when *concern* is a SERVER-SDK obligation under this extension (one of [`ServerSdkObligations`](./UiHost/ServerSdkObligations.md)); returns `false` for any host-only concern (spec §26.9, R-26.9-d). |
| static [IsUiDialectMethodName](UiHost/IsUiDialectMethodName.md)(…) | Returns `true` when *name* is one of the verbatim dialect method/notification names — matched byte-for-byte and case-sensitively, so `"UI/Initialize"` or `"ui/Initialize"` do NOT match (spec §26.6, R-26.5-a). |
| static [IsUiDialectProtocolVersion](UiHost/IsUiDialectProtocolVersion.md)(…) | Returns `true` when *value* is exactly [`DialectProtocolVersion`](./UiHost/DialectProtocolVersion.md), matched byte-for-byte (spec R-26.5-b). |
| static [IsUiDisplayMode](UiHost/IsUiDisplayMode.md)(…) | Returns `true` when *value* is one of the three defined display modes (spec §26.5.1). |
| static [IsUiInitializeResult](UiHost/IsUiInitializeResult.md)(…) | Returns `true` when *value* is a well-formed `UiInitializeResult` — in particular it carries a string `protocolVersion`. The absence of that field is a conformance failure (spec §26.5.1, R-26.5.1-b). |
| static [MediateOpenLink](UiHost/MediateOpenLink.md)(…) | Decides whether a host may honor a `ui/open-link` request. The host MAY decline and SHOULD confirm with the user before honoring it; a non-confirming auto-open is a conformance failure (spec §26.5.3, §26.7, R-26.5.3-d, R-26.7-l). Returns `Route == true` only when the host both chose to honor the request AND obtained the user's confirmation; otherwise a Policy (host declined) or NoConsent (no confirmation) decline. |
| static [MediateUiMessage](UiHost/MediateUiMessage.md)(…) | Decides whether a host may honor a `ui/message` insertion. The host SHOULD confirm with the user before inserting the message into the conversation (spec §26.7, R-26.7-l). Same gate shape as [`MediateOpenLink`](./UiHost/MediateOpenLink.md). |
| static [MediateUiToolsCall](UiHost/MediateUiToolsCall.md)(…) | Decides whether a host may route a UI-initiated `tools/call` to the server (spec §26.5.3, §26.7, R-26.5.3-a, R-26.5.3-b, R-26.7-i, R-26.7-j, R-26.7-k). |
| static [MethodNotFoundResponse](UiHost/MethodNotFoundResponse.md)(…) | Builds the §22 method-not-found (`-32601`) error response a receiver MUST send when it receives a dialect REQUEST naming a method it does not implement (spec §26.8, R-26.8-c). |
| static [SandboxIsolationIsConforming](UiHost/SandboxIsolationIsConforming.md)(…) | Returns `true` when a proposed sandbox configuration is conforming: it denies EVERY category in [`SandboxDeniedAccess`](./UiHost/SandboxDeniedAccess.md), leaving the §26.5 dialect channel as the only path between the UI and the host (spec §26.7, R-26.7-a, R-26.7-b, R-26.7-c). |
| static [UiExposureIsClean](UiHost/UiExposureIsClean.md)(…) | Returns `true` when the data a host is about to expose to the UI contains ONLY permitted categories — every top-level key is in [`AllowedUiExposureKeys`](./UiHost/AllowedUiExposureKeys.md). Any other key (a credential, token, cookie, or unrelated conversation/context datum) makes the exposure dirty (spec §26.7, R-26.7-m). The check is allow-list based (not merely "no forbidden key present"), so an unforeseen leaking key is caught too. |
| static [UiMayEmitBeforeInitResponse](UiHost/UiMayEmitBeforeInitResponse.md)(…) | Returns `true` when a conforming UI MAY emit a dialect message with *method* BEFORE it has received the `ui/initialize` response. Only `ui/initialize` itself qualifies; every other dialect message — including `ui/notifications/initialized` — MUST wait for the response (spec §26.5.1, R-26.5.1-a). |
| static [ValidateDialectMessage](UiHost/ValidateDialectMessage.md)(…) | Validates an incoming dialect message against the §3 JSON-RPC framing BEFORE a host acts on it, treating the rendered content as untrusted (spec §26.7, R-26.7-n, R-26.7-o). |
| enum [ChannelPhase](UiHost.ChannelPhase.md) | The phases of the dialect channel's lifecycle, from the UI's perspective. |
| enum [DeclineReason](UiHost.DeclineReason.md) | Why a host declined a UI-initiated request, used to pick the §22 error code (spec §26.8). |
| enum [DialectKind](UiHost.DialectKind.md) | Whether a registry entry is a JSON-RPC `request` or a `notification` (spec §26.6). |
| enum [DialectMessageClass](UiHost.DialectMessageClass.md) | The class an incoming dialect message validates into. |
| struct [DialectMessageValidation](UiHost.DialectMessageValidation.md) | Outcome of [`ValidateDialectMessage`](./UiHost/ValidateDialectMessage.md). |
| record [DialectRegistryEntry](UiHost.DialectRegistryEntry.md) | One row of the §26.6 registry: the verbatim name, its kind, and its direction. |
| enum [DialectSender](UiHost.DialectSender.md) | The originator/direction of a dialect message, per the §26.6 "Sender" column. |
| enum [DialectValidationFailure](UiHost.DialectValidationFailure.md) | Why a dialect message failed validation. |
| struct [HandshakeOrderResult](UiHost.HandshakeOrderResult.md) | Outcome of [`CheckHandshakeOrder`](./UiHost/CheckHandshakeOrder.md). |
| struct [MediationDecision](UiHost.MediationDecision.md) | Outcome of a host mediation decision. |
| static class [Methods](UiHost.Methods.md) | The verbatim dialect method/notification names, reproduced case-sensitively (spec §26.6, R-26.5-a). |
| struct [ToolsCallMediationInput](UiHost.ToolsCallMediationInput.md) | The host's per-request mediation policy inputs for a UI-initiated `tools/call` (spec §26.5.3, §26.7). |

## Remarks

The runtime, dynamic half of the apps extension: the JSON-RPC 2.0 dialect a rendered UI (running in its sandbox) speaks with its host over a host-provided channel. The dialect is framed identically to core MCP (§3): every message is a request, response, or notification. It reuses a small subset of core method names verbatim (`tools/call`, `resources/read`, `ping`, `notifications/message`) and adds the `ui/`-prefixed names. Its handshake carries its OWN protocol-version revision ([`DialectProtocolVersion`](./UiHost/DialectProtocolVersion.md), `"2026-01-26"`) — independent of the core revision negotiated at `server/discover`.

As with S41, rendering, sandboxing, CSP/permission enforcement, running the channel runtime, and obtaining user consent are HOST responsibilities and are NOT obligations of a server SDK (R-26.9-d). This module models the dialect declaratively — the registry, the message-shape predicates, and a set of host predicates/builders a host implementation consults — but renders nothing and takes no browser/UI-toolkit dependency. The §26.8 error responses reuse the foundation's [`JsonRpcError`](../Stackific.Mcp.JsonRpc/JsonRpcError.md) / [`JsonRpcErrorResponse`](../Stackific.Mcp.JsonRpc/JsonRpcErrorResponse.md), and message validation reuses the S03 classifier [`FromNode`](../Stackific.Mcp.JsonRpc/JsonRpcMessageSerializer/FromNode.md).

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
