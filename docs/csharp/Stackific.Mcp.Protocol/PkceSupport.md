# PkceSupport class

The §28.5 PKCE-support gate: a client MUST confirm, from authorization-server metadata, that the AS advertises PKCE `S256` before proceeding, refusing otherwise (spec §28.5, R-28.5-k).

```csharp
public static class PkceSupport
```

## Public Members

| name | description |
| --- | --- |
| static [AssertConfirmed](PkceSupport/AssertConfirmed.md)(…) | Asserts PKCE `S256` support is confirmable from AS metadata, throwing [`PkceSupportException`](./PkceSupportException.md) when it is not — so the client refuses to proceed rather than starting a flow against an AS that may not support PKCE (R-28.5-k). |
| static [Confirm](PkceSupport/Confirm.md)(…) | Confirms, from *codeChallengeMethodsSupported*, that the AS supports PKCE with the `S256` method (R-28.5-k). Support is confirmable ONLY when the field is present AND includes `S256`; an absent field means support is unconfirmable (the client MUST refuse). |
| static [IsConfirmed](PkceSupport/IsConfirmed.md)(…) | Returns `true` when AS metadata confirms PKCE `S256` support (R-28.5-k). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
