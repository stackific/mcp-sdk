# Dcr class

Dynamic Client Registration response handling and per-issuer credential persistence (spec §23.4, R-23.4-m – R-23.4-t).

```csharp
public static class Dcr
```

## Public Members

| name | description |
| --- | --- |
| static [HandleResponse](Dcr/HandleResponse.md)(…) | Handles a DCR registration response, surfacing a meaningful error on failure and flagging whether a retry may help, rather than crashing (R-23.4-p, R-23.4-q, R-23.4-r). A success body (valid JSON with a `client_id`) yields success; an HTTP failure status, or a body lacking `client_id`, yields a failure with a human-readable reason. `Retryable` is `true` for a `400` (typically a redirect-URI / application-type constraint). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
