# ElicitationForm.ValidateRestrictedFormSchema method

Validates a form-mode `requestedSchema` against the FULL §20.4 restricted form schema: the outer object shape (`type: "object"`, a `properties` map, optional `required`/`$schema`) PLUS the requirement that every property is a valid `PrimitiveSchemaDefinition` (spec §20.4, R-20.4-a).

```csharp
public static RestrictedFormSchemaValidation ValidateRestrictedFormSchema(JsonNode? value)
```

| parameter | description |
| --- | --- |
| value | The candidate `requestedSchema` object. |

## Return Value

The validation outcome.

## Remarks

This is the §20.4 deepening of the §20.3 structural check, and it owns the full flatness judgement: the primitive union itself excludes nesting — a nested object (`type: "object"`), a generic array-of-objects, a `$ref`, or a composition keyword on a property fails to match any of the four members and is rejected. Crucially it ACCEPTS the enum array forms (`oneOf`/`anyOf`/`items`), the deliberate exceptions §20.4 carves out — they are matched as `EnumSchema` members rather than treated as forbidden nesting. Every `required` entry must name a declared property.

## See Also

* struct [RestrictedFormSchemaValidation](../ElicitationForm.RestrictedFormSchemaValidation.md)
* class [ElicitationForm](../ElicitationForm.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
