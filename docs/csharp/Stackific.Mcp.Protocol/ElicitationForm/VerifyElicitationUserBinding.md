# ElicitationForm.VerifyElicitationUserBinding method

Verifies, for a URL-mode elicitation, that the user who opened the URL is the same user who started the elicitation — the §20.7 cross-user anti-phishing check (spec §20.7, R-20.7-j … R-20.7-o).

```csharp
public static ElicitationUserBindingResult VerifyElicitationUserBinding(string? mcpSessionSubject, 
    string? browserSessionSubject)
```

| parameter | description |
| --- | --- |
| mcpSessionSubject | Authoritative `sub` of the MCP session that started the elicitation. |
| browserSessionSubject | Authoritative `sub` of the browser session that opened the elicitation URL. |

## Return Value

The binding outcome.

## Remarks

The server MUST compare server-side-verified subjects (e.g. the authoritative `sub` of the MCP session vs the `sub` of the browser session that opened the URL), NOT any identity carried in the URL (R-20.7-n, R-20.7-o); both inputs here are expected to be authoritative subjects the caller resolved through its authorization server. A missing/empty subject yields UnverifiedIdentity (R-20.7-k); differing subjects yield SubjectMismatch (R-20.7-m).

## See Also

* struct [ElicitationUserBindingResult](../ElicitationForm.ElicitationUserBindingResult.md)
* class [ElicitationForm](../ElicitationForm.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
