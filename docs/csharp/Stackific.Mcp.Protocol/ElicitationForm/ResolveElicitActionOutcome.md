# ElicitationForm.ResolveElicitActionOutcome method

Maps a returned `ElicitResult` to the server's handling directive, encoding the §20.5 rule that a server MUST NOT assume success and MUST handle decline, cancel, and a client failure to process (spec §20.5, R-20.5-d … R-20.5-h). The returned [`Handle`](../ElicitationForm.ElicitActionOutcome/Handle.md) gives the server an explicit branch for every action — including `malformed` (the client's answer did not conform), which is treated as a failure to process, never as success.

```csharp
public static ElicitActionOutcome ResolveElicitActionOutcome(ElicitResult? result, string mode, 
    JsonNode? requestedSchema = null)
```

| parameter | description |
| --- | --- |
| result | The `ElicitResult` returned by the client. |
| mode | The mode of the originating request. |
| requestedSchema | The form-mode `requestedSchema` (for content checks). |

## Return Value

The handling directive.

## See Also

* struct [ElicitActionOutcome](../ElicitationForm.ElicitActionOutcome.md)
* record [ElicitResult](../ElicitResult.md)
* class [ElicitationForm](../ElicitationForm.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
