# ProtocolSupportCache class

Caches the protocol-support determination per server endpoint (§5.7, R-5.7-e). The determination is a property of the endpoint, not of an individual request, so a client SHOULD cache it for the lifetime of the connected server; it MAY persist it across restarts of the same server configuration (via [`Entries`](./ProtocolSupportCache/Entries.md) / [`FromEntries`](./ProtocolSupportCache/FromEntries.md)) and re-probe — via [`Invalidate`](./ProtocolSupportCache/Invalidate.md) — if a cached assumption later proves wrong (R-5.7-f). Mirrors TS `ProtocolSupportCache`.

```csharp
public sealed class ProtocolSupportCache
```

## Public Members

| name | description |
| --- | --- |
| [ProtocolSupportCache](ProtocolSupportCache/ProtocolSupportCache.md)() | The default constructor. |
| static [FromEntries](ProtocolSupportCache/FromEntries.md)(…) | Rebuilds a cache from persisted [`Entries`](./ProtocolSupportCache/Entries.md) (R-5.7-f). |
| [Entries](ProtocolSupportCache/Entries.md)() | Snapshot of all cached determinations, for persistence (R-5.7-f). |
| [Get](ProtocolSupportCache/Get.md)(…) | Returns the cached determination for *endpoint*, or `null` when absent. |
| [Has](ProtocolSupportCache/Has.md)(…) | Returns `true` when a determination is cached for *endpoint*. |
| [Invalidate](ProtocolSupportCache/Invalidate.md)(…) | Drops the cached determination so the client re-probes (R-5.7-f). |
| [Set](ProtocolSupportCache/Set.md)(…) | Records a determination for *endpoint*. |

## Remarks

Endpoints are identified by an opaque, caller-chosen key (for example a stdio command line or an HTTP endpoint URL). This type is not thread-safe; guard it externally if shared across threads.

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
