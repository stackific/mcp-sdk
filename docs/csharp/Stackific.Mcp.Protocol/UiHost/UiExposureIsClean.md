# UiHost.UiExposureIsClean method

Returns `true` when the data a host is about to expose to the UI contains ONLY permitted categories — every top-level key is in [`AllowedUiExposureKeys`](./AllowedUiExposureKeys.md). Any other key (a credential, token, cookie, or unrelated conversation/context datum) makes the exposure dirty (spec §26.7, R-26.7-m). The check is allow-list based (not merely "no forbidden key present"), so an unforeseen leaking key is caught too.

```csharp
public static bool UiExposureIsClean(JsonObject exposed)
```

| parameter | description |
| --- | --- |
| exposed | The object a host intends to hand to the UI. |

## Return Value

`true` when the exposure is clean.

## See Also

* class [UiHost](../UiHost.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
