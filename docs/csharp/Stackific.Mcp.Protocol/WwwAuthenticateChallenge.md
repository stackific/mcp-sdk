# WwwAuthenticateChallenge record

The structured fields of a `Bearer``WWW-Authenticate` challenge (spec §23.1) — the parameter set carried in the HTTP response header, not a JSON object.

```csharp
public record WwwAuthenticateChallenge
```

## Public Members

| name | description |
| --- | --- |
| [WwwAuthenticateChallenge](WwwAuthenticateChallenge/WwwAuthenticateChallenge.md)(…) | The structured fields of a `Bearer``WWW-Authenticate` challenge (spec §23.1) — the parameter set carried in the HTTP response header, not a JSON object. |
| [Error](WwwAuthenticateChallenge/Error.md) { get; set; } | The failure code; `insufficient_scope` on a `403` (R-23.1-ab). |
| [ErrorDescription](WwwAuthenticateChallenge/ErrorDescription.md) { get; set; } | OPTIONAL human-readable description of the failure (R-23.1-ad). |
| [ResourceMetadata](WwwAuthenticateChallenge/ResourceMetadata.md) { get; set; } | Absolute URI of the protected-resource metadata document (R-23.1-v). |
| [Scope](WwwAuthenticateChallenge/Scope.md) { get; set; } | Space-delimited scopes required for the operation (R-23.1-w, R-23.1-ab). |

## Remarks

On a `401` (§7.4) [`ResourceMetadata`](./WwwAuthenticateChallenge/ResourceMetadata.md) is REQUIRED and [`Scope`](./WwwAuthenticateChallenge/Scope.md) SHOULD be present; on a `403` insufficient-scope challenge (§7.5) [`Error`](./WwwAuthenticateChallenge/Error.md) is `insufficient_scope` and [`Scope`](./WwwAuthenticateChallenge/Scope.md), [`ResourceMetadata`](./WwwAuthenticateChallenge/ResourceMetadata.md), and an OPTIONAL [`ErrorDescription`](./WwwAuthenticateChallenge/ErrorDescription.md) accompany it.

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
