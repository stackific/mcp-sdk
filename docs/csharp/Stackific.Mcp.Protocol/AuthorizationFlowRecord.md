# AuthorizationFlowRecord record

Client-side bookkeeping captured in Step 1, associated with the `code_verifier` (and `state`, if used), to validate the redirect later (spec §23.5, R-23.5-c).

```csharp
public record AuthorizationFlowRecord
```

## Public Members

| name | description |
| --- | --- |
| [AuthorizationFlowRecord](AuthorizationFlowRecord/AuthorizationFlowRecord.md)(…) | Client-side bookkeeping captured in Step 1, associated with the `code_verifier` (and `state`, if used), to validate the redirect later (spec §23.5, R-23.5-c). |
| static [Create](AuthorizationFlowRecord/Create.md)(…) | Builds the Step-1 per-request record: a fresh PKCE pair (unless supplied), an opaque `state` (unless supplied), and the recorded `issuer` (R-23.5-a, R-23.5-b, R-23.5-c, R-23.5-g). The record MUST be created and the `issuer` recorded BEFORE the user agent is redirected. |
| [CodeChallenge](AuthorizationFlowRecord/CodeChallenge.md) { get; set; } | The `code_challenge` derived from *CodeVerifier* (R-23.5-b). |
| [CodeChallengeMethod](AuthorizationFlowRecord/CodeChallengeMethod.md) { get; set; } | The PKCE method; always `S256` (R-23.5-a). |
| [CodeVerifier](AuthorizationFlowRecord/CodeVerifier.md) { get; set; } | The high-entropy PKCE verifier this record is keyed to (R-23.5-c). |
| [RecordedIssuer](AuthorizationFlowRecord/RecordedIssuer.md) { get; set; } | The `issuer` from the selected AS's validated metadata, recorded BEFORE redirecting for later `iss` comparison (R-23.5-c). |
| [State](AuthorizationFlowRecord/State.md) { get; set; } | The opaque `state` sent, if any (R-23.5-c, R-23.5-g). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
