# DynamicClientRegistrationStore class

A store for persisted DCR credentials, each keyed by the issuing authorization server's `issuer`, that re-registers when the authorization server changes (spec §23.4, R-23.4-s, R-23.4-t).

```csharp
public sealed class DynamicClientRegistrationStore
```

## Public Members

| name | description |
| --- | --- |
| [DynamicClientRegistrationStore](DynamicClientRegistrationStore/DynamicClientRegistrationStore.md)() | The default constructor. |
| [CredentialFor](DynamicClientRegistrationStore/CredentialFor.md)(…) | Returns the persisted credential for *issuer*, or `null` (R-23.4-s). |
| [NeedsRegistration](DynamicClientRegistrationStore/NeedsRegistration.md)(…) | Returns `true` when the client must (re-)register against *issuer* — i.e. no credential is yet persisted for that authorization server. A client MUST re-register when the authorization server changes, which manifests as the new `issuer` having no persisted credential (R-23.4-t). |
| [Save](DynamicClientRegistrationStore/Save.md)(…) | Persists *credential*, keyed by its `issuer` (R-23.4-s). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
