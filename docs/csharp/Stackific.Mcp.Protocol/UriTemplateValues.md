# UriTemplateValues class

A simple, immutable set of variable bindings for [`Expand`](./UriTemplate/Expand.md) (spec §17.4): each variable maps to a single scalar string or a list of strings (for the explode modifier).

```csharp
public sealed class UriTemplateValues
```

## Public Members

| name | description |
| --- | --- |
| static [FromLists](UriTemplateValues/FromLists.md)(…) | Builds bindings from a `name → list` map (each list value participates in explode expansion). |
| static [FromScalars](UriTemplateValues/FromScalars.md)(…) | Builds bindings from a `name → scalar` map. |
| [TryGet](UriTemplateValues/TryGet.md)(…) | Resolves the (possibly multi-valued) binding for *name*. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
