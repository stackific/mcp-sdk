# Security.ContinuationTokenRecord&lt;TState&gt; constructor

A server-side record of continuation state — the §28.6 handling profile for the requestState token.

```csharp
public ContinuationTokenRecord(string Value, string IntegrityTag, TState State, long? ExpiresAtMs, 
    bool Consumed)
```

| parameter | description |
| --- | --- |
| TState | The server-held state type. |
| Value | The opaque token value handed to the client. |
| IntegrityTag | The integrity tag the server uses to detect tampering (R-28.6-a). |
| State | The server-held continuation state the token stands for. |
| ExpiresAtMs | Epoch ms after which the token is expired; `null` ⇒ no time bound (R-28.6-c). |
| Consumed | `true` once the token has been consumed, for single-use replay defense (R-28.6-c). |

## See Also

* record [ContinuationTokenRecord&lt;TState&gt;](../Security.ContinuationTokenRecord-1.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
