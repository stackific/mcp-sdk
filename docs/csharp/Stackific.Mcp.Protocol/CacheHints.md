# CacheHints record

Response-cache hints carried by a cacheable result (spec §13.1): a client-cache time-to-live in milliseconds and the sharing scope. Both are REQUIRED on a cacheable result.

```csharp
public record CacheHints
```

## Public Members

| name | description |
| --- | --- |
| [CacheHints](CacheHints/CacheHints.md)(…) | Response-cache hints carried by a cacheable result (spec §13.1): a client-cache time-to-live in milliseconds and the sharing scope. Both are REQUIRED on a cacheable result. |
| static [None](CacheHints/None.md) { get; } | The conservative "no-cache" hint: a zero TTL (immediately stale) with the privacy-default Private scope (spec §13.1-e, §13.4-b). A server that does not wish to encourage caching MUST still include both fields and SHOULD set `ttlMs` to `0`; when the scope is unknown the conservative default is `private`, never `public`. |
| [CacheScope](CacheHints/CacheScope.md) { get; set; } | The sharing scope of the cached result. |
| [TtlMs](CacheHints/TtlMs.md) { get; set; } | How long the client MAY treat the result as fresh, in milliseconds (minimum 0). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
