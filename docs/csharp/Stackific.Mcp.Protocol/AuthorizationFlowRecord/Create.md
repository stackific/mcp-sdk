# AuthorizationFlowRecord.Create method

Builds the Step-1 per-request record: a fresh PKCE pair (unless supplied), an opaque `state` (unless supplied), and the recorded `issuer` (R-23.5-a, R-23.5-b, R-23.5-c, R-23.5-g). The record MUST be created and the `issuer` recorded BEFORE the user agent is redirected.

```csharp
public static AuthorizationFlowRecord Create(string recordedIssuer, PkceChallenge? pkce = null, 
    string? state = null, Func<int, byte[]>? randomSource = null)
```

| parameter | description |
| --- | --- |
| recordedIssuer | The `issuer` of the selected AS's validated metadata (R-23.5-c). |
| pkce | OPTIONAL pre-generated PKCE pair; one is generated when omitted. |
| state | OPTIONAL `state`; one is generated when omitted. |
| randomSource | OPTIONAL byte source for PKCE/state generation; defaults to the CSPRNG. |

## Return Value

The per-request record.

## See Also

* record [PkceChallenge](../PkceChallenge.md)
* record [AuthorizationFlowRecord](../AuthorizationFlowRecord.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
