# AuthorizationRedirect.VerifyState method

Verifies the redirect `state` against the value sent in Step 1 — the check a client MUST pass before redeeming the code (R-23.5-h, R-23.5-l). When a `state` was sent, the returned `state` MUST be present and equal it (exact match). When none was sent, a returned `state` is ignored.

```csharp
public static AuthorizationResult VerifyState(string? sentState, string? returnedState)
```

| parameter | description |
| --- | --- |
| sentState | The `state` sent in the authorization request, or `null`. |
| returnedState | The `state` echoed on the redirect, or `null`. |

## Return Value

The validation outcome.

## See Also

* struct [AuthorizationResult](../AuthorizationResult.md)
* class [AuthorizationRedirect](../AuthorizationRedirect.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
