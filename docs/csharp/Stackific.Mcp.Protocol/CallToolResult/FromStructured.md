# CallToolResult.FromStructured method

Builds a result for a tool that declares an `outputSchema` (spec §16.5, R-16.5-p): it sets the REQUIRED [`StructuredContent`](./StructuredContent.md) and ALSO provides the SHOULD-level unstructured text fallback — a single text block holding the structured value's JSON serialization — so a client that does not consume `structuredContent` still has a renderable representation.

```csharp
public static CallToolResult FromStructured(JsonNode? structuredContent)
```

| parameter | description |
| --- | --- |
| structuredContent | The structured result value (any JSON type, including `null`). |

## Return Value

The result carrying both the structured value and its serialized text fallback.

## See Also

* record [CallToolResult](../CallToolResult.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
