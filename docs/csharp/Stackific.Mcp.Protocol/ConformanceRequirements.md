# ConformanceRequirements class

S45 — Conformance Requirements &amp; References (spec §29–§30).

```csharp
public static class ConformanceRequirements
```

## Public Members

| name | description |
| --- | --- |
| static [All](ConformanceRequirements/All.md) { get; } | The complete registry of §29/§30 normative requirements, in document order (spec §29.1–§29.9, §30). Each entry mirrors exactly one `[R-… · KEYWORD]` atom; the keyword and level honor the spec verbatim. |
| static [Axes](ConformanceRequirements/Axes.md) { get; } | The three conformance axes, in spec order (§29.1). |
| static [CapabilityObligations](ConformanceRequirements/CapabilityObligations.md) { get; } | The per-capability obligation map of §29.4 (spec R-29.4-b – R-29.4-g): tools → §16, resources → §17 (resources.subscribe additionally → §10), prompts → §18, completions → §19, elicitation → §20 (client). |
| static [CitationStatus](ConformanceRequirements/CitationStatus.md) { get; } | The §30 citation status (spec R-30-a): never load-bearing, always self-contained. |
| static [InputRequestRequiredCapability](ConformanceRequirements/InputRequestRequiredCapability.md) { get; } | The map from an input-request method to the client capability that authorizes a server to place it into an `input_required` result (spec §29.4 item 5, R-29.4-l). |
| static [RequiredClientRequestMetaKeys](ConformanceRequirements/RequiredClientRequestMetaKeys.md) { get; } | The fields a client MUST include in every request's per-request metadata (spec §29.3 item 1, R-29.3-a). Exposed for a conformance harness to assert presence. |
| static [RequirementKeywords](ConformanceRequirements/RequirementKeywords.md) { get; } | Every distinct RFC 2119 keyword recognized in the story's atoms, mapped to its canonical [`Level`](./ConformanceRequirements.Level.md) family (spec §2). The keys are the exact tokens used in the `[R-… · KEYWORD]` markers. |
| static [RequirementLevels](ConformanceRequirements/RequirementLevels.md) { get; } | The three requirement-level families, strongest first (spec §2). |
| static [StatelessInvariants](ConformanceRequirements/StatelessInvariants.md) { get; } | The §29.7 invariants, all `true` (spec R-29.7-a – R-29.7-e). |
| const [StreamableHttpNegotiationErrorStatus](ConformanceRequirements/StreamableHttpNegotiationErrorStatus.md) | The Streamable HTTP status a §29.8 negotiation/envelope error code maps to (spec §29.8 item 3). |
| static [ClassifyRequirementLevel](ConformanceRequirements/ClassifyRequirementLevel.md)(…) | Classifies a normative *keyword* into its [`Level`](./ConformanceRequirements.Level.md) family (spec §2). Returns `null` for an unrecognized token — never throws — so a conformance harness can report rather than crash on a malformed marker. |
| static [ClassifyServerRequest](ConformanceRequirements/ClassifyServerRequest.md)(…) | Applies the ordered §29.2 baseline-server request checks to ONE self-contained §4 request and returns its [`ServerRequestDisposition`](./ConformanceRequirements.ServerRequestDisposition.md) (spec §29.2, R-29.2-e – R-29.2-n, R-29.4-k). The checks run strictly in the §7 flow order — judged on this request's own envelope, NEVER on connection or prior-request state (R-29.1-e, R-29.2-f): |
| static [ClientRequestCarriesBaselineEnvelope](ConformanceRequirements/ClientRequestCarriesBaselineEnvelope.md)(…) | Validates that a client request's metadata carries the three §4-required fields — protocol revision, client identity, and client capabilities — mandated on EVERY request (spec §29.3 item 1, R-29.3-a). A thin wrapper over the shared envelope check. |
| static [DecideRequestStateHandling](ConformanceRequirements/DecideRequestStateHandling.md)(…) | Decides how a server must treat a `requestState` value that passed through a client (spec §29.7 item 4, R-29.7-d, R-29.7-e). It is ALWAYS attacker-controlled input; when it influences authorization, resource access, or business logic the server MUST verify its integrity and reject what fails. |
| static [DecideResultAction](ConformanceRequirements/DecideResultAction.md)(…) | Applies the §29.6 + §3 receiver rules to a result's `resultType` (spec R-29.6-f, R-29.6-g, R-29.6-h). A recognized value (core or an accepted extension value) is acted upon; a present but unaccepted value is treated as an error; an ABSENT or null discriminator resolves to `complete` via the §3 absence rule and is acted upon. |
| static [EvaluateTransportConformance](ConformanceRequirements/EvaluateTransportConformance.md)(…) | Evaluates the authorization-applicability conformance points for a single transport (spec §29.8 items 4 &amp; 5, R-29.8-d, R-29.8-e). An HTTP-based transport SHOULD conform to authorization and conveys credentials as a Bearer token; a stdio transport SHOULD NOT apply it and obtains credentials from its Environment; any other transport applies neither rule (None). |
| static [ForAxis](ConformanceRequirements/ForAxis.md)(…) | Returns every requirement whose axis matches *axis* (spec §29.1). |
| static [ForRole](ConformanceRequirements/ForRole.md)(…) | Returns every requirement that binds *role* (spec §29.1 item 1). A requirement with an empty roles list binds every role; otherwise it binds only the named roles. |
| static [IsAdvisoryKeyword](ConformanceRequirements/IsAdvisoryKeyword.md)(…) | Returns `true` when *keyword* is ADVISORY (SHOULD family) (spec §2). |
| static [IsCitationLoadBearing](ConformanceRequirements/IsCitationLoadBearing.md)(…) | Returns `false` always: no §30 citation marker is ever load-bearing (spec R-30-a). The answer is unconditionally "not load-bearing", independent of which marker is named. |
| static [IsFeatureFullyConformant](ConformanceRequirements/IsFeatureFullyConformant.md)(…) | Enforces "no partial feature conformance": an implementation either fully satisfies the MUST-level behavior of an advertised feature or MUST NOT advertise it (spec §29.9 item 4, R-29.9-b; R-29.4-a, R-29.4-j). An UNadvertised feature that is not implemented is perfectly conformant. |
| static [IsMandatoryKeyword](ConformanceRequirements/IsMandatoryKeyword.md)(…) | Returns `true` when *keyword* is a MANDATORY keyword (MUST family) (spec §2, R-29.1-a). |
| static [IsOptionalKeyword](ConformanceRequirements/IsOptionalKeyword.md)(…) | Returns `true` when *keyword* is OPTIONAL (MAY family) (spec §2, §29.5). |
| static [Lookup](ConformanceRequirements/Lookup.md)(…) | Looks up a requirement by its id (e.g. `R-29.2-h`), or `null`. |
| static [MayPlaceInputRequest](ConformanceRequirements/MayPlaceInputRequest.md)(…) | Returns `true` when a server MAY place an input request of *method* into an `input_required` result for a client declaring *clientCapabilities* (spec §29.4 item 5, R-29.4-l). An unrecognized method is rejected: a server must not solicit a kind it cannot tie to a declared capability. |
| static [ObligationForCapability](ConformanceRequirements/ObligationForCapability.md)(…) | Returns the obligation a party incurs by advertising *capability*, or `null` when the capability carries no enumerated feature-section obligation (spec §29.4). |
| static [ObligedSectionsForCapabilities](ConformanceRequirements/ObligedSectionsForCapabilities.md)(…) | Returns the spec sections whose MUST-level behavior an implementation is bound to, given the capabilities it advertises (spec §29.4 item 1, R-29.4-a – R-29.4-g). The result is deterministic, de-duplicated, numerically sorted, and includes the additional sections. |
| static [ProfileSupportsRevision](ConformanceRequirements/ProfileSupportsRevision.md)(…) | Returns `true` when *revision* is supported as a profile revision: it is the current wire value, or any revision the profile advertises (spec §29.9 item 3). |
| static [RequirementsForProfile](ConformanceRequirements/RequirementsForProfile.md)(…) | Enumerates every normative requirement that APPLIES to a profile: every baseline requirement for the role(s) it plays, plus every transport requirement (spec §29.1, §29.9 item 1). The §29.4 capability-conditioned atoms apply only when the relevant capability is advertised. |
| static [RobustnessDisposition](ConformanceRequirements/RobustnessDisposition.md)(…) | Computes the §29.6 robustness disposition for one received element, given whether the receiver recognizes it (spec §29.6, R-29.6-a – R-29.6-h). A recognized element always returns Accept; robustness applies only to the unrecognized (R-29.6-i). |
| static [SatisfiesRole](ConformanceRequirements/SatisfiesRole.md)(…) | Returns `true` when an implementation satisfying ONLY one role's requirements is conformant for *targetRole* (spec §29.1, R-29.1-a, R-29.1-b). A both-roles implementation must satisfy each role; satisfying only the other role's requirements is non-conformant. |
| static [StreamableHttpStatusForProtocolError](ConformanceRequirements/StreamableHttpStatusForProtocolError.md)(…) | Maps a protocol error *code* to the HTTP status it MUST ride on the Streamable HTTP transport for the §29.8 negotiation/envelope conditions (spec §29.8 item 3, R-29.8-c). `-32602` and `-32003` map to `400`; any other code returns `null` (governed by §9 / S34, not this conformance point). |
| static [ValidateConformanceProfile](ConformanceRequirements/ValidateConformanceProfile.md)(…) | Validates that a [`ConformanceProfile`](./ConformanceRequirements.ConformanceProfile.md) is well-formed against the structural requirements of §29 (spec §29.5 item 2, §29.8 item 1, §29.9 item 3, R-29.1-b, R-29.5-c, R-29.8-a, R-29.9-c). Accumulates ALL violations: at least one recognized role; non-empty revisions including `2026-07-28`; every extension identifier well-formed per §6 (an empty list is conformant); at least one transport. Capabilities are not constrained beyond being a list. |
| static [ValidateInputRequiredRetry](ConformanceRequirements/ValidateInputRequiredRetry.md)(…) | Validates a client's retry request after an `input_required` result (spec §29.3 item 4, R-29.3-j). The retry MUST use a request id distinct from the original, echo `requestState` byte-for-byte when one was provided, and omit it when none was provided. Identity comparison uses [`RequestId`](../Stackific.Mcp.JsonRpc/RequestId.md) value equality, which preserves the JSON type (a numeric and a string id are never coerced); `requestState` comparison is ordinal string equality (the value is opaque and echoed exactly, R-29.3-f). |
| static [ValidateSuccessResultType](ConformanceRequirements/ValidateSuccessResultType.md)(…) | Asserts that a successful result carries a `resultType` discriminator drawn from the core set plus the values of advertised extensions only (spec §29.2 items 7 &amp; 8, R-29.2-k, R-29.2-l). |
| enum [Axis](ConformanceRequirements.Axis.md) | The three independent axes along which conformance is scoped (spec §29.1). Conformance is the product of these: an implementation is conformant iff every applicable requirement on its chosen roles, advertised features, and implemented transports is satisfied. |
| record [CapabilityObligation](ConformanceRequirements.CapabilityObligation.md) | One capability-conditioned obligation: advertising *Capability* binds the advertising *Party* to the MUST-level requirements of *Section* (spec §29.4 item 1, R-29.4-b – R-29.4-g). |
| struct [CitationStatusInfo](ConformanceRequirements.CitationStatusInfo.md) | The status the §30 citation markers carry: provenance only, never load-bearing (spec §30, R-30-a). No normative behavior, code, name, or wire format depends on the content of any citation. |
| record [ConformanceProfile](ConformanceRequirements.ConformanceProfile.md) | The abstract descriptor that fully describes an implementation's conformance: the tuple of roles, advertised revisions, advertised capabilities, advertised extensions, and implemented transports (spec §29.9 item 3, story §6). NOT a wire message — it is used to reason about and report conformance. |
| struct [ConformanceProfileValidation](ConformanceRequirements.ConformanceProfileValidation.md) | Outcome of [`ValidateConformanceProfile`](./ConformanceRequirements/ValidateConformanceProfile.md). |
| struct [ConformanceProfileViolation](ConformanceRequirements.ConformanceProfileViolation.md) | A single way a [`ConformanceProfile`](./ConformanceRequirements.ConformanceProfile.md) fails to be well-formed. |
| record [ConformanceRequirement](ConformanceRequirements.ConformanceRequirement.md) | One normative requirement ("atom") of §29/§30, identified by its stable id, its subsection, the role(s)/axis it binds, and its RFC 2119 level. A conformance harness enumerates these to know exactly what to check. |
| struct [FeatureConformance](ConformanceRequirements.FeatureConformance.md) | Outcome of [`IsFeatureFullyConformant`](./ConformanceRequirements/IsFeatureFullyConformant.md). |
| struct [InputRequiredRetryValidation](ConformanceRequirements.InputRequiredRetryValidation.md) | Outcome of [`ValidateInputRequiredRetry`](./ConformanceRequirements/ValidateInputRequiredRetry.md). |
| enum [Level](ConformanceRequirements.Level.md) | A normative requirement level in the RFC 2119 / RFC 8174 sense (spec §2). The `MUST` family also covers MUST NOT / REQUIRED / SHALL / SHALL NOT; `SHOULD` also covers SHOULD NOT / RECOMMENDED; `MAY` also covers OPTIONAL. |
| struct [RequestStateHandling](ConformanceRequirements.RequestStateHandling.md) | The action a server takes for a `requestState` value (spec §29.7 item 4). |
| enum [RequestStateTrust](ConformanceRequirements.RequestStateTrust.md) | The trust a server may place in a `requestState` value (spec §29.7 item 4). The model admits a single value: a `requestState` that passed through a client is ALWAYS attacker-controlled input (R-29.7-d), so there is no "trusted" disposition. |
| struct [ResultActionDecision](ConformanceRequirements.ResultActionDecision.md) | Outcome of [`DecideResultAction`](./ConformanceRequirements/DecideResultAction.md). |
| enum [ResultTypeFailure](ConformanceRequirements.ResultTypeFailure.md) | The result-type validation outcome for [`ValidateSuccessResultType`](./ConformanceRequirements/ValidateSuccessResultType.md). |
| enum [RetryFailure](ConformanceRequirements.RetryFailure.md) | The result of validating an `input_required` retry request (spec §29.3 item 4). |
| enum [RobustnessDispositionKind](ConformanceRequirements.RobustnessDispositionKind.md) | How a conformant receiver disposes of an element of a received message under §29.6 (spec §29.6). |
| enum [RobustnessElement](ConformanceRequirements.RobustnessElement.md) | The kind of received element being disposed of under §29.6. |
| enum [Role](ConformanceRequirements.Role.md) | A role an implementation plays (spec §29.1, R-29.1-a, R-29.1-b). |
| record [ServerRequestDisposition](ConformanceRequirements.ServerRequestDisposition.md) | The disposition a conformant server reaches for an incoming request after the ordered §29.2 checks (spec §29.2). On a revision failure, [`Code`](./ConformanceRequirements.ServerRequestDisposition/Code.md) is `-32004` and [`Supported`](./ConformanceRequirements.ServerRequestDisposition/Supported.md) / [`Requested`](./ConformanceRequirements.ServerRequestDisposition/Requested.md) are populated; on an envelope failure, [`Code`](./ConformanceRequirements.ServerRequestDisposition/Code.md) is `-32602` with a [`Message`](./ConformanceRequirements.ServerRequestDisposition/Message.md); on a capability failure, [`Code`](./ConformanceRequirements.ServerRequestDisposition/Code.md) is `-32003` with [`RequiredCapabilities`](./ConformanceRequirements.ServerRequestDisposition/RequiredCapabilities.md); on a gating refusal, none of these are set. |
| enum [ServerRequestStage](ConformanceRequirements.ServerRequestStage.md) | The stage at which a baseline-server request is rejected, or acceptance (spec §29.2). |
| record [StatelessConformanceInvariants](ConformanceRequirements.StatelessConformanceInvariants.md) | The stateless-model invariants that bind every role (spec §29.7, R-29.7-a – R-29.7-e). Each is unconditionally `true`; exposed as a flat checklist a conformance harness can assert against. |
| struct [SuccessResultTypeValidation](ConformanceRequirements.SuccessResultTypeValidation.md) | Outcome of [`ValidateSuccessResultType`](./ConformanceRequirements/ValidateSuccessResultType.md). |
| record [TransportConformance](ConformanceRequirements.TransportConformance.md) | The conformance evaluation of a SINGLE transport an implementation offers (spec §29.8). |
| enum [TransportCredentialConveyance](ConformanceRequirements.TransportCredentialConveyance.md) | How credentials are conveyed for a transport under the §29.8 conformance points (spec §29.8 items 4 &amp; 5). Distinct from the §23 [`CredentialConveyance`](./CredentialConveyance.md) in that its third case is None (neither §29.8-d nor §29.8-e applies), not a best-practice mechanism. |

## Remarks

The formal conformance contract: the precise, testable definition of what it means for an MCP implementation to be conformant, decomposed along the three independent axes of §29.1 — role (client/server/both), feature surface (baseline plus advertised), and transport (each implemented transport, independently). It restates, as a single machine-checkable rulebook, the baseline obligations of every server and client (§29.2, §29.3), the bidirectional advertise⇔implement principle (§29.4, §29.5), the robustness rules for richer-than-understood inputs (§29.6), the stateless-model invariants (§29.7), the transport obligations (§29.8), the conformance method (§29.9), and the provenance-only status of the §30 reference markers.

This defines NO new wire types. Its artifacts are the requirement registry ([`All`](./ConformanceRequirements/All.md)), the RFC 2119 keyword classifier ([`ClassifyRequirementLevel`](./ConformanceRequirements/ClassifyRequirementLevel.md)), the [`ConformanceProfile`](./ConformanceRequirements.ConformanceProfile.md) descriptor and its validator, the baseline server request-disposition predicate ([`ClassifyServerRequest`](./ConformanceRequirements/ClassifyServerRequest.md)), the capability→obligation map, the robustness disposition, the stateless invariants, the transport-conformance evaluator, and the §30 citation status.

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
