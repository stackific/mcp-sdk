# Cimd.Validate method

Validates a fetched CIMD document against the URL it was fetched from — the fetch/validate duties an authorization server performs on encountering a URL-formatted `client_id` (R-23.4-i, R-23.4-j, R-23.4-k). On success *document* is populated.

```csharp
public static AuthorizationResult Validate(string documentUrl, JsonNode? value, 
    out ClientIdMetadataDocument? document, string? presentedRedirectUri = null)
```

| parameter | description |
| --- | --- |
| documentUrl | The URL the document was fetched from (== `client_id`). |
| value | The raw fetched document body. |
| document | On success, the validated document; otherwise `null`. |
| presentedRedirectUri | OPTIONAL redirect URI to validate against `redirect_uris` (R-23.4-j). |

## Return Value

The validation outcome.

## Remarks

Checks, in order: the `client_id` URL is a valid HTTPS URL with a path (R-23.4-e); the body is valid JSON with the REQUIRED fields (R-23.4-k); the document's `client_id` exactly equals the fetch URL (R-23.4-i); and, when a *presentedRedirectUri* is supplied, it appears in the document's `redirect_uris` (R-23.4-j).

## See Also

* struct [AuthorizationResult](../AuthorizationResult.md)
* record [ClientIdMetadataDocument](../ClientIdMetadataDocument.md)
* class [Cimd](../Cimd.md)
* namespace [Stackific.Mcp.Protocol](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
