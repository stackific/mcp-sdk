# ResolvedToolAnnotationHints structure

The four boolean [`ToolAnnotations`](./ToolAnnotations.md) hints resolved to concrete values (spec §16.7).

```csharp
public struct ResolvedToolAnnotationHints : IEquatable<ResolvedToolAnnotationHints>
```

| parameter | description |
| --- | --- |
| ReadOnlyHint | Whether the tool does not modify its environment (default `false`, R-16.7-b). |
| DestructiveHint | Whether destructive updates are possible (default `true`; meaningful only when not read-only, R-16.7-c). |
| IdempotentHint | Whether repeated same-arg calls have no extra effect (default `false`; meaningful only when not read-only, R-16.7-d). |
| OpenWorldHint | Whether the tool may interact with an open world (default `true`, R-16.7-e). |

## Public Members

| name | description |
| --- | --- |
| [ResolvedToolAnnotationHints](ResolvedToolAnnotationHints/ResolvedToolAnnotationHints.md)(…) | The four boolean [`ToolAnnotations`](./ToolAnnotations.md) hints resolved to concrete values (spec §16.7). |
| [DestructiveHint](ResolvedToolAnnotationHints/DestructiveHint.md) { get; set; } | Whether destructive updates are possible (default `true`; meaningful only when not read-only, R-16.7-c). |
| [IdempotentHint](ResolvedToolAnnotationHints/IdempotentHint.md) { get; set; } | Whether repeated same-arg calls have no extra effect (default `false`; meaningful only when not read-only, R-16.7-d). |
| [OpenWorldHint](ResolvedToolAnnotationHints/OpenWorldHint.md) { get; set; } | Whether the tool may interact with an open world (default `true`, R-16.7-e). |
| [ReadOnlyHint](ResolvedToolAnnotationHints/ReadOnlyHint.md) { get; set; } | Whether the tool does not modify its environment (default `false`, R-16.7-b). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
