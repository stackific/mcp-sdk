# DiscoveryRobustness class

Discovery-robustness wrappers over S35's well-known ordering, plus the `authorization_servers` requirement and per-AS issuer-identity validation (spec §23.17, R-23.17-a – R-23.17-i).

```csharp
public static class DiscoveryRobustness
```

## Public Members

| name | description |
| --- | --- |
| static [AuthorizationServerMetadataUris](DiscoveryRobustness/AuthorizationServerMetadataUris.md)(…) | Returns the ordered authorization-server-metadata well-known URIs to try for *issuer*, covering both OAuth 2.0 AS Metadata and OpenID Connect Discovery, for issuers with and without a path component, in the mandated priority order (R-23.17-e, R-23.17-f, R-23.17-g). A pass-through over S35's [`AuthorizationServerUris`](./WellKnownDiscovery/AuthorizationServerUris.md). |
| static [ProtectedResourceMetadataUris](DiscoveryRobustness/ProtectedResourceMetadataUris.md)(…) | Resolves the ordered protected-resource-metadata URIs to try, honouring the `WWW-Authenticate``resource_metadata` precedence (R-23.17-a, R-23.17-b): the `401`'s `resource_metadata` URL MUST be used when present; otherwise the well-known URIs (path-prefixed first, then host root). |
| static [RequireAuthorizationServers](DiscoveryRobustness/RequireAuthorizationServers.md)(…) | Validates that protected-resource metadata carries the REQUIRED `authorization_servers` array of one or more issuer identifiers (R-23.17-c). On success *authorizationServers* is populated. When more than one is listed, each is independent and the client maintains separate registration state per AS (R-23.17-d, via [`IssuerBoundCredentialStore`](./IssuerBoundCredentialStore.md)). |
| static [ValidateDiscoveredIssuer](DiscoveryRobustness/ValidateDiscoveredIssuer.md)(…) | Validates that a fetched authorization-server metadata document's `issuer` is IDENTICAL to the issuer used to construct the well-known URL; if it differs the document MUST NOT be used (R-23.17-h, R-23.17-i). Exact string comparison — the same mix-up defence as [`Validate`](./AuthorizationServerMetadata/Validate.md), surfaced for callers that have already structurally validated the document. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
