# ReadResourceResult record

The cacheable result of `resources/read` (spec §17.5).

```csharp
public record ReadResourceResult
```

## Public Members

| name | description |
| --- | --- |
| [CacheScope](ReadResourceResult/CacheScope.md) { get; set; } | The cache sharing scope (§13); REQUIRED on emit, tolerated absent on receipt. |
| [Contents](ReadResourceResult/Contents.md) { get; set; } | REQUIRED. One or more content entries (text or blob variant); never empty for an existing resource. |
| [ResultType](ReadResourceResult/ResultType.md) { get; set; } | REQUIRED on emit (§3.6, §17.5): the base result-type discriminator, `"complete"` for a completed read. Defaults to `"complete"`, which also makes an inbound result that omits `resultType` degrade to `complete` per the §3.6 receiver rule (R-17.5-q). A read reply MAY instead carry `"input_required"` (§11); discriminate with [`DiscriminateReadResourceResponse`](./Resources/DiscriminateReadResourceResponse.md) before binding to this record. |
| [TtlMs](ReadResourceResult/TtlMs.md) { get; set; } | The cache time-to-live hint in milliseconds (§13); REQUIRED on emit, tolerated absent on receipt. |
| [Validated](ReadResourceResult/Validated.md)() | Asserts the §17.5/§13 emit invariant — [`ResultType`](./ReadResourceResult/ResultType.md) is `"complete"` and both caching hints are present and well-formed — returning this same instance. Apply before sending. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
