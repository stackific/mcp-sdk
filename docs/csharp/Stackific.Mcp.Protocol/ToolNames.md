# ToolNames class

Tool name-convention and display-name helpers (spec §16.3).

```csharp
public static class ToolNames
```

## Public Members

| name | description |
| --- | --- |
| const [MaxLength](ToolNames/MaxLength.md) | The inclusive upper bound recommended for a tool `name` length (R-16.3-b). |
| const [MinLength](ToolNames/MinLength.md) | The inclusive lower bound recommended for a tool `name` length (R-16.3-b). |
| static [Disambiguate](ToolNames/Disambiguate.md)(…) | Applies a disambiguation strategy for an aggregated tool name by prefixing the server identifier (for example `server.tool`). A client/proxy that hits a name collision SHOULD apply such a strategy. (R-16.3-h) |
| static [DisplayName](ToolNames/DisplayName.md)(…) | Resolves the display name to show for a tool, applying the §16.3 precedence: `title` → `annotations.title` → `name`. (R-16.3-i) |
| static [FindDuplicates](ToolNames/FindDuplicates.md)(…) | Returns the names that occur more than once across *tools*. Tool names SHOULD be unique within a single server; an aggregating client/proxy MAY encounter collisions. (R-16.3-f, R-16.3-g) |
| static [IsConventional](ToolNames/IsConventional.md)(…) | Returns `true` when a tool `name` follows the recommended conventions: 1–128 characters, only `A–Z a–z 0–9 _ - .` (so no spaces/commas/other special characters). Names SHOULD be treated case-sensitively. (§16.3, R-16.3-b, R-16.3-c, R-16.3-d, R-16.3-e) |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
