# IssuerBoundCredentials constructor

Persisted client credentials bound to the issuing authorization server (spec §23.16, R-23.16-a).

```csharp
public IssuerBoundCredentials(string Issuer, string ClientId, string? ClientSecret = null, 
    bool Cimd = false)
```

| parameter | description |
| --- | --- |
| Issuer | The issuing authorization server's `issuer` identifier; the storage key (R-23.16-b). |
| ClientId | The `client_id` issued by (or pre-registered with) that authorization server. |
| ClientSecret | OPTIONAL `client_secret` for confidential clients. |
| Cimd | `true` when these credentials are a CIMD: a portable self-hosted HTTPS-URL `client_id` with no per-issuer state, hence exempt from re-binding (R-23.16, CIMD exemption). |

## See Also

* record [IssuerBoundCredentials](../IssuerBoundCredentials.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
