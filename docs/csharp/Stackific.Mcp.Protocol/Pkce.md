# Pkce class

PKCE `S256` generation, derivation, and verification (spec §23.5, R-23.5-a, R-23.5-b), backed by RandomNumberGenerator and SHA256. Randomness is injectable so callers can produce a deterministic verifier in tests.

```csharp
public static class Pkce
```

## Public Members

| name | description |
| --- | --- |
| const [CodeVerifierMaxLength](Pkce/CodeVerifierMaxLength.md) | The maximum `code_verifier` length mandated by RFC 7636 (R-23.5-b). |
| const [CodeVerifierMinLength](Pkce/CodeVerifierMinLength.md) | The minimum `code_verifier` length mandated by RFC 7636 (R-23.5-b). |
| static [CreateChallenge](Pkce/CreateChallenge.md)(…) | Creates a complete PKCE pair (verifier + `S256` challenge + method) (R-23.5-a, R-23.5-b). Randomness is injectable for deterministic tests. |
| static [DeriveCodeChallenge](Pkce/DeriveCodeChallenge.md)(…) | Derives the `S256``code_challenge` from a `code_verifier`: `BASE64URL(SHA-256(code_verifier))` (R-23.5-b). |
| static [GenerateCodeVerifier](Pkce/GenerateCodeVerifier.md)(…) | Generates a high-entropy PKCE `code_verifier` (R-23.5-b): 32 random bytes base64url-encode to a 43-character unreserved string. Randomness is injectable via *randomSource* for deterministic tests; the default draws from RandomNumberGenerator. |
| static [GenerateState](Pkce/GenerateState.md)(…) | Generates an opaque, unguessable `state` value binding an authorization request to the user-agent session (R-23.5-g): 32 random bytes, base64url-encoded. Randomness is injectable. |
| static [IsValidCodeVerifier](Pkce/IsValidCodeVerifier.md)(…) | Returns `true` when *verifier* is a valid PKCE `code_verifier`: 43–128 characters drawn solely from the unreserved alphabet (R-23.5-b). |
| static [Verify](Pkce/Verify.md)(…) | Verifies that a presented `code_verifier` matches a previously issued `code_challenge` under the `S256` method — the check an authorization server's token endpoint performs (R-23.5-b). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
