# UriTemplate.IsUriTemplate method

Returns `true` when *value* conforms to the URI Template grammar of [RFC 6570]: literal characters interspersed with well-formed `{…}` variable expressions (for example `file:///{path}`, `db://{table}/{id}`, `https://api/{+base}/items{?q,page}`). A literal-only string (no expressions) is a valid template. (§17.4, R-17.4-m)

```csharp
public static bool IsUriTemplate(string? value)
```

| parameter | description |
| --- | --- |
| value | The candidate URI-template string. |

## Return Value

`true` when the value is a well-formed RFC 6570 template.

## Remarks

Verifies brace balance and that every expression is non-empty and contains a valid variable list — an OPTIONAL leading operator from `+#./;?&` followed by one or more comma-separated `varspec`s. A literal `{` or `}` outside a balanced expression, an empty expression `{}`, or a nested opener is rejected. An empty string is NOT a valid template (it identifies no resource family), matching the TS contract.

## See Also

* class [UriTemplate](../UriTemplate.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
