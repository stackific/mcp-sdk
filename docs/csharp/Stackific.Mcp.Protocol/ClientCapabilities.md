# ClientCapabilities record

The capabilities a client advertises, per request, in the `io.modelcontextprotocol/clientCapabilities` metadata key (spec §6.2). A capability is declared by the presence of its field — an empty object `{}` still means "supported"; absence (here, `null`) means "not supported". Unknown fields are ignored on receipt (§6.6).

```csharp
public record ClientCapabilities
```

## Public Members

| name | description |
| --- | --- |
| [ClientCapabilities](ClientCapabilities/ClientCapabilities.md)() | The default constructor. |
| static [None](ClientCapabilities/None.md) { get; } | A shared, empty capability set declaring no optional client behaviors. |
| [Elicitation](ClientCapabilities/Elicitation.md) { get; set; } | Present if the client supports server-initiated elicitation (§20). |
| [Experimental](ClientCapabilities/Experimental.md) { get; set; } | Non-standard, experimental capabilities keyed by identifier (§6.2). |
| [Extensions](ClientCapabilities/Extensions.md) { get; set; } | The MCP extensions the client supports, keyed by extension identifier (§6.5). |
| [Roots](ClientCapabilities/Roots.md) { get; set; } | Present if the client exposes filesystem roots. Status: Deprecated (§21). |
| [Sampling](ClientCapabilities/Sampling.md) { get; set; } | Present if the client supports server-initiated sampling. Status: Deprecated (§21). |
| [SupportsElicitation](ClientCapabilities/SupportsElicitation.md) { get; } | `true` if the client declared elicitation support (form mode is the implicit baseline, §6.2). |
| [SupportsElicitationUrl](ClientCapabilities/SupportsElicitationUrl.md) { get; } | `true` if the client supports URL-mode elicitation (§6.2). |
| [SupportsExperimental](ClientCapabilities/SupportsExperimental.md) { get; } | `true` if the client declared an `experimental` map (presence means supported, §6.2). |
| [SupportsExtensions](ClientCapabilities/SupportsExtensions.md) { get; } | `true` if the client declared an `extensions` map (presence means supported, §6.5). |
| [SupportsRoots](ClientCapabilities/SupportsRoots.md) { get; } | `true` if the client declared the deprecated roots capability (§21). |
| [SupportsSampling](ClientCapabilities/SupportsSampling.md) { get; } | `true` if the client declared the deprecated sampling capability (§21). |
| [SupportsSamplingContext](ClientCapabilities/SupportsSamplingContext.md) { get; } | `true` if the client declared the deprecated `sampling.context` sub-flag (§6.2, R-6.2-n). This gates non-`none``includeContext` during sampling (see `CapabilityNegotiation.MayUseIncludeContext`). |
| [SupportsSamplingTools](ClientCapabilities/SupportsSamplingTools.md) { get; } | `true` if the client declared the deprecated `sampling.tools` sub-flag (§6.2, R-6.2-p). This gates supplying `tools`/`toolChoice` during sampling (see `CapabilityNegotiation.MayUseSamplingTools`). |
| [HasExtension](ClientCapabilities/HasExtension.md)(…) | Returns `true` if the client VALIDLY advertised the extension *identifier* (§6.5). Routed through [`IsAdvertised`](./Extensions/IsAdvertised.md) so an entry that is present but malformed — a `null` or non-object settings value — is NOT treated as advertised (R-6.5-j). A bare ContainsKey check would wrongly count a `null`-valued key. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
