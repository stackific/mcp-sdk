# CreateMessageResult record

The completion delivered back to the server, on retry, in response to a `sampling/createMessage` input request (spec §21.2.8) for the Deprecated Sampling capability (§21.2). The base `resultType` discriminator (§3) is supplied by the runtime.

```csharp
public record CreateMessageResult
```

## Public Members

| name | description |
| --- | --- |
| [Content](CreateMessageResult/Content.md) { get; set; } | REQUIRED. The produced content (spec §21.2.8). On the wire this is a single content block or an array of blocks; this SDK models it as an array (a single-block response is the one-element case). Tool-use requests are returned in the `assistant` role as [`ToolUseContent`](./ToolUseContent.md). |
| [Meta](CreateMessageResult/Meta.md) { get; set; } | OPTIONAL. Reserved metadata (spec §21.2.8/§4). |
| [Model](CreateMessageResult/Model.md) { get; set; } | REQUIRED. The name of the model that generated the message (spec §21.2.8). |
| [ResultType](CreateMessageResult/ResultType.md) { get; set; } | REQUIRED on emit (spec §21.2.8, R-21.2.8-e; §3.6): the base result-type discriminator. A sampling completion is always `"complete"`. Defaults to `"complete"` so a constructed result is well-formed, and so an inbound result that OMITS `resultType` degrades to `complete` per the §3.6 receiver rule rather than failing to bind. |
| [Role](CreateMessageResult/Role.md) { get; set; } | REQUIRED. The role of the produced message, `user` or `assistant`; a completion is normally `assistant` (spec §21.2.8). |
| [StopReason](CreateMessageResult/StopReason.md) { get; set; } | OPTIONAL. The reason sampling stopped, if known (spec §21.2.8). This is an open string to allow provider-specific values; the standard values are `endTurn`, `stopSequence`, `maxTokens`, and `toolUse`. |
| [Validated](CreateMessageResult/Validated.md)() | Asserts the §21.2.8 emit invariant before this result is sent back to the server: the required [`Model`](./CreateMessageResult/Model.md) is non-empty (R-21.2.8-c) and [`ResultType`](./CreateMessageResult/ResultType.md) is the completion discriminator `"complete"` (R-21.2.8-e). [`Role`](./CreateMessageResult/Role.md) and [`Content`](./CreateMessageResult/Content.md) are `required` members, so their presence is already enforced when binding from the wire. Returns this same instance for fluent use; never apply on receipt (a receiver degrades per §3.6). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
