# WellKnownDiscovery class

The well-known `.well-known` URI construction orders for protected-resource and authorization-server metadata discovery (spec §23.2 R-23.2-c – R-23.2-g; §23.3 R-23.3-b, R-23.3-c).

```csharp
public static class WellKnownDiscovery
```

## Public Members

| name | description |
| --- | --- |
| const [OAuthAsWellKnown](WellKnownDiscovery/OAuthAsWellKnown.md) | OAuth 2.0 Authorization Server Metadata well-known suffix (§23.3). |
| const [OpenIdConfigurationWellKnown](WellKnownDiscovery/OpenIdConfigurationWellKnown.md) | OpenID Connect Discovery well-known suffix (§23.3). |
| const [ProtectedResourceWellKnown](WellKnownDiscovery/ProtectedResourceWellKnown.md) | The protected-resource metadata well-known path suffix (§23.2). |
| static [AuthorizationServerUris](WellKnownDiscovery/AuthorizationServerUris.md)(…) | Builds the ordered list of authorization-server metadata well-known URIs to try for an *issuer*, in the exact specified priority order (R-23.3-b, R-23.3-c). |
| static [ProtectedResourceUris](WellKnownDiscovery/ProtectedResourceUris.md)(…) | Builds the ordered list of protected-resource-metadata well-known URIs to try for an MCP server endpoint, when no `resource_metadata` header URI is available (R-23.2-e, R-23.2-f): (1) path-aware insertion `/.well-known/oauth-protected-resource/<path>`; then (2) the host root. When the endpoint has no path beyond `/`, only the root URI is returned. |
| static [ResolveProtectedResourceUris](WellKnownDiscovery/ResolveProtectedResourceUris.md)(…) | Resolves where to fetch protected-resource metadata from, honoring discovery precedence (R-23.2-c, R-23.2-d, R-23.2-e, R-23.2-g): the `resource_metadata` header URI takes precedence (returned as the single entry); otherwise the ordered well-known URIs are returned. When neither is available the result is empty — the caller MUST then abort or fall back to pre-configured values (R-23.2-g). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
