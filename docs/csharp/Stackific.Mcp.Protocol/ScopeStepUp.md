# ScopeStepUp class

Least-privilege scope selection, the scope union that never drops already-granted scopes, and the bounded step-up re-authorization plan (spec §23.18, R-23.18-a – R-23.18-r, R-23.1-ae – R-23.1-ag).

```csharp
public static class ScopeStepUp
```

## Public Members

| name | description |
| --- | --- |
| static [FormatScopeSet](ScopeStepUp/FormatScopeSet.md)(…) | Serializes a scope list back into a space-delimited string. |
| static [ParseScopeSet](ScopeStepUp/ParseScopeSet.md)(…) | Splits a space-delimited scope string into a deduplicated, order-preserving list. Empty/whitespace input yields an empty list. |
| static [Plan](ScopeStepUp/Plan.md)(…) | Plans one step-up re-authorization end to end: decides whether to proceed (by actor and remaining retries), computes the UNION scope set that never drops already-granted scopes, and records the attempt against the bound (R-23.18-l – R-23.18-r, R-23.1-ae – R-23.1-ag). |
| static [SelectInitial](ScopeStepUp/SelectInitial.md)(…) | Selects the least-privilege scopes for the initial authorization handshake, applying the §23.18 priority (R-23.18-a – R-23.18-d): the challenge `scope` (authoritative), else all of `scopes_supported`, else omit (`null`). Delegates to S36's [`Resolve`](./AuthorizationScopes/Resolve.md). |
| static [ShouldAttempt](ScopeStepUp/ShouldAttempt.md)(…) | Returns `true` when a client SHOULD attempt the step-up flow for a scope-related error: always for a user-acting client (R-23.18-m); for a ClientCredentials client it MAY attempt or abort, so this returns `false` (the conservative default) (R-23.18-l – R-23.18-n). |
| static [UnionScopes](ScopeStepUp/UnionScopes.md)(…) | Computes the UNION of already-granted/already-requested scopes with the newly-challenged scopes — the scope set a step-up re-authorization requests (R-23.18-o, R-23.18-p, R-23.1-ae). Order-preserving and deduplicating: every already-granted scope is retained (R-23.18-p — never dropped) and the challenged scopes are appended. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
