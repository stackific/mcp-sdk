# FetchIconOptions record

Options for [`FetchIconAsync`](./IconSecurity/FetchIconAsync.md).

```csharp
public record FetchIconOptions
```

## Public Members

| name | description |
| --- | --- |
| [FetchIconOptions](FetchIconOptions/FetchIconOptions.md)() | The default constructor. |
| [AllowedTypes](FetchIconOptions/AllowedTypes.md) { get; set; } | The allowed rendered MIME types; defaults to [`DefaultImageAllowlist`](./IconSecurity/DefaultImageAllowlist.md). |
| [HttpClient](FetchIconOptions/HttpClient.md) { get; set; } | An explicit [`HttpClient`](./FetchIconOptions/HttpClient.md) to fetch with. When set, it is used as-is and never disposed by the SDK; the caller is responsible for configuring it to not follow redirects automatically (so the cross-origin / scheme-change gate can run on each hop). |
| [HttpMessageHandler](FetchIconOptions/HttpMessageHandler.md) { get; set; } | An [`HttpMessageHandler`](./FetchIconOptions/HttpMessageHandler.md) to build a one-shot HttpClient around (an injection point for tests). Ignored when [`HttpClient`](./FetchIconOptions/HttpClient.md) is set; it is not disposed by the SDK so a test handler can be reused across calls. |
| [MaxRedirects](FetchIconOptions/MaxRedirects.md) { get; set; } | The maximum number of same-origin redirects to follow before giving up; defaults to 5. |
| [TrustedHosts](FetchIconOptions/TrustedHosts.md) { get; set; } | An OPTIONAL allowlist of trusted hosts (§14.2 hardening). When set, an `https:` icon whose host is not on the list is refused before any request is made; `null` (the default) permits any host that passes the scheme and same-origin-redirect rules. Hosts are matched case-sensitively on Host. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
