# DcrRetry class

Loopback-aware `application_type` classification and bounded DCR retry that flips the `application_type` on a retryable rejection (spec §23.15, R-23.15-a – R-23.15-f).

```csharp
public static class DcrRetry
```

## Public Members

| name | description |
| --- | --- |
| static [ApplicationTypeForRedirectUris](DcrRetry/ApplicationTypeForRedirectUris.md)(…) | Classifies a set of redirect URIs as native or web and returns the `application_type` a client SHOULD register, consistent with those URIs (R-23.15-a, R-23.15-b, R-23.15-c). Redirect URIs that all resolve to a loopback host (`localhost`, `127.0.0.0/8`, or IPv6 `::1`, as decided by IsLoopback) indicate a native application → Native; otherwise Web. |
| static [RegisterWithRetryAsync](DcrRetry/RegisterWithRetryAsync.md)(…) | Performs Dynamic Client Registration with bounded retry, surfacing a meaningful error and retrying with an adjusted `application_type` when the AS rejects on a redirect-URI / application-type constraint (R-23.15-d, R-23.15-e, R-23.15-f). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
