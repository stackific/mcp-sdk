# InboundFailureStage enumeration

The stage at which an inbound message failed validation, used to select the authoritative `error.code` per the §22.6 classification pipeline.

```csharp
public enum InboundFailureStage
```

## Values

| name | value | description |
| --- | --- | --- |
| UnparseableJson | `0` | Bytes were not parseable as JSON → `-32700` (R-22.6-e). |
| InvalidRequestObject | `1` | Parsed JSON is not a valid request object (and not a routing failure) → `-32600` (R-22.6-c/f). |
| RoutingHeader | `2` | A routing header is missing/malformed/mismatched (HTTP) → `-32001` (R-22.6-b). |
| InvalidMetadata | `3` | Required per-request metadata is missing/invalid → `-32602` (R-22.6-d). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
