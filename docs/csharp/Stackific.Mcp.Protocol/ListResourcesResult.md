# ListResourcesResult record

The paginated, cacheable result of `resources/list` (spec §17.2).

```csharp
public record ListResourcesResult
```

## Public Members

| name | description |
| --- | --- |
| [CacheScope](ListResourcesResult/CacheScope.md) { get; set; } | The cache sharing scope (§13); REQUIRED on emit, tolerated absent on receipt. |
| [NextCursor](ListResourcesResult/NextCursor.md) { get; set; } | OPTIONAL. Opaque cursor for the next page; absent on the last page (§12). |
| [Resources](ListResourcesResult/Resources.md) { get; set; } | REQUIRED. The page of resources (may be empty). |
| [ResultType](ListResourcesResult/ResultType.md) { get; set; } | REQUIRED on emit (§3.6, §17.2): the base result-type discriminator, `"complete"` for a list page. Defaults to `"complete"` so a constructed result is well-formed, and — because it is plain `init`-settable — an inbound result that OMITS `resultType` deserializes to this default, which is exactly the §3.6 receiver rule ("treat an absent `resultType` as if it were `complete`"). |
| [TtlMs](ListResourcesResult/TtlMs.md) { get; set; } | The cache time-to-live hint in milliseconds (§13); REQUIRED on emit, tolerated absent on receipt. |
| [Validated](ListResourcesResult/Validated.md)() | Asserts the §17.2/§13 emit invariant — [`ResultType`](./ListResourcesResult/ResultType.md) is `"complete"` and both caching hints are present and well-formed — returning this same instance for fluent use. Apply before sending; never on receipt (a receiver degrades per §3.6/§13.1 instead of throwing). |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
