# TraceContext class

The §15.4 W3C trace-context propagation helpers — the C# counterpart of the TypeScript `protocol/logging.ts` trace utilities. The three bare keys (`traceparent`, `tracestate`, `baggage`) ride in `_meta`; receivers MUST treat them as opaque and intermediaries SHOULD propagate them UNCHANGED (R-15.4.2-h). This class uses the format validators already defined on [`MetaKeys`](../Stackific.Mcp.Json/MetaKeys.md).

```csharp
public static class TraceContext
```

## Public Members

| name | description |
| --- | --- |
| static [BareKeys](TraceContext/BareKeys.md) { get; } | The three W3C trace-context bare keys carried in `_meta` (§15.4.1). Mirrors TS `TRACE_CONTEXT_BARE_KEYS`. |
| static [ExtractTraceContext](TraceContext/ExtractTraceContext.md)(…) | Extracts only the string trace-context keys from *meta*, returning an object that contains at most `traceparent`, `tracestate`, and `baggage`. A non-tracing receiver can safely ignore the result. Values are copied verbatim (not parsed). Mirrors TS `extractTraceContext`. (R-15.4.2-c/g) |
| static [HasBaggage](TraceContext/HasBaggage.md)(…) | Returns `true` when *meta* carries a W3C-conformant `baggage`. Mirrors TS `hasBaggage`. (R-15.4.1-c) |
| static [HasTraceparent](TraceContext/HasTraceparent.md)(…) | Returns `true` when *meta* carries a W3C-conformant `traceparent`. Mirrors TS `hasTraceparent`. (R-15.4.1-a) |
| static [HasTracestate](TraceContext/HasTracestate.md)(…) | Returns `true` when *meta* carries a W3C-conformant `tracestate`. Mirrors TS `hasTracestate`. (R-15.4.1-b) |
| static [RelayTraceContext](TraceContext/RelayTraceContext.md)(…) | Copies the three trace-context keys present in *inbound* onto a fresh copy of *outbound*, UNCHANGED, for intermediary relay. Only keys present in *inbound* are copied; existing same-named outbound values are overwritten so the inbound values propagate unchanged; *outbound* is NOT mutated. Mirrors TS `relayTraceContext`. (R-15.4.2-h) |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
