# AccessTokenUsage class

Resource Indicators / audience binding (§23.6) and access-token usage / server-side per-request validation (§23.8): the `resource` parameter, audience validation (string OR array), token selection, the bearer header, and the `401`/`403` validation state machine.

```csharp
public static class AccessTokenUsage
```

## Public Members

| name | description |
| --- | --- |
| static [BuildBearerHeader](AccessTokenUsage/BuildBearerHeader.md)(…) | Builds the `Authorization: Bearer <access-token>` request header value a client MUST send on every request to the MCP server (R-23.8-a, R-23.8-b). |
| static [ExtractBearerToken](AccessTokenUsage/ExtractBearerToken.md)(…) | Extracts the bearer token from an `Authorization` header value, or `null` when the header is absent or does not use the `Bearer` scheme (R-23.8-b). The scheme match is case-insensitive per RFC 7235. |
| static [ResourceParameterFor](AccessTokenUsage/ResourceParameterFor.md)(…) | Returns the `resource` parameter value for the MCP server — its canonical resource identifier — that MUST be sent in BOTH the authorization and token requests, regardless of whether the AS advertises `resource` support (R-23.6-b – R-23.6-e). Surfaced as a named helper so the "always send it" rule is explicit. |
| static [SelectTokenForServer](AccessTokenUsage/SelectTokenForServer.md)(…) | Selects the access token a client may send to a given MCP server — strictly the one issued by that server's authorization server for that server, and no other (R-23.6-i). Returns a failed outcome (and *accessToken*`null`) when no matching token exists, so the client sends nothing rather than a wrong-audience token. |
| static [UrlContainsAccessTokenInQuery](AccessTokenUsage/UrlContainsAccessTokenInQuery.md)(…) | Returns `true` when a URL carries an `access_token` in its query string, which a client MUST NOT do (R-23.8-c). |
| static [ValidateRequest](AccessTokenUsage/ValidateRequest.md)(…) | Validates a presented access token on the MCP server side, on EVERY request, yielding a `401`/`403` challenge on failure (R-23.8-a, R-23.8-d, R-23.8-e, R-23.8-f). |
| static [ValidateTokenAudience](AccessTokenUsage/ValidateTokenAudience.md)(…) | Validates, on the MCP server side, that a presented token was issued for THIS server as the intended audience, rejecting any token whose audience is some other resource (R-23.6-f, R-23.6-g, R-23.6-h). Accepts a single audience OR an array, comparing each by canonical resource identity (uppercase scheme/host tolerant, trailing-slash tolerant; R-23.1-p). |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
