# ServerCapabilities.HasExtension method

Returns `true` if the server VALIDLY advertised the extension *identifier* (§6.5). Routed through [`IsAdvertised`](../Extensions/IsAdvertised.md) so an entry that is present but malformed — a `null` or non-object settings value — is NOT treated as advertised (R-6.5-j). A bare ContainsKey check would wrongly count a `null`-valued key.

```csharp
public bool HasExtension(string identifier)
```

| parameter | description |
| --- | --- |
| identifier | The extension identifier. |

## Return Value

`true` when validly advertised.

## See Also

* record [ServerCapabilities](../ServerCapabilities.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
