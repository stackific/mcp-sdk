# IssuerBoundCredentialStore class

An issuer-keyed store for persisted, issuer-bound client credentials, keeping separate registration state per authorization server (spec §23.16, R-23.16-a, R-23.16-b; §23.17, R-23.17-d).

```csharp
public sealed class IssuerBoundCredentialStore
```

## Public Members

| name | description |
| --- | --- |
| [IssuerBoundCredentialStore](IssuerBoundCredentialStore/IssuerBoundCredentialStore.md)() | The default constructor. |
| [CredentialsFor](IssuerBoundCredentialStore/CredentialsFor.md)(…) | Returns the credentials stored for *issuer*, or `null`. Never another issuer's (R-23.16-b, R-23.16-c). |
| [DecideFor](IssuerBoundCredentialStore/DecideFor.md)(…) | Returns the [`CredentialBindingDecision`](./CredentialBindingDecision.md) for the credentials stored under *discoveredIssuer* — the convenience entry point combining lookup and [`Decide`](./CredentialBinding/Decide.md) (R-23.16-c – R-23.16-g). |
| [Has](IssuerBoundCredentialStore/Has.md)(…) | Returns `true` when credentials are stored for *issuer*. |
| [Save](IssuerBoundCredentialStore/Save.md)(…) | Persists *credentials*, keyed by their `issuer` (R-23.16-a, R-23.16-b). |

## Remarks

The storage key is the authorization server's `issuer` identifier (R-23.16-b); a lookup never returns another issuer's credentials, so a caller cannot reuse credentials across authorization servers (R-23.16-c). Distinct from S36's [`DynamicClientRegistrationStore`](./DynamicClientRegistrationStore.md) (DCR-specific) and S35's [`CredentialStore`](./CredentialStore.md) (runtime tokens): this holds the persisted registration identity for ALL mechanisms, flagged with the CIMD exemption.

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
