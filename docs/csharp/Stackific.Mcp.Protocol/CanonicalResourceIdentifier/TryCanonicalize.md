# CanonicalResourceIdentifier.TryCanonicalize method

Validates and canonicalizes an MCP server endpoint URL into its canonical resource identifier (spec §23.1, R-23.1-m – R-23.1-s), writing the lowercase-scheme/host, fragment-free form to *canonical* on success.

```csharp
public static bool TryCanonicalize(string endpointUrl, out string? canonical, out string? reason)
```

| parameter | description |
| --- | --- |
| endpointUrl | The MCP server's endpoint URL. |
| canonical | On success, the canonicalized identifier; otherwise `null`. |
| reason | On failure, why the candidate is invalid; otherwise `null`. |

## Return Value

`true` when *endpointUrl* is a valid canonical resource identifier.

## Remarks

Enforced constraints: MUST be an absolute URI (R-23.1-m); MUST use `https`, or `http` only for a loopback host (R-23.1-n); MUST NOT contain a fragment (R-23.1-o). For robustness the scheme and host are lowercased (R-23.1-p). A host-root URL is emitted in bare-origin form (no trailing slash) per R-23.1-s; a path-level trailing slash is preserved (this function cannot know whether it is semantically significant — see [`StripDefaultTrailingSlash`](./StripDefaultTrailingSlash.md)).

## See Also

* class [CanonicalResourceIdentifier](../CanonicalResourceIdentifier.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
