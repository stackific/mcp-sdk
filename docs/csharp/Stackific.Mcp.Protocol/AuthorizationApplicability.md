# AuthorizationApplicability class

Applicability predicates for the §23 authorization flow: which transports it governs, which are forbidden from using it, and how each conveys credentials (spec §23.1, R-23.1-a – R-23.1-c).

```csharp
public static class AuthorizationApplicability
```

## Public Members

| name | description |
| --- | --- |
| static [AppliesTo](AuthorizationApplicability/AppliesTo.md)(…) | Returns `true` when the §23 authorization flow applies to *transport*. Authorization applies ONLY to HTTP-based transports (R-23.1-a). |
| static [ConveyanceFor](AuthorizationApplicability/ConveyanceFor.md)(…) | Returns how credentials are conveyed for *transport* (R-23.1-a – R-23.1-c). |
| static [ForbiddenFor](AuthorizationApplicability/ForbiddenFor.md)(…) | Returns `true` when *transport* MUST NOT use the §23 flow. Only stdio is explicitly forbidden (R-23.1-b); Other is merely outside §23's scope (R-23.1-c), not forbidden. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
