# ServerCapabilities record

The capabilities a server advertises in its `server/discover` result (spec §6.3). As with client capabilities, presence declares support and unknown fields are ignored (§6.6).

```csharp
public record ServerCapabilities
```

## Public Members

| name | description |
| --- | --- |
| [ServerCapabilities](ServerCapabilities/ServerCapabilities.md)() | The default constructor. |
| [Completions](ServerCapabilities/Completions.md) { get; set; } | Present if the server supports argument completion via `completion/complete` (§19). |
| [DeclaresCompletions](ServerCapabilities/DeclaresCompletions.md) { get; } | `true` if the server declared a `completions` capability (presence means supported, §6.3). |
| [DeclaresLogging](ServerCapabilities/DeclaresLogging.md) { get; } | `true` if the server declared the deprecated `logging` capability (§6.3, §15.3). |
| [DeclaresPrompts](ServerCapabilities/DeclaresPrompts.md) { get; } | `true` if the server declared a `prompts` capability (presence means supported, §6.3). |
| [DeclaresPromptsListChanged](ServerCapabilities/DeclaresPromptsListChanged.md) { get; } | `true` only when `prompts.listChanged` is explicitly `true` — absent or `false` means not declared (§6.3, R-6.3-h). The boolean sub-flags follow "true means declared". |
| [DeclaresResources](ServerCapabilities/DeclaresResources.md) { get; } | `true` if the server declared a `resources` capability (presence means supported, §6.3). |
| [DeclaresResourcesListChanged](ServerCapabilities/DeclaresResourcesListChanged.md) { get; } | `true` only when `resources.listChanged` is explicitly `true` (§6.3, R-6.3-l). |
| [DeclaresResourcesSubscribe](ServerCapabilities/DeclaresResourcesSubscribe.md) { get; } | `true` only when `resources.subscribe` is explicitly `true` (§6.3, R-6.3-l). |
| [DeclaresTools](ServerCapabilities/DeclaresTools.md) { get; } | `true` if the server declared a `tools` capability (presence means supported, §6.3). |
| [DeclaresToolsListChanged](ServerCapabilities/DeclaresToolsListChanged.md) { get; } | `true` only when `tools.listChanged` is explicitly `true` (§6.3, R-6.3-o). |
| [Experimental](ServerCapabilities/Experimental.md) { get; set; } | Non-standard, experimental capabilities keyed by identifier (§6.3). |
| [Extensions](ServerCapabilities/Extensions.md) { get; set; } | The MCP extensions the server supports, keyed by extension identifier (§6.5). |
| [Logging](ServerCapabilities/Logging.md) { get; set; } | Present if the server emits log messages. Status: Deprecated (§15.3). |
| [Prompts](ServerCapabilities/Prompts.md) { get; set; } | Present if the server offers prompts (§18). |
| [Resources](ServerCapabilities/Resources.md) { get; set; } | Present if the server offers resources (§17). |
| [Tools](ServerCapabilities/Tools.md) { get; set; } | Present if the server offers tools (§16). |
| [HasExtension](ServerCapabilities/HasExtension.md)(…) | Returns `true` if the server VALIDLY advertised the extension *identifier* (§6.5). Routed through [`IsAdvertised`](./Extensions/IsAdvertised.md) so an entry that is present but malformed — a `null` or non-object settings value — is NOT treated as advertised (R-6.5-j). A bare ContainsKey check would wrongly count a `null`-valued key. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
