# Ui class

S41 — Interactive UI Extension I: the server-facing, static behavior of the apps extension (spec §26.1–§26.4). The C# counterpart of the TypeScript `protocol/ui.ts` module: MIME/scheme predicates, host-advertisement reading, server gating (R-26.2-f/g), the visibility model (R-26.3-e/f), CSP/permission logic, and the UI-resource builders.

```csharp
public static class Ui
```

## Public Members

| name | description |
| --- | --- |
| static [DefaultVisibility](Ui/DefaultVisibility.md) { get; } | The effective visibility when `_meta.ui.visibility` is omitted: both actors may invoke the tool (spec §26.3, R-26.3-d). |
| static [DenyByDefaultCsp](Ui/DenyByDefaultCsp.md) { get; } | The deny-by-default CSP a host MUST apply when a UI resource omits `csp`: every directive an empty list (spec §26.4, R-26.4-h). |
| const [ToolUiMetaKey](Ui/ToolUiMetaKey.md) | The reserved nested key under a tool's `_meta` that carries the UI declaration: `ui` (spec §26.3). |
| const [UriScheme](Ui/UriScheme.md) | The `ui://` URI scheme prefix designating an MCP UI resource (spec §26.4, R-26.4-b). |
| static [BuildServerUiAcknowledgement](Ui/BuildServerUiAcknowledgement.md)() | Builds the `capabilities.extensions` fragment a server includes in its `server/discover` result to acknowledge the apps extension: a single [`ExtensionId`](./UiResource/ExtensionId.md) key mapped to an empty object (spec §26.2, R-26.2-j). Acknowledgement is OPTIONAL; a server merges this fragment into the `extensions` map of its result capabilities when it chooses to acknowledge. |
| static [BuildUiHostExtensionCapability](Ui/BuildUiHostExtensionCapability.md)(…) | Builds a conformant [`UiHostExtensionCapability`](./UiHostExtensionCapability.md) for a host that supports UI rendering. [`MimeType`](./UiResource/MimeType.md) is always included (deduplicated) so the result satisfies R-26.2-e; additional renderable MIME types are appended in order (spec §26.2, R-26.2-d, R-26.2-e). |
| static [BuildUiResourceContents](Ui/BuildUiResourceContents.md)(…) | Builds a UI resource `contents` entry: the `ui://`*uri*, the verbatim [`MimeType`](./UiResource/MimeType.md), the `text` OR `blob` payload, and — when supplied — the [`ResourceUiMeta`](./ResourceUiMeta.md) hints nested under `_meta.ui` (spec §26.4, R-26.4-d, R-26.4-e). Exactly one of *text*/*blob* MUST be supplied (S21 exclusivity). |
| static [BuildUiResourceReadResult](Ui/BuildUiResourceReadResult.md)(…) | Builds the result object a server returns from `resources/read` for a UI resource: a complete, cacheable result carrying the single UI `contents` entry, with the REQUIRED `ttlMs` / `cacheScope` cache fields (spec §26.4). Mirrors the S27 `ReadResourceResult` shape used in the §26.4 wire example. |
| static [CapabilityRendersUi](Ui/CapabilityRendersUi.md)(…) | Returns `true` when an advertised host capability value enables UI rendering: it is a well-formed [`IsUiHostExtensionCapability`](./Ui/IsUiHostExtensionCapability.md) AND its `mimeTypes` array contains the verbatim [`MimeType`](./UiResource/MimeType.md) (spec §26.2, R-26.2-d, R-26.2-e). |
| static [CspAllowsOrigin](Ui/CspAllowsOrigin.md)(…) | Returns `true` when *origin* is ALLOWED for the given CSP *directive* of a *csp* descriptor — it is explicitly listed in that member. An origin not listed (including when the member is absent, or when `csp` is `null`) MUST be blocked — deny-by-default applies (spec §26.4, R-26.4-g, R-26.4-h). |
| static [EffectiveVisibility](Ui/EffectiveVisibility.md)(…) | Returns the EFFECTIVE visibility of a UI declaration: the declared `visibility` array when present, otherwise the default `["model","app"]` (spec §26.3, R-26.3-d). |
| static [GetResourceUiMeta](Ui/GetResourceUiMeta.md)(…) | Extracts the [`ResourceUiMeta`](./ResourceUiMeta.md) hints from a UI resource `contents` entry — i.e. parses `contents._meta.ui` — returning `null` when there are no hints or they are malformed (spec §26.4, R-26.4-e). When present, these hints take effect for rendering. |
| static [GetToolUiMeta](Ui/GetToolUiMeta.md)(…) | Extracts the [`ToolUiMeta`](./ToolUiMeta.md) from a tool — i.e. parses `tool._meta.ui` — returning `null` when there is no `_meta`, no `ui` key, or the value is not a well-formed declaration (spec §26.3). This does NOT gate on negotiation: a receiver that has not negotiated the extension MUST ignore the key (R-26.3-g) — use [`ReadToolUiMeta`](./Ui/ReadToolUiMeta.md) for that. |
| static [GetUiHostCapability](Ui/GetUiHostCapability.md)(…) | Reads the `UiHostExtensionCapability` a host advertised under [`ExtensionId`](./UiResource/ExtensionId.md) from an `extensions` map (raw), or `null` when the extension is not validly advertised or its value is not a well-formed capability (spec §26.2, R-26.2-c, R-26.2-d). |
| static [HostAdvertisesUiRendering](Ui/HostAdvertisesUiRendering.md)(…) | Returns `true` when a host's `extensions` map advertises the apps extension in a way that enables UI rendering: the [`ExtensionId`](./UiResource/ExtensionId.md) key is present with a capability whose `mimeTypes` includes the verbatim [`MimeType`](./UiResource/MimeType.md) (spec §26.2, R-26.2-c, R-26.2-d, R-26.2-e). This is the predicate behind the server's two prohibitions — [`MayServerDeclareUi`](./Ui/MayServerDeclareUi.md) / [`MayServerExpectRendering`](./Ui/MayServerExpectRendering.md). |
| static [HostShouldRejectUiOriginatedCall](Ui/HostShouldRejectUiOriginatedCall.md)(…) | Returns `true` when a host SHOULD REJECT a `tools/call` that originates from a rendered UI, given the tool's UI declaration: it is rejected exactly when the tool's effective visibility excludes `"app"`, OR when the tool has no UI declaration at all (it was not exposed to the UI) (spec §26.3, R-26.3-e). |
| static [IsAppInvokable](Ui/IsAppInvokable.md)(…) | Returns `true` when a tool's effective visibility includes `"app"` — i.e. the rendered UI MAY invoke it over the channel (spec §26.3, R-26.3-e). |
| static [IsResourceUiMeta](Ui/IsResourceUiMeta.md)(…) | Returns `true` when *value* is a well-formed `ResourceUiMeta` — a JSON object whose OPTIONAL members (`csp`, `permissions`, `domain`, `prefersBorder`) are each of the correct shape when present (spec §26.4). Round-trips through the typed record. |
| static [IsToolUiMeta](Ui/IsToolUiMeta.md)(…) | Returns `true` when *value* is a well-formed `ToolUiMeta` — a JSON object carrying a REQUIRED `resourceUri` that uses the `ui://` scheme and an OPTIONAL `visibility` array drawn from `"model"`/`"app"` (spec §26.3, R-26.3-a, R-26.3-b, R-26.3-d). A non-`ui://``resourceUri` is rejected. |
| static [IsUiExtensionActive](Ui/IsUiExtensionActive.md)(…) | Returns `true` when, for an interaction, the apps extension is ACTIVE between client and server — both validly advertise [`ExtensionId`](./UiResource/ExtensionId.md) in their `extensions` maps (spec §26.2, R-26.2-a). When inactive, the host treats a tool carrying `_meta.ui` as a normal tool and ignores the UI key (R-26.2-i). |
| static [IsUiHostExtensionCapability](Ui/IsUiHostExtensionCapability.md)(…) | Returns `true` when *value* is a well-formed `UiHostExtensionCapability` — a JSON object carrying a `mimeTypes` string array (spec §26.2, R-26.2-d). This does NOT require the UI MIME type to be present — use [`CapabilityRendersUi`](./Ui/CapabilityRendersUi.md) for that. |
| static [IsUiMimeType](Ui/IsUiMimeType.md)(…) | Returns `true` when *mimeType* is exactly the UI MIME type [`MimeType`](./UiResource/MimeType.md) — matched verbatim and case-sensitively, with NO whitespace tolerance (spec §26.2/§26.4, R-26.2-e, R-26.4-d). `"text/html; profile=mcp-app"` (extra space) and `"TEXT/HTML;PROFILE=MCP-APP"` (wrong case) do NOT satisfy the requirement. |
| static [IsUiResourceContents](Ui/IsUiResourceContents.md)(…) | Returns `true` when *value* is a well-formed UI resource `contents` entry: a `ui://``uri`, the verbatim [`MimeType`](./UiResource/MimeType.md), exactly one of `text`/`blob`, and — when present — well-formed `_meta.ui` hints (spec §26.4, R-26.4-d, R-26.4-e). |
| static [IsUiResourceUri](Ui/IsUiResourceUri.md)(…) | Returns `true` when *uri* is a `ui://`-scheme URI string. The authority and path after `ui://` are server-defined and opaque; this only checks the scheme — the host MUST treat the whole URI as an opaque identifier and derive no network origin from it (spec §26.4, R-26.3-b, R-26.4-b, R-26.4-c). |
| static [IsVisibleToModel](Ui/IsVisibleToModel.md)(…) | Returns `true` when a tool's effective visibility includes `"model"` — i.e. it appears in the model's tool list and is callable via ordinary tool-calling. A tool with `visibility``["app"]` is callable ONLY by the UI and is HIDDEN from the model's list, so this returns `false` (spec §26.3, R-26.3-f). |
| static [MayEmitUiSurface](Ui/MayEmitUiSurface.md)(…) | Returns `true` when the apps extension is in *activeSet* and the server MAY therefore emit its surface (the `_meta.ui` key, the `ui://` resource) for this interaction (spec §26.2, R-26.2-a). |
| static [MayGrantPermission](Ui/MayGrantPermission.md)(…) | Returns `true` when a host MAY grant the named sandbox capability for a UI resource: ONLY when it was requested (the host MUST NOT grant an unrequested capability) AND the host did not decline it (the host MAY decline a requested one) (spec §26.4, R-26.4-j, R-26.4-k). |
| static [MayServerDeclareUi](Ui/MayServerDeclareUi.md)(…) | Returns `true` when a server MAY declare UI associations on its tools — only when the host has advertised the extension with a `mimeTypes` array that includes the verbatim [`MimeType`](./UiResource/MimeType.md). A server MUST NOT declare UI associations otherwise (spec §26.2, R-26.2-f). |
| static [MayServerExpectRendering](Ui/MayServerExpectRendering.md)(…) | Returns `true` when a server MAY expect a UI resource to be rendered — only when the host advertised the extension with the required [`MimeType`](./UiResource/MimeType.md). A server MUST NOT expect rendering otherwise (spec §26.2, R-26.2-g). Same gate as [`MayServerDeclareUi`](./Ui/MayServerDeclareUi.md), named separately so each prohibition reads clearly at the call site. |
| static [PermissionRequested](Ui/PermissionRequested.md)(…) | Returns `true` when a UI resource's `permissions` REQUESTS the named sandbox capability — i.e. the member is present. Absence means the capability is not requested, and the host MUST NOT grant it (spec §26.4, R-26.4-i, R-26.4-j). |
| static [ReadToolUiMeta](Ui/ReadToolUiMeta.md)(…) | Reads a tool's UI declaration ONLY when the extension is active for the interaction; returns `null` when the extension is not active, modeling "a receiver that does not negotiate this extension MUST ignore the `_meta.ui` key" (spec §26.3, R-26.3-g, R-26.2-i). When inactive the tool is treated as a normal tool and the key is ignored — its presence MUST NOT change the behavior of an ordinary `tools/call` (R-26.3-h). |
| static [RequestAdvertisesUiRendering](Ui/RequestAdvertisesUiRendering.md)(…) | Reads the host's advertised `extensions` map from a single request's `_meta` (the map nested under `io.modelcontextprotocol/clientCapabilities.extensions`) and reports whether it advertises UI rendering with the required MIME type (spec §26.2, R-26.2-c). A host that supports rendering MUST advertise the extension in the `_meta` of EVERY request; a request whose `_meta` omits the advertisement — or omits `clientCapabilities` entirely — yields `false`, and the server treats that request as if the extension were inactive (R-26.2-i). |
| static [RequestedPermissions](Ui/RequestedPermissions.md)(…) | Returns the set of sandbox capabilities a UI resource requests, as the subset of the four permission names present in *permissions* (spec §26.4, R-26.4-i). The host MUST NOT grant any capability outside this set (R-26.4-j) and MAY decline any within it (R-26.4-k). |
| static [ResolveCsp](Ui/ResolveCsp.md)(…) | Resolves the CSP a host applies for a UI resource: the declared `csp` when present, otherwise the restrictive [`DenyByDefaultCsp`](./Ui/DenyByDefaultCsp.md) (deny-by-default) (spec §26.4, R-26.4-h). A present `csp` is returned as-is for the host to constrain its policy by (R-26.4-o); an absent `csp` yields the all-empty deny-by-default policy. |
| static [ServerAcknowledgesUi](Ui/ServerAcknowledgesUi.md)(…) | Returns `true` when a server's `server/discover` result `capabilities.extensions` map acknowledges the apps extension — the [`ExtensionId`](./UiResource/ExtensionId.md) key is present with a (possibly empty) object value (spec §26.2, R-26.2-j). |
| static [ToolsVisibleToModel](Ui/ToolsVisibleToModel.md)(…) | Filters tools to those visible to the model, applying the §26.3 hide rule: a tool whose effective UI visibility is `["app"]`-only is omitted from the model's tool list (spec §26.3, R-26.3-f). The extension must be active for the rule to apply (R-26.3-g): when inactive, `_meta.ui` is ignored and every tool is treated as an ordinary, model-visible tool. A tool with no UI declaration is always model-visible. |
| static [UiResourceReadUri](Ui/UiResourceReadUri.md)(…) | Returns the `ui://` URI to use in a `resources/read` request for a tool's UI resource: the EXACT `resourceUri` from the tool's `_meta.ui`, treated as an opaque identifier (spec §26.4, R-26.3-c, R-26.4-b, R-26.4-c). Returns `null` when the meta is absent. |
| enum [UiCspDirective](Ui.UiCspDirective.md) | The four CSP descriptor members, in spec order (spec §26.4, R-26.4-f). |
| enum [UiPermissionName](Ui.UiPermissionName.md) | The four sandbox capability names a UI MAY request, in spec order (spec §26.4, R-26.4-i). |

## Remarks

The extension is an instance of the general Extension Mechanism (§24): the identifier [`ExtensionId`](./UiResource/ExtensionId.md) is an ordinary key in the `extensions` capability map, negotiated by intersection (reusing [`Extensions`](./Extensions.md)), and `_meta.ui` is the extension's reserved tool-metadata key. Rendering, sandbox/CSP enforcement, the message channel, and consent mediation are HOST responsibilities and are NOT implemented here — this models the host obligations declaratively (as constants and predicates a host consults) but renders nothing.

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
