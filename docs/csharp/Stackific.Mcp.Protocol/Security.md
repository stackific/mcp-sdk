# Security class

S44 — Security Considerations (spec §28).

```csharp
public static class Security
```

## Public Members

| name | description |
| --- | --- |
| static [DefaultInputBounds](Security/DefaultInputBounds.md) { get; } | Default input bounds: a conservative schema-depth and 4 MiB payload-size cap (spec §28.10, R-28.10-k, R-28.10-l). |
| static [Principles](Security/Principles.md) { get; } | The four §28.1 core security principles every conforming implementation is built around (spec R-28.1-a). |
| static [Requirements](Security/Requirements.md) { get; } | Every numbered §28 requirement atom, in spec order — the single enumerable security baseline an implementation must address (spec R-28-a, and every R-28.x-y). Each atom id is used throughout the per-feature modules so a reviewer can trace an obligation to the code that enforces it. |
| const [DefaultMaxSchemaDepth](Security/DefaultMaxSchemaDepth.md) | The default schema nesting-depth limit (mirrors the SDK's `DEFAULT_SCHEMA_LIMITS.maxDepth`). |
| const [InvalidCursorCode](Security/InvalidCursorCode.md) | The JSON-RPC error code an invalid pagination cursor is reported with (spec §18 / §28.10-j). |
| const [RateLimitRejectionCode](Security/RateLimitRejectionCode.md) | The JSON-RPC error code a rate-limited or invalid-request rejection carries (spec §28.3 wire example). |
| const [RedactedPlaceholder](Security/RedactedPlaceholder.md) | The placeholder substituted for a redacted credential/token value (spec R-28.9-d, R-28.9-e). |
| const [ValidationErrorCode](Security/ValidationErrorCode.md) | The JSON-RPC error code a validation/cursor/argument failure is reported with (spec §28.10). |
| static [AccessControlsAreCommensurate](Security/AccessControlsAreCommensurate.md)(…) | Returns `true` when the access controls a host applies are at least as strong as the data's sensitivity requires (spec §28.1, §28.4, R-28.1-g, R-28.4-c; AC-44.4). `confidential` data protected only at `internal` strength fails. |
| static [AssertConsentedDataExposure](Security/AssertConsentedDataExposure.md)(…) | Asserts that user/resource data is exposed to a server (or onward) ONLY with the user's consent (spec §28.4, R-28.4-b, R-28.1-e, R-28.1-f; AC-44.3, AC-44.11). Wraps [`EvaluateConsent`](./Security/EvaluateConsent.md) with the `resource-exposure` operation. |
| static [AssertElicitationUnderUserControl](Security/AssertElicitationUnderUserControl.md)(…) | Asserts a server-initiated elicitation remained under user control before anything was returned to the server (spec §28.7, R-28.7-a … R-28.7-e; AC-44.19): the user could review and reach an explicit decision, the requesting server's identity was shown, and the request did not phish for secrets via form mode. A `decline`/`cancel` decision is always permitted (the user may stop at any point), returning `Ok` without requiring the schema to be safe. |
| static [AssertHumanInTheLoop](Security/AssertHumanInTheLoop.md)(…) | Asserts the human-in-the-loop invariant for a proposed tool invocation: the user could review and understand it and the decision did not rest solely with the model (spec §28.3, R-28.3-d, R-28.3-e; AC-44.8). |
| static [AssertNoTokenPassthrough](Security/AssertNoTokenPassthrough.md)(…) | Asserts the no-token-passthrough / confused-deputy rule: a server never forwards a client token upstream and uses a SEPARATE token issued by the upstream AS (spec §28.5, R-28.5-f, R-28.5-g; AC-44.13). |
| static [AssertSamplingUnderUserControl](Security/AssertSamplingUnderUserControl.md)(…) | Asserts a server-driven sampling flow remained under user control: the MUST-level §28.7 obligations are met (human review of prompt and completion before they are acted upon or transmitted) and the host disclosed no more conversation context than the user authorized (spec §28.7, R-28.7-a, R-28.7-f, R-28.7-g; AC-44.20). |
| static [AssertSelfContainedSchema](Security/AssertSelfContainedSchema.md)(…) | Asserts a tool schema is self-contained — it carries no external `$ref` that the server would have to dereference — unless external resolution is explicitly permitted against a trusted source (spec §28.10, R-28.10-m, R-28.10-n; AC-44.29). A pure structural inspection that performs no I/O. An in-document `$ref` (starting with `#`) is allowed. |
| static [AssertServerIsolation](Security/AssertServerIsolation.md)(…) | Asserts the §28.4 server-isolation invariant: a server receives only host-elected context, never another server's requests/results/context/credentials (spec §28.4, R-28.4-a, R-28.4-d, R-28.4-e, R-28.4-f; AC-44.11). |
| static [AssertTokenTransportSecurity](Security/AssertTokenTransportSecurity.md)(…) | Asserts the §28.5 token-confidentiality transport rules: tokens are never logged, never forwarded to a party other than the one they were issued for, and authorization-server endpoints and redirect URIs use HTTPS (a `localhost` redirect is permitted) (spec §28.5, R-28.5-n … R-28.5-q, R-28.9-d; AC-44.17). Returns the first violation. |
| static [AssertUiSandboxConforming](Security/AssertUiSandboxConforming.md)(…) | Asserts a server-provided UI is rendered conformingly: it runs in an isolated sandbox that denies DOM/cookies/storage/navigation, under a restrictive CSP, and exposes no credentials/tokens/unrelated context (spec §28.8, R-28.8-a, R-28.8-e, R-28.8-f, R-28.8-g; AC-44.21, AC-44.22). |
| static [AssessBaseline](Security/AssessBaseline.md)(…) | Asserts that an implementation is designed around all four §28.1 core principles (spec R-28-a, R-28.1-a; AC-44.1). Returns `Ok` only when every principle is claimed; otherwise lists the unmet ones in canonical order. |
| static [BuildRateLimitRejection](Security/BuildRateLimitRejection.md)(…) | Builds the `-32600` rate-limit rejection error a server returns for a `tools/call` that exceeds the limit, matching the §28.3 wire example (spec R-28.3-h; AC-44.9). |
| static [ClassifyToolDefinitionTrust](Security/ClassifyToolDefinitionTrust.md)(…) | Classifies a tool definition's trust: Untrusted unless obtained from a server the host trusts (spec §28.3, R-28.1-i, R-28.3-b; AC-44.6). |
| static [EnforceInputBounds](Security/EnforceInputBounds.md)(…) | Bounds the resources consumed while validating a peer input: rejects a schema whose nesting depth exceeds the limit and a payload exceeding the size limit (spec §28.10, R-28.10-k, R-28.10-l; AC-44.28). The depth probe stops at the cap so a pathological self-referential schema cannot exhaust the stack while being measured. The payload-size check uses the UTF-8 byte length. |
| static [EvaluateConsent](Security/EvaluateConsent.md)(…) | The host consent gate every operation acting on the user's behalf passes before it reaches a server (spec §28.2, R-28.2-a … R-28.2-f; AC-44.2, AC-44.7). Allows the operation ONLY when it matches a prior grant for the SAME operation and scope, or the user freshly, informedly approved THIS proposal. Denies (with a reason) on no-consent, not-informed, or silent-escalation. The gate never treats a missing approval as approval. |
| static [LookupRequirement](Security/LookupRequirement.md)(…) | Looks up a §28 requirement atom by id (e.g. `R-28.5-b`), or `null` (spec R-28-a). |
| static [MandatoryRequirements](Security/MandatoryRequirements.md)() | Returns every MUST / MUST NOT requirement — the hard obligations conformance turns on (spec R-28-a). |
| static [MayDisplayToolAnnotations](Security/MayDisplayToolAnnotations.md)(…) | Returns whether a host MAY surface a tool's annotation hints to the user for THIS server (spec §28.3, R-28.3-b; AC-44.6). Displaying a hint from a trusted server is permitted; relying on it as a guarantee is not ([`ToolAnnotationIsSecurityGuarantee`](./Security/ToolAnnotationIsSecurityGuarantee.md), R-28.3-c). |
| static [MediateUiInitiatedToolCall](Security/MediateUiInitiatedToolCall.md)(…) | Mediates a UI-requested `tools/call`, routing it through the host's normal consent / human-in-the-loop path; the UI can never cause a tool to run without host mediation and user consent (spec §28.8, R-28.8-b, R-28.8-c, R-28.8-d; AC-44.21). Routes only when the tool has `app` visibility AND the user consented AND host policy allows. |
| static [MetadataConveysAuthority](Security/MetadataConveysAuthority.md)() | Returns `false` — metadata MUST NOT be a source of authority (spec §28.9, R-28.9-a; AC-44.23). Unconditional, so a caller cannot accidentally derive authority from a metadata field. |
| static [RecordConsentGrant](Security/RecordConsentGrant.md)(…) | Builds the [`ConsentGrant`](./Security.ConsentGrant.md) to persist after a successful, informed approval, so a later identical operation matches without re-prompting (spec R-28.2-b, R-28.2-f). Only call after the user has actively and informedly approved. |
| static [RedactForLogging](Security/RedactForLogging.md)(…) | Returns a copy of a JSON value intended for a log/trace/telemetry sink with credential/token values redacted (spec §28.9, R-28.9-c, R-28.9-d, R-28.9-e; AC-44.23, AC-44.17). Walks the value recursively; any property whose key names a credential/token has its value replaced with [`RedactedPlaceholder`](./Security/RedactedPlaceholder.md). The input is never mutated. |
| static [RequirementsForPrinciple](Security/RequirementsForPrinciple.md)(…) | Returns every §28 requirement that derives from a given core principle, in spec order (spec R-28.1-a). |
| static [SanitizeConsumedMetadata](Security/SanitizeConsumedMetadata.md)(…) | Validates the structure of consumed metadata, returning only the entries the receiver understands and ignoring the rest (spec §28.9, R-28.9-b; AC-44.23). Keeps only keys in *known* (and only when present and non-null). Never throws — a non-object yields an empty object. |
| static [SanitizeFilePath](Security/SanitizeFilePath.md)(…) | Sanitizes a requested `file://` resource path against an authorized root, rejecting directory-traversal and any path that escapes the root (spec §28.10, R-28.10-o, R-28.10-p; AC-44.30). Purely lexical (no filesystem I/O): normalizes `.`/`..` segments POSIX-style and confirms the result stays within *authorizedRoot*. A NUL byte is rejected outright. |
| static [SanitizeToolOutputText](Security/SanitizeToolOutputText.md)(…) | Sanitizes a tool-output text string so a result cannot carry control sequences that would compromise the client, model, or downstream consumers (spec §28.3, R-28.3-i; AC-44.9). Strips C0/C1 control characters (excluding the ordinary whitespace `\t`, `\n`, `\r`). |
| static [ToolAnnotationIsSecurityGuarantee](Security/ToolAnnotationIsSecurityGuarantee.md)() | Returns `false` — a tool annotation is NEVER a security guarantee (spec §28.3, R-28.3-c; AC-44.6). Unconditional: a receiver MUST NOT rely on an annotation as enforcement. |
| static [ToolOutputHasControlSequences](Security/ToolOutputHasControlSequences.md)(…) | Returns `true` when *text* contains a control sequence a sanitized output MUST NOT carry (spec R-28.3-i). |
| static [ValidateAuthorizationIssuer](Security/ValidateAuthorizationIssuer.md)(…) | Validates the exact-issuer mix-up defense for an authorization response: the client MUST have recorded the expected issuer before redirect and MUST compare any returned issuer by exact string comparison, rejecting mismatches (spec §28.5, R-28.5-h, R-28.5-i; AC-44.14). |
| static [ValidatePaginationCursor](Security/ValidatePaginationCursor.md)(…) | Validates a pagination cursor as opaque, untrusted input: it is rejected with a `-32602` error when malformed, unknown, or expired, rather than having its attacker-controlled contents interpreted (spec §28.10, R-28.10-j; AC-44.27). The *isKnown* predicate is the server's own recognition check. An absent cursor is valid — it requests the first page. |
| static [ValidatePeerToolCall](Security/ValidatePeerToolCall.md)(…) | Validates `tools/call` arguments against a tool's declared input schema and, optionally, structured results against an output schema, reporting a failure as a `-32602` error rather than acting on the input (spec §28.10, R-28.10-a … R-28.10-e; AC-44.24). The schema check enforces the common JSON Schema constraints (`required`, top-level property `type`, `enum`), mirroring the SDK's pragmatic validator. |
| static [ValidateRequestOrigin](Security/ValidateRequestOrigin.md)(…) | Validates an `Origin` header against the server's accepted-origin set on every incoming HTTP connection, rejecting untrusted origins to defend against DNS-rebinding — the §28.10-i restatement of the §9.11 rule (spec §28.10, R-28.10-i; AC-44.26). An absent `Origin` or one in the set passes. |
| static [ValidateResourceUriAccess](Security/ValidateResourceUriAccess.md)(…) | Validates a resource URI before dereferencing or matching it: it parses as an absolute URI, its location is one the user has authorized, and (when it could trigger a network request) it is not an SSRF target (spec §28.10, R-28.10-f, R-28.10-g, R-28.10-h; AC-44.25). |
| static [ValidateServerAccessToken](Security/ValidateServerAccessToken.md)(…) | Validates, server-side, that a presented access token is audience-bound to THIS server and was validated before the request is processed; rejects otherwise so no data is returned to an unauthorized party (spec §28.5, R-28.5-b … R-28.5-e; AC-44.12). The audience claim may be a single resource (string) or an array; it matches when it includes the canonical resource. |
| struct [ConsentDecision](Security.ConsentDecision.md) | The §28.2 consent-gate decision. |
| struct [ConsentGrant](Security.ConsentGrant.md) | A record of the consent a user has explicitly granted for a single operation (spec §28.2). |
| enum [ConsentReason](Security.ConsentReason.md) | The §28.2 consent-gate decision reason. |
| struct [ConsentRequest](Security.ConsentRequest.md) | A proposed operation seeking the host's consent gate (spec §28.2). |
| enum [ContinuationTokenFailure](Security.ContinuationTokenFailure.md) | The reason a continuation token is rejected (spec §28.6). |
| record [ContinuationTokenRecord&lt;TState&gt;](Security.ContinuationTokenRecord-1.md) | A server-side record of continuation state — the §28.6 handling profile for the requestState token. |
| struct [ContinuationTokenValidation&lt;TState&gt;](Security.ContinuationTokenValidation-1.md) | Outcome of [`Validate`](./ContinuationTokenStore-1/Validate.md). |
| struct [CursorValidation](Security.CursorValidation.md) | Outcome of [`ValidatePaginationCursor`](./Security/ValidatePaginationCursor.md). |
| enum [DataSensitivity](Security.DataSensitivity.md) | A coarse data-sensitivity class governing the strength of access controls a host SHOULD apply (spec §28.1, §28.4, R-28.1-g, R-28.4-c). |
| enum [ElicitationUserDecision](Security.ElicitationUserDecision.md) | The terminal user decision on a server-initiated elicitation (spec §28.7, R-28.7-b, R-28.7-c). |
| struct [FilePathValidation](Security.FilePathValidation.md) | Outcome of [`SanitizeFilePath`](./Security/SanitizeFilePath.md). |
| struct [HumanInTheLoopValidation](Security.HumanInTheLoopValidation.md) | Outcome of [`AssertHumanInTheLoop`](./Security/AssertHumanInTheLoop.md). |
| struct [InputBounds](Security.InputBounds.md) | Resource bounds a receiver imposes while validating peer inputs (spec §28.10, R-28.10-k, R-28.10-l). |
| struct [InputBoundsValidation](Security.InputBoundsValidation.md) | Outcome of [`EnforceInputBounds`](./Security/EnforceInputBounds.md). |
| enum [InputTrust](Security.InputTrust.md) | Classification of an input's trust, the §28 trust-boundary primitive (spec §28.1, §28.3, R-28.1-i, R-28.3-b). |
| struct [InvalidCursorError](Security.InvalidCursorError.md) | The error a malformed/unknown/expired cursor is rejected with (spec §28.10-j). |
| struct [IsolationValidation](Security.IsolationValidation.md) | Outcome of [`AssertServerIsolation`](./Security/AssertServerIsolation.md) / [`AssertConsentedDataExposure`](./Security/AssertConsentedDataExposure.md). |
| struct [IssuerValidation](Security.IssuerValidation.md) | Outcome of [`ValidateAuthorizationIssuer`](./Security/ValidateAuthorizationIssuer.md). |
| struct [OriginValidation](Security.OriginValidation.md) | Outcome of [`ValidateRequestOrigin`](./Security/ValidateRequestOrigin.md). |
| struct [PeerToolCallValidation](Security.PeerToolCallValidation.md) | Outcome of [`ValidatePeerToolCall`](./Security/ValidatePeerToolCall.md). |
| struct [RateLimitRejectionError](Security.RateLimitRejectionError.md) | A §28.3 rate-limit rejection error object, matching the story's wire example. |
| struct [ResourceUriValidation](Security.ResourceUriValidation.md) | Outcome of [`ValidateResourceUriAccess`](./Security/ValidateResourceUriAccess.md). |
| struct [SamplingConsentObligations](Security.SamplingConsentObligations.md) | The host's §21.2.10 sampling consent-obligation claims (spec §28.7). The MUST-level fields gate whether a server-driven sampling flow remained under user control. |
| struct [SchemaSelfContainmentValidation](Security.SchemaSelfContainmentValidation.md) | Outcome of [`AssertSelfContainedSchema`](./Security/AssertSelfContainedSchema.md). |
| struct [SecurityBaselineAssessment](Security.SecurityBaselineAssessment.md) | Outcome of [`AssessBaseline`](./Security/AssessBaseline.md). |
| struct [SecurityBaselineClaims](Security.SecurityBaselineClaims.md) | A host's self-assertion that it addresses each of the four §28.1 core principles (spec R-28.1-a; AC-44.1). |
| record [SecurityRequirement](Security.SecurityRequirement.md) | A single normative §28 requirement, as consolidated by S44. |
| struct [ServerTokenValidation](Security.ServerTokenValidation.md) | Outcome of [`ValidateServerAccessToken`](./Security/ValidateServerAccessToken.md). |
| struct [TokenPassthroughValidation](Security.TokenPassthroughValidation.md) | Outcome of [`AssertNoTokenPassthrough`](./Security/AssertNoTokenPassthrough.md). |
| struct [TokenTransportValidation](Security.TokenTransportValidation.md) | Outcome of [`AssertTokenTransportSecurity`](./Security/AssertTokenTransportSecurity.md). |
| struct [ToolsCallMediationDecision](Security.ToolsCallMediationDecision.md) | The mediation decision for a UI-initiated `tools/call`. |
| struct [ToolsCallMediationInput](Security.ToolsCallMediationInput.md) | The input to a UI-initiated `tools/call` mediation (spec §28.8 / S42). |
| struct [ToolSchemas](Security.ToolSchemas.md) | A tool's declared schemas for [`ValidatePeerToolCall`](./Security/ValidatePeerToolCall.md). |
| struct [UserControlValidation](Security.UserControlValidation.md) | Outcome of [`AssertElicitationUnderUserControl`](./Security/AssertElicitationUnderUserControl.md) / [`AssertSamplingUnderUserControl`](./Security/AssertSamplingUnderUserControl.md). |

## Remarks

The cross-cutting security and trust model every conforming MCP implementation MUST honor. §28 is a consolidating section: it defines no new wire types but binds together the most critical obligations introduced piecemeal alongside individual features. The protocol cannot enforce most of these at the wire level, so conformance depends on implementations honoring them. This module models them as a registry of every numbered §28 requirement atom ([`Requirements`](./Security/Requirements.md)), predicates and validators for the obligations that are checkable in code (consent gating, trust classification, token handling, continuation-token integrity, input/URI/cursor validation, resource bounds), and a four-principle baseline checklist ([`AssessBaseline`](./Security/AssessBaseline.md)).

The TypeScript SDK delegates several validators to per-feature modules (S25 tools, S33 sampling, S37 authorization, S42 UI host, S18 pagination). Where those modules have no C# counterpart yet, the small, deterministic mechanics they own (audience/issuer matching, schema validation, control-char stripping, file-path normalization, SSRF host classification) are reproduced here so the §28 surface is self-contained and behaviorally faithful.

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
