# CancelledRequestSet class

A sender-side set of request ids for which a `notifications/cancelled` was sent but whose response has not yet arrived — the C# counterpart of the TypeScript `CancelledRequestSet`. A sender SHOULD distinctly ignore (not merely tolerate) late responses to cancelled requests so the race is detectable rather than silently processed (R-15.2.3-e / RC-6).

```csharp
public sealed class CancelledRequestSet
```

## Public Members

| name | description |
| --- | --- |
| [CancelledRequestSet](CancelledRequestSet/CancelledRequestSet.md)() | The default constructor. |
| [Count](CancelledRequestSet/Count.md) { get; } | The number of ids awaiting a late response to discard. Mirrors TS `size`. |
| [Acknowledge](CancelledRequestSet/Acknowledge.md)(…) | Removes *requestId* after its late response has been discarded. Safe for an unknown id. Mirrors TS `acknowledge`. |
| [Add](CancelledRequestSet/Add.md)(…) | Marks *requestId* as cancelled (call after sending `notifications/cancelled`). Mirrors TS `add`. |
| [IsIgnorable](CancelledRequestSet/IsIgnorable.md)(…) | Returns `true` when a response for *requestId* SHOULD be ignored. Mirrors TS `isIgnorable`. (R-15.2.3-e) |

## Remarks

Lifecycle: [`Add`](./CancelledRequestSet/Add.md) immediately after sending the cancellation; [`IsIgnorable`](./CancelledRequestSet/IsIgnorable.md) when a response arrives (discard it if `true`); [`Acknowledge`](./CancelledRequestSet/Acknowledge.md) after discarding the late response to bound the set's growth. String and number ids are tracked independently. Not thread-safe.

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
