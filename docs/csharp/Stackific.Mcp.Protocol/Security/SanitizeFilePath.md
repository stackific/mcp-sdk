# Security.SanitizeFilePath method

Sanitizes a requested `file://` resource path against an authorized root, rejecting directory-traversal and any path that escapes the root (spec §28.10, R-28.10-o, R-28.10-p; AC-44.30). Purely lexical (no filesystem I/O): normalizes `.`/`..` segments POSIX-style and confirms the result stays within *authorizedRoot*. A NUL byte is rejected outright.

```csharp
public static FilePathValidation SanitizeFilePath(string requestedPath, string authorizedRoot)
```

| parameter | description |
| --- | --- |
| requestedPath | The requested file path (relative to, or under, the root) (R-28.10-o). |
| authorizedRoot | The absolute root directory the user has authorized (R-28.10-p). |

## Return Value

The validation outcome.

## See Also

* struct [FilePathValidation](../Security.FilePathValidation.md)
* class [Security](../Security.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
