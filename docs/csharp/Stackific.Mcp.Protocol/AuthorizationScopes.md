# AuthorizationScopes class

Authorization-request scope resolution (the scope-priority rule) and `offline_access` handling (spec §23.5 R-23.5-f, §23.9 R-23.9-b, R-23.9-g).

```csharp
public static class AuthorizationScopes
```

## Public Members

| name | description |
| --- | --- |
| static [AdvertisedScopesExcludeOfflineAccess](AuthorizationScopes/AdvertisedScopesExcludeOfflineAccess.md)(…) | Returns `true` when neither the `WWW-Authenticate``scope` nor protected-resource `scopes_supported` includes `offline_access`, as an MCP server SHOULD ensure (R-23.9-g). |
| static [Resolve](AuthorizationScopes/Resolve.md)(…) | Resolves the `scope` parameter to send in the authorization request, applying the scope priority (R-23.5-f): (1) the `WWW-Authenticate` challenge's `scope`; (2) all of protected-resource `scopes_supported`; (3) otherwise `null` (omit `scope`). |
| static [WithOfflineAccess](AuthorizationScopes/WithOfflineAccess.md)(…) | Adds `offline_access` to a `scope` string when, and only when, the authorization-server metadata advertises it in `scopes_supported` (R-23.9-b). Returns the scope unchanged when `offline_access` is not advertised or already present. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
