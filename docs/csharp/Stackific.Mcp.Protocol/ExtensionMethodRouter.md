# ExtensionMethodRouter class

Routes extension-defined methods to their handlers, enforcing the two framework rules that govern dispatch (spec §24.5): method strings are namespaced under the registering extension (R-24.5-b), and a handler is invoked ONLY when its extension is in the active set for the interaction (R-24.5-c).

```csharp
public sealed class ExtensionMethodRouter
```

## Public Members

| name | description |
| --- | --- |
| [ExtensionMethodRouter](ExtensionMethodRouter/ExtensionMethodRouter.md)() | The default constructor. |
| [Dispatch](ExtensionMethodRouter/Dispatch.md)(…) | Dispatches *method* with *parameters*, but only when the owning extension is in *activeSet* (spec R-24.5-c). An unknown method or an inactive owning extension is refused with `-32602` so a caller can convert the outcome into a core error response when it chooses to reject rather than ignore (R-24.3-f). |
| [Has](ExtensionMethodRouter/Has.md)(…) | Returns `true` when *method* has a registered handler. |
| [OwnerOf](ExtensionMethodRouter/OwnerOf.md)(…) | Returns the extension identifier that owns *method*, or `null`. |
| [Register](ExtensionMethodRouter/Register.md)(…) | Registers *handler* for an extension-defined *method*. The method MUST be in *identifier*'s derived namespace (R-24.5-b) and MUST NOT already be registered (no redefinition, R-24.5-i). |
| struct [DispatchOutcome](ExtensionMethodRouter.DispatchOutcome.md) | Outcome of [`Dispatch`](./ExtensionMethodRouter/Dispatch.md). |
| enum [DispatchRejection](ExtensionMethodRouter.DispatchRejection.md) | Why [`Dispatch`](./ExtensionMethodRouter/Dispatch.md) declined to invoke a handler. |

## Remarks

Registration validates the namespace eagerly so a misnamed method is rejected at wiring time, not silently at dispatch. The router holds no per-connection state; the active set is supplied per dispatch, honoring the stateless model (§24.4).

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
