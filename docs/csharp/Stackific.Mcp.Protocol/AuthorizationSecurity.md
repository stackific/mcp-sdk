# AuthorizationSecurity class

Consolidated §23.19 authorization-security predicates: audience binding, exact issuer validation, the per-request record invariant, token confidentiality/redaction, header-only token presentation, and refresh-token handling (spec §23.19, R-23.19-a – R-23.19-u).

```csharp
public static class AuthorizationSecurity
```

## Public Members

| name | description |
| --- | --- |
| const [RedactedTokenMarker](AuthorizationSecurity/RedactedTokenMarker.md) | The fixed redaction marker used in place of a token at any log/forward sink (R-23.19-m, R-23.19-n, R-23.19-o). |
| const [RefreshTokenAlwaysOptional](AuthorizationSecurity/RefreshTokenAlwaysOptional.md) | `true` — a client MUST NOT assume a refresh token will be issued; the authorization server retains discretion (R-23.19-t). This is an unconditional control-flow invariant (treat the refresh token as optional and handle its absence, pairing with [`HasNoRefreshToken`](./TokenResponse/HasNoRefreshToken.md)), so it is a named constant rather than a no-argument predicate. |
| const [TokensAreConfidential](AuthorizationSecurity/TokensAreConfidential.md) | `true` — access and refresh tokens are confidential: they MUST NOT be logged and MUST NOT be forwarded to third parties (R-23.19-m, R-23.19-n). The rule is unconditional, so it is a named constant rather than a predicate over a token (which would only invite passing the secret somewhere it could be captured). |
| static [CheckBearerHeaderOnly](AuthorizationSecurity/CheckBearerHeaderOnly.md)(…) | Validates that the access token is presented ONLY in the `Authorization: Bearer` request header and NEVER in the URI query string (R-23.19-p). Reuses S36's [`UrlContainsAccessTokenInQuery`](./AccessTokenUsage/UrlContainsAccessTokenInQuery.md) and requires an `Authorization` header to be present. |
| static [CheckResourceParameterBinding](AuthorizationSecurity/CheckResourceParameterBinding.md)(…) | Validates the audience-binding requirement: the SAME `resource` parameter, identifying the MCP server by its canonical URI, MUST be present in BOTH the authorization request and the token request, regardless of advertised AS support (R-23.19-a). |
| static [GrantTypesWithRefresh](AuthorizationSecurity/GrantTypesWithRefresh.md)(…) | Returns the `grant_types` a client wanting refresh tokens SHOULD register: the given grant types plus `refresh_token` (deduplicated) (R-23.19-r). |
| static [MayForwardTokenToServer](AuthorizationSecurity/MayForwardTokenToServer.md)(…) | Returns `true` when a client MAY send the access token it holds for *tokenIssuer* to the MCP server whose authorization server is *serverIssuer* — strictly only when the issuers match exactly. A client MUST NOT send a token to an MCP server other than one issued by that server's authorization server (R-23.19-c). |
| static [RedactToken](AuthorizationSecurity/RedactToken.md)() | Returns a redacted placeholder for a token so diagnostics never carry the secret itself, enforcing token confidentiality at log/forward sinks (R-23.19-m, R-23.19-n, R-23.19-o). Returns a fixed marker regardless of input, so the secret is never embedded. |
| static [SameRequestRecord](AuthorizationSecurity/SameRequestRecord.md)(…) | Asserts that the recorded issuer, PKCE code verifier, and `state` are all present in the same per-request record — the §23.19 storage invariant (R-23.19-j). An empty field means the record is incomplete and the flow MUST NOT proceed. |
| static [ServerScopesOmitOfflineAccess](AuthorizationSecurity/ServerScopesOmitOfflineAccess.md)(…) | Validates that a server (protected resource) does NOT include `offline_access` in its `WWW-Authenticate``scope` or in its `scopes_supported`, as a server SHOULD ensure — refresh tokens are not a resource requirement (R-23.19-u). |
| static [ValidateExactIssuer](AuthorizationSecurity/ValidateExactIssuer.md)(…) | Validates the authorization response's `iss` against the recorded issuer by exact string comparison — the mix-up defence a client MUST perform BEFORE transmitting the authorization code, including the `authorization_response_iss_parameter_supported` reject rule (R-23.19-e – R-23.19-h). Delegates to S36's [`ValidateIssuer`](./AuthorizationRedirect/ValidateIssuer.md) (the §23.7 decision table). |
| static [WithOfflineAccessIfAdvertised](AuthorizationSecurity/WithOfflineAccessIfAdvertised.md)(…) | Adds `offline_access` to a scope list when, and only when, the authorization-server metadata advertises it in `scopes_supported`, for a client that wants a refresh token (R-23.19-s). The result is deduplicated. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
