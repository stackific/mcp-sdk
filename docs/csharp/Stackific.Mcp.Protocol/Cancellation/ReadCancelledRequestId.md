# Cancellation.ReadCancelledRequestId method

Reads the `requestId` a `notifications/cancelled` targets from its raw `params` (§15.2.2), returning `null` when the params are absent, malformed, or omit the id — a cancellation that omits the target is simply ignored (R-15.2.2-f), never an error. This is the parse a server transport uses to find the in-flight request to abort.

```csharp
public static RequestId? ReadCancelledRequestId(JsonObject? parameters)
```

| parameter | description |
| --- | --- |
| parameters | The notification's `params` object, or `null`. |

## Return Value

The targeted request id, or `null` when absent/malformed.

## See Also

* struct [RequestId](../../Stackific.Mcp.JsonRpc/RequestId.md)
* class [Cancellation](../Cancellation.md)
* namespace [Stackific.Mcp.Protocol](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
