# WwwAuthenticate class

Builders, the parser, and scope helpers for the `Bearer``WWW-Authenticate` challenge (spec §23.1, R-23.1-t – R-23.1-ad).

```csharp
public static class WwwAuthenticate
```

## Public Members

| name | description |
| --- | --- |
| static [BuildInsufficientScopeValue](WwwAuthenticate/BuildInsufficientScopeValue.md)(…) | Builds an MCP server's `403 Forbidden` insufficient-scope `WWW-Authenticate` header value carrying `error="insufficient_scope"`, the `scope`, and `resource_metadata` (R-23.1-ab), with an OPTIONAL `error_description` (R-23.1-ad) (spec §23.1, R-23.1-aa – R-23.1-ad). The caller SHOULD pass the union of all scopes the operation needs so this is a single, complete challenge (R-23.1-ac). |
| static [BuildUnauthorizedValue](WwwAuthenticate/BuildUnauthorizedValue.md)(…) | Builds an MCP server's `401 Unauthorized``WWW-Authenticate` header value with the REQUIRED `resource_metadata` parameter (R-23.1-v) and the SHOULD-present `scope` (R-23.1-w) (spec §23.1, R-23.1-t – R-23.1-w). |
| static [BuildValue](WwwAuthenticate/BuildValue.md)(…) | Builds the `WWW-Authenticate` header value for a `Bearer` challenge from its structured fields (R-23.1-u – R-23.1-w, R-23.1-ab – R-23.1-ad). Parameters are emitted in a stable order — `error`, `scope`, `resource_metadata`, `error_description` — each only when present; the `Bearer` scheme always leads. |
| static [ChallengedScopes](WwwAuthenticate/ChallengedScopes.md)(…) | Resolves the scopes a client MUST treat as required for the request from a challenge (R-23.1-x, R-23.1-y). The challenged scope set is authoritative; this derives the required scopes SOLELY from the challenge's `scope`, never from `scopes_supported`. Returns an empty list when the challenge carried no `scope`. |
| static [FromHeaders](WwwAuthenticate/FromHeaders.md)(…) | Extracts the parsed `Bearer` challenge from a case-insensitive header lookup, or `null` when there is no parseable `WWW-Authenticate``Bearer` challenge (R-23.1-z). |
| static [IsInsufficientScopeChallenge](WwwAuthenticate/IsInsufficientScopeChallenge.md)(…) | Returns `true` when *challenge* is an insufficient-scope (`403`) challenge (R-23.1-ab). |
| static [Parse](WwwAuthenticate/Parse.md)(…) | Parses a `WWW-Authenticate` header value carrying a `Bearer` challenge into its structured fields (R-23.1-z). A client MUST be able to parse `WWW-Authenticate` headers and react to a `401`; this is that parser. |

## See Also

* namespace [Stackific.Mcp.Protocol](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
