# Cimd class

Client ID Metadata Document validation — the `https`-with-path `client_id` URL rule, the schema, the `client_id == URL` identity rule, and the redirect-URI membership check (spec §23.4, R-23.4-d – R-23.4-l).

```csharp
public static class Cimd
```

## Public Members

| name | description |
| --- | --- |
| static [IsValid](Cimd/IsValid.md)(…) | Returns `true` when *value* is a structurally valid CIMD document (R-23.4-f). |
| static [IsValidClientIdUrl](Cimd/IsValidClientIdUrl.md)(…) | Returns `true` when *clientId* is a syntactically valid CIMD `client_id` URL: an absolute `https` URL with a (non-root) path component (R-23.4-e). A bare-origin URL (path `/`) is rejected. |
| static [Parse](Cimd/Parse.md)(…) | Parses a CIMD document, returning `null` when it is not a JSON object with the REQUIRED `client_id`, `client_name`, and non-empty `redirect_uris` (R-23.4-f). |
| static [Validate](Cimd/Validate.md)(…) | Validates a fetched CIMD document against the URL it was fetched from — the fetch/validate duties an authorization server performs on encountering a URL-formatted `client_id` (R-23.4-i, R-23.4-j, R-23.4-k). On success *document* is populated. |

## See Also

* namespace [Stackific.Mcp.Protocol](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
