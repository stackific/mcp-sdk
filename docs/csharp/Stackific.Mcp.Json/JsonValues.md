# JsonValues class

The JSON value model and numeric-handling helpers for MCP (spec §2.3, §2.5).

```csharp
public static class JsonValues
```

## Public Members

| name | description |
| --- | --- |
| const [SafeIntegerMax](JsonValues/SafeIntegerMax.md) | Inclusive upper bound for safe identifiers and counters: `2^53 − 1` (§2.5, R-2.5-c). Equal to JavaScript's `Number.MAX_SAFE_INTEGER`. |
| const [SafeIntegerMin](JsonValues/SafeIntegerMin.md) | Inclusive lower bound for safe identifiers and counters: `−(2^53 − 1)` (§2.5, R-2.5-c). Equal to JavaScript's `Number.MIN_SAFE_INTEGER`. |
| static [AssertInteger](JsonValues/AssertInteger.md)(…) | Asserts that *n* has no fractional part. Throws when a fractional value is supplied where an integer field is required. (R-2.5-b, AC-02.13) |
| static [AssertSafeInteger](JsonValues/AssertSafeInteger.md)(…) | Asserts that *n* is within the safe-integer range. Senders MUST NOT emit identifier or counter values outside this range. (R-2.5-d) |
| static [IsInteger](JsonValues/IsInteger.md)(…) | Returns `true` when *n* has no fractional part (it may still be outside the safe-integer range). Used to validate integer-typed fields. (R-2.5-a, R-2.5-b, AC-02.13) |
| static [IsJsonValue](JsonValues/IsJsonValue.md)(…) | Returns `true` when *value* is a valid JSON value — one of the six wire forms (string, number, boolean, null, object, array) — recursively. Useful as a runtime guard at a system boundary. (R-2.3-a, AC-02.1) |
| static [IsSafeInteger](JsonValues/IsSafeInteger.md)(…) | Returns `true` when *n* is a safe integer: it has no fractional part and lies within the safe-integer range. (R-2.5-c, R-2.5-e, AC-02.14) |
| static [LastDuplicateWins](JsonValues/LastDuplicateWins.md)(…) | Builds a JsonObject from a sequence of name/value pairs, applying the last-duplicate-wins rule (§2.3.1, R-2.3.1-c, AC-02.3). |
| static [NumericEqual](JsonValues/NumericEqual.md)(…) | Returns `true` when *a* and *b* are numerically equal regardless of their textual JSON representation (for example `1e2` equals `100`, and `1.0` equals `1`). Two numerically equal JSON numbers MUST be treated as equal. (R-2.5-g, AC-02.15) |

## Remarks

Every value that crosses the wire is exactly one of the six JSON forms — string, number, boolean, null, object, or array (§2.3). This SDK represents those forms with JsonNode and its derivatives rather than a bespoke union type, so the helpers here operate on JsonNode where a runtime guard is needed.

Numeric handling (§2.5): identifiers and counters (request ids, error codes, progress counters, pagination counters) MUST stay within the IEEE 754 safe-integer range `−9007199254740991` to `9007199254740991`, so a number round-trips through a double-precision float without losing precision. These bounds and the predicates that enforce them mirror `json/value.ts`.

## See Also

* namespace [Stackific.Mcp.Json](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
