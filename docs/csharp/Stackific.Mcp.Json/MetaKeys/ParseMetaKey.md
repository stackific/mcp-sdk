# MetaKeys.ParseMetaKey method

Splits a `_meta` key into its prefix (if any) and name using the first slash as the separator. The prefix includes the trailing slash; the name is everything after it. A key with no slash has a `null` prefix and the whole key as its name.

```csharp
public static ParsedMetaKey ParseMetaKey(string key)
```

| parameter | description |
| --- | --- |
| key | The key to split. |

## Return Value

The parsed prefix and name.

## Remarks

Because only the first slash separates the two parts, a key such as `a.b/c/d` parses to `{ Prefix = "a.b/", Name = "c/d" }`.

## See Also

* struct [ParsedMetaKey](../MetaKeys.ParsedMetaKey.md)
* class [MetaKeys](../MetaKeys.md)
* namespace [Stackific.Mcp.Json](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
