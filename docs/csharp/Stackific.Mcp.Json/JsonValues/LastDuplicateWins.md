# JsonValues.LastDuplicateWins method

Builds a JsonObject from a sequence of name/value pairs, applying the last-duplicate-wins rule (§2.3.1, R-2.3.1-c, AC-02.3).

```csharp
public static JsonObject LastDuplicateWins(IEnumerable<KeyValuePair<string, JsonNode?>> entries)
```

| parameter | description |
| --- | --- |
| entries | The ordered name/value pairs; later names overwrite earlier ones. |

## Return Value

An object in which each name maps to the value of its last occurrence.

## Remarks

When a receiver does not reject an object with duplicate member names as malformed, it MUST behave as though only the last occurrence is present. This helper makes that behaviour explicit and testable. Each value is deep-cloned before insertion so that a node already parented elsewhere can be supplied safely.

## See Also

* class [JsonValues](../JsonValues.md)
* namespace [Stackific.Mcp.Json](../../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
