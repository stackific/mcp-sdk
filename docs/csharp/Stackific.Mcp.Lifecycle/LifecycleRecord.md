# LifecycleRecord record

Per-feature lifecycle bookkeeping (spec §27.1, §27.2). This is a conceptual governance record, not a wire type — it captures where a feature sits in its lifecycle and the metadata the policy rules consume (deprecation date, earliest-removal revision, migration note, expedited flag).

```csharp
public record LifecycleRecord
```

## Public Members

| name | description |
| --- | --- |
| [DeprecatedSince](LifecycleRecord/DeprecatedSince.md) { get; set; } | ISO-8601 (`YYYY-MM-DD`) date on which the feature first became Deprecated. Present only when [`State`](./LifecycleRecord/State.md) is Deprecated. |
| [EarliestRemoval](LifecycleRecord/EarliestRemoval.md) { get; set; } | The protocol revision on or after which the feature becomes eligible for removal (R-27.2-c). |
| [Expedited](LifecycleRecord/Expedited.md) { get; set; } | Whether a security-driven, shortened deprecation window applies (a minimum of 90 days rather than the standard 12 months) (R-27.2-k, R-27.2-l). Defaults to `false`. |
| [Feature](LifecycleRecord/Feature.md) { get; set; } | Identifier of the governed feature (a method, capability, type, etc.). |
| [Migration](LifecycleRecord/Migration.md) { get; set; } | The documented migration path, or `"none required"`. REQUIRED when the feature is Deprecated (R-27.2-g). |
| [State](LifecycleRecord/State.md) { get; set; } | The feature's current lifecycle state. |

## Remarks

Mirrors the TypeScript `LifecycleRecord` interface. [`DeprecatedSince`](./LifecycleRecord/DeprecatedSince.md) and [`Migration`](./LifecycleRecord/Migration.md) are present only when the feature is Deprecated; [`Migration`](./LifecycleRecord/Migration.md) is REQUIRED when Deprecated (R-27.2-g) and may be the literal `"none required"`.

## See Also

* namespace [Stackific.Mcp.Lifecycle](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
