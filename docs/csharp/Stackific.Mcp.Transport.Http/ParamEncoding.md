# ParamEncoding class

S14 — `Mcp-Param-*` value encoding (spec §9.5.3).

```csharp
public static class ParamEncoding
```

## Public Members

| name | description |
| --- | --- |
| const [Base64SentinelPrefix](ParamEncoding/Base64SentinelPrefix.md) | The exact (lowercase) sentinel prefix. (R-9.5.3-c) |
| const [Base64SentinelSuffix](ParamEncoding/Base64SentinelSuffix.md) | The exact (lowercase) sentinel suffix. (R-9.5.3-c) |
| const [MaxSafeAnnotatedInteger](ParamEncoding/MaxSafeAnnotatedInteger.md) | The widest integer that may safely carry the `x-mcp-header` annotation: `2^53 − 1`. (R-9.5.1-g) Equal to JavaScript's `Number.MAX_SAFE_INTEGER`. |
| const [MinSafeAnnotatedInteger](ParamEncoding/MinSafeAnnotatedInteger.md) | The smallest integer that may safely carry the `x-mcp-header` annotation: `−(2^53 − 1)`. (R-9.5.1-g) Equal to JavaScript's `Number.MIN_SAFE_INTEGER`. |
| static [DecodeHeaderValue](ParamEncoding/DecodeHeaderValue.md)(…) | Decodes a header value back to its string form, decoding the Base64 payload first when the sentinel is present. (R-9.5.3-d) This is the exact inverse of [`EncodeHeaderValue`](./ParamEncoding/EncodeHeaderValue.md). |
| static [EncodeHeaderValue](ParamEncoding/EncodeHeaderValue.md)(…) | Encodes a string parameter value into its header-value form. (§9.5.3) Returns the plain string when it is safe ASCII; otherwise the `=?base64?{payload}?=` sentinel form. (R-9.5.3-a/b/e) (3 methods) |
| static [IsAnnotatedIntegerInRange](ParamEncoding/IsAnnotatedIntegerInRange.md)(…) | Returns `true` when *value* is an integer within the safe annotated range. (R-9.5.1-g) |
| static [IsSentinelEncoded](ParamEncoding/IsSentinelEncoded.md)(…) | Returns `true` when *headerValue* is wrapped in the Base64 sentinel. |
| static [NeedsSentinel](ParamEncoding/NeedsSentinel.md)(…) | Returns `true` when *plain* cannot be safely carried as a plain ASCII header value and so MUST be sentinel-encoded. (R-9.5.3-b, R-9.5.3-e) |
| static [PlainStringForm](ParamEncoding/PlainStringForm.md)(…) | Returns the per-type plain string form of a string parameter value (string → as-is). (R-9.5.3-a) (3 methods) |
| static [SentinelEncode](ParamEncoding/SentinelEncode.md)(…) | Wraps the UTF-8 Base64 of *text* in the sentinel form. (R-9.5.3-b, R-9.5.3-c) |

## Remarks

A client MUST encode each parameter value before placing it in a header, to ensure safe transmission and prevent injection. The per-type string form is:

* `string` → as-is.
* `integer` → its decimal string (`42`, `-7`).
* `boolean` → lowercase `true` / `false`.

When that string cannot be carried safely as a plain ASCII header value — it has non-ASCII or control characters, leading/trailing whitespace, or it itself looks like the sentinel — the client Base64-encodes the UTF-8 bytes and wraps the result as `=?base64?{payload}?=` (lowercase, exact). A receiver detects the sentinel and decodes it before use. This mirrors the TypeScript SDK's `transport/http/param-encoding.ts` exactly, including the self-collision guard (R-9.5.3-e): a literal value that already looks like the sentinel is re-encoded so it round-trips unambiguously.

## See Also

* namespace [Stackific.Mcp.Transport.Http](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
