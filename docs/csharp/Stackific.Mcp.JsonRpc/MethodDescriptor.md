# MethodDescriptor class

Describes a method that a receiver recognizes, for use by [`DispatchRequest`](./Dispatch/DispatchRequest.md).

```csharp
public sealed class MethodDescriptor
```

## Public Members

| name | description |
| --- | --- |
| [MethodDescriptor](MethodDescriptor/MethodDescriptor.md)() | The default constructor. |
| [ParamsValidator](MethodDescriptor/ParamsValidator.md) { get; set; } | Optional validator for the incoming `params` object. When provided and the request's `params` fails validation (the delegate returns `false`), [`DispatchRequest`](./Dispatch/DispatchRequest.md) returns an invalid-params error response. (R-3.3-k) |
| [RequiresParams](MethodDescriptor/RequiresParams.md) { get; set; } | When `true`, the `params` object MUST be present on every request to this method. This covers the case where a method's per-request `_meta` is REQUIRED — `params` must be provided to carry it. A request that omits `params` for such a method is rejected with an invalid-params error response. (R-3.3-i) |

## Remarks

A method is registered by name in a [`MethodRegistry`](./MethodRegistry.md); an absent entry causes [`DispatchRequest`](./Dispatch/DispatchRequest.md) to produce a method-not-found error response (R-3.3-j).

## See Also

* namespace [Stackific.Mcp.JsonRpc](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
