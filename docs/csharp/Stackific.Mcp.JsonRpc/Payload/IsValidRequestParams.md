# Payload.IsValidRequestParams method

Returns `true` when *node* is a valid base `RequestParams` (§3.7): a JSON object whose `_meta` member is present AND is a JSON object. Additional method-specific members are allowed (passthrough). (R-3.7-a)

```csharp
public static bool IsValidRequestParams(JsonNode? node)
```

| parameter | description |
| --- | --- |
| node | The candidate params node. |

## Return Value

`true` when the node is a valid request-params base.

## Remarks

`_meta` is REQUIRED on request params because it conveys per-request protocol state (protocol revision, client info, capabilities, …). An empty `_meta` object is accepted; the protocol-specific reserved keys are validated elsewhere.

## See Also

* class [Payload](../Payload.md)
* namespace [Stackific.Mcp.JsonRpc](../../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
