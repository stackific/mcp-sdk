# ResultTypeInterpretation structure

The outcome of interpreting a received result's `resultType` field (§3.6).

```csharp
public struct ResultTypeInterpretation : IEquatable<ResultTypeInterpretation>
```

| parameter | description |
| --- | --- |
| Recognized | `true` when [`ResultType`](./ResultTypeInterpretation/ResultType.md) is one of the spec-defined values, or when the field was absent/null and therefore defaulted to `"complete"`. When `false`, the receiver MUST treat the whole response as an error and MUST NOT read any other result members (R-3.6-f, R-3.6-g). |
| ResultType | The resolved `resultType` string. |

## Public Members

| name | description |
| --- | --- |
| [ResultTypeInterpretation](ResultTypeInterpretation/ResultTypeInterpretation.md)(…) | The outcome of interpreting a received result's `resultType` field (§3.6). |
| [Recognized](ResultTypeInterpretation/Recognized.md) { get; set; } | `true` when [`ResultType`](./ResultTypeInterpretation/ResultType.md) is one of the spec-defined values, or when the field was absent/null and therefore defaulted to `"complete"`. When `false`, the receiver MUST treat the whole response as an error and MUST NOT read any other result members (R-3.6-f, R-3.6-g). |
| [ResultType](ResultTypeInterpretation/ResultType.md) { get; set; } | The resolved `resultType` string. |

## See Also

* namespace [Stackific.Mcp.JsonRpc](../README.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
