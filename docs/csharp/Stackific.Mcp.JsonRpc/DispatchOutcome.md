# DispatchOutcome structure

The result of attempting to dispatch a request: either success, or a failure carrying the error response that should be sent back to the requester.

```csharp
public struct DispatchOutcome : IEquatable<DispatchOutcome>
```

| parameter | description |
| --- | --- |
| Ok | `true` when the request can be dispatched; `false` when it must be rejected. |
| Response | The error response to return when [`Ok`](./DispatchOutcome/Ok.md) is `false`; otherwise `null`. |

## Public Members

| name | description |
| --- | --- |
| [DispatchOutcome](DispatchOutcome/DispatchOutcome.md)(…) | The result of attempting to dispatch a request: either success, or a failure carrying the error response that should be sent back to the requester. |
| static [Success](DispatchOutcome/Success.md) { get; } | The successful outcome — the request is dispatchable. |
| static [Failure](DispatchOutcome/Failure.md)(…) | Builds a failed outcome carrying the rejection *response*. |
| [Ok](DispatchOutcome/Ok.md) { get; set; } | `true` when the request can be dispatched; `false` when it must be rejected. |
| [Response](DispatchOutcome/Response.md) { get; set; } | The error response to return when [`Ok`](./DispatchOutcome/Ok.md) is `false`; otherwise `null`. |

## See Also

* namespace [Stackific.Mcp.JsonRpc](../Stackific.Mcp.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stackific.Mcp.dll -->
